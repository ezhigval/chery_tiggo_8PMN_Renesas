# ✅ ГОТОВО К ЗАПУСКУ!

## 🎯 Ответ на ваш вопрос

**НЕ НУЖНО устанавливать образы в систему!**
**НЕ НУЖНО распаковывать файловые системы!**

**Образы используются напрямую через QEMU!**

## Как это работает

### Механизм работы

1. **QEMU монтирует образы как виртуальные диски:**
   ```
   system.img  → /dev/vda  (внутри QEMU)
   vendor.img  → /dev/vdb  (внутри QEMU)
   product.img → /dev/vdc  (внутри QEMU)
   ```

2. **Android kernel автоматически монтирует их:**
   ```
   /dev/vda → /system   (автоматически)
   /dev/vdb → /vendor   (автоматически)
   /dev/vdc → /product  (автоматически)
   ```

3. **Все работает "из коробки":**
   - Образы остаются нетронутыми
   - Никаких изменений в образах
   - Все изменения в userdata.img (создается автоматически)

### Почему это работает

- **ext2 образы** - это raw файловые системы БЕЗ MBR/GPT
- **QEMU с `format=raw`** монтирует их напрямую
- **Android** автоматически распознает и монтирует разделы
- **Никакой установки не требуется!**

## ✅ Текущий статус

### Проверка готовности

Запустите:
```bash
cd development/emulator
python3 CHECK_READY.py
```

**Результат проверки:**
- ✅ Все образы найдены и валидны
- ✅ QEMU установлен
- ⚠️ Требуется установить Python пакеты

### Установка зависимостей

```bash
cd development/emulator
pip install -r requirements.txt
```

Или по отдельности:
```bash
pip install PyQt6 loguru psutil python-can pexpect pyyaml
```

## 🚀 Запуск

### Вариант 1: GUI эмулятор (РЕКОМЕНДУЕТСЯ)

```bash
cd development/emulator
python main.py
```

Затем:
1. Нажмите **"Start"**
2. Дождитесь загрузки (может занять время для 12GB product.img)
3. Подключитесь к VNC:
   - Display 1: `vncviewer localhost:5900`
   - Display 2: `vncviewer localhost:5901`

### Вариант 2: Скрипт запуска

```bash
cd development/scripts
./simulate_t18fl3_complete.sh
```

## 📋 Что происходит при запуске

### Автоматически:

1. **Извлечение компонентов:**
   - Kernel из `boot.img`
   - Ramdisk из `boot.img`
   - Сохранение во временную директорию

2. **Создание образов:**
   - `userdata.img` (6GB) - создается автоматически
   - `cache.img` (512MB) - создается автоматически

3. **Запуск QEMU:**
   - Монтирование всех образов как raw дисков
   - Загрузка Android kernel
   - Автоматическое монтирование разделов Android

4. **Запуск системы:**
   - Android загружается
   - Все разделы доступны
   - Система готова к работе

## ⚠️ Важные моменты

### Образы НЕ изменяются

- ✅ Работают в режиме **только для чтения**
- ✅ Все изменения в `userdata.img`
- ✅ Оригинальные образы остаются нетронутыми

### Производительность

- **Первый запуск:** может занять 2-5 минут (загрузка 12GB product.img)
- **Последующие запуски:** быстрее (кэширование)
- **Рекомендуется:** SSD, 8GB+ RAM

### Если нужно изменить образы

**НЕ РЕКОМЕНДУЕТСЯ**, но если нужно:

```bash
# Linux: монтирование ext2
sudo mkdir -p /mnt/t18fl3_system
sudo mount -o loop update_extracted/payload/system.img /mnt/t18fl3_system
# ... изменения ...
sudo umount /mnt/t18fl3_system

# macOS: требует специальные инструменты (FUSE)
```

**⚠️ ВАЖНО:** Изменения в образах могут нарушить целостность системы!

## 🔧 Альтернатива: Распаковка для анализа

Если нужно **только посмотреть содержимое** (без запуска):

```bash
# Linux
sudo mount -o loop,ro update_extracted/payload/system.img /mnt/t18fl3_system
ls /mnt/t18fl3_system
sudo umount /mnt/t18fl3_system

# macOS (требует osxfuse)
# brew install osxfuse
# Используйте специальные инструменты для монтирования ext2
```

Но для **запуска эмулятора это НЕ нужно!**

## ✅ Итог

**ВСЕ ГОТОВО К ЗАПУСКУ БЕЗ УСТАНОВКИ!**

Просто:
1. ✅ Установите Python пакеты: `pip install -r requirements.txt`
2. ✅ Запустите: `python main.py`
3. ✅ Нажмите Start

**Образы используются напрямую через QEMU - никакой установки не требуется!**

