# План разработки мост-сервиса

**Дата:** 2024  
**Версия:** 1.0  
**Статус:** 🔄 В процессе

---

## 📋 Оглавление

1. [Этапы разработки](#этапы-разработки)
2. [Структура проекта](#структура-проекта)
3. [Компоненты сервиса](#компоненты-сервиса)
4. [Графический интерфейс](#графический-интерфейс)
5. [Система дебага](#система-дебага)

---

## 🎯 Этапы разработки

### Этап 1: Базовая структура проекта
**Цель:** Создать основу проекта на базе NS.apk

**Задачи:**
1. ✅ Создать структуру Android проекта
2. ✅ Настроить манифест (разрешения, сервисы, активности)
3. ✅ Создать базовые классы (MainActivity, BridgeService, ConfigManager, BootReceiver)
4. ✅ Настроить Gradle и зависимости
5. ✅ Реализовать автозапуск сервиса в фоне (незаметно для пользователя)
6. ✅ Создать систему логирования (Logger)
7. ✅ Создать файл констант (Constants)

**Результат:** ✅ Работающий базовый сервис, который запускается при старте системы в фоне

---

### Этап 2: Мониторинг Яндекс Навигатора
**Цель:** Научиться получать данные из Яндекс Навигатора

**Задачи:**
1. ⏳ Реализовать AccessibilityService для мониторинга UI
2. ⏳ Реализовать NavigationMonitor для отслеживания состояния навигации
3. ⏳ Реализовать DataExtractor для извлечения данных через Yandex MapKit SDK
4. ⏳ Протестировать извлечение данных (маневры, маршрут, расстояние)

**Результат:** Сервис успешно извлекает данные из Яндекс Навигатора

---

### Этап 3: Отправка данных на приборную панель
**Цель:** Научиться отправлять данные в систему автомобиля

**Важное уточнение:**
- QNX система сама отрисовывает карту на приборной панели на основе данных
- Наше приложение отправляет данные (координаты маршрута, позицию, маневры)
- QNX получает данные через SVMeterInteraction и отрисовывает карту
- Альтернатива: наше приложение отрисовывает упрощенную карту и отправляет bitmap (требует исследования)

**Задачи:**
1. ⏳ Реализовать SystemSender для отправки Broadcast Intent
2. ⏳ Реализовать MapRenderer для отрисовки упрощенной карты (или координат для QNX)
3. ⏳ Реализовать отправку статус бара (километры, время в пути, время прибытия)
4. ⏳ Реализовать отправку данных для проекции (стрелочка маневра, расстояние до маневра и финиша)
5. ⏳ Протестировать отправку данных на приборную панель
6. ⏳ Проверить отображение на реальном устройстве

**Результат:** Данные успешно отправляются и отображаются на приборной панели и проекции

---

### Этап 4: Графический интерфейс настроек
**Цель:** Создать UI для настройки сервиса

**Задачи:**
1. ⏳ Создать SettingsActivity с вкладками
2. ⏳ Реализовать настройку положения элементов (статус бар, карта, текст)
3. ⏳ Реализовать настройку масштаба (карта, текст, элементы, общий)
4. ⏳ Реализовать ползунок автозапуска навигатора
5. ⏳ Сохранение и загрузка настроек

**Результат:** Полнофункциональный интерфейс настроек

---

### Этап 5: Система дебага и логирования
**Цель:** Реализовать систему для отладки и мониторинга

**Задачи:**
1. ⏳ Создать Logger с уровнями логирования
2. ⏳ Реализовать сохранение логов в файл
3. ⏳ Создать LogsActivity для просмотра логов
4. ⏳ Реализовать фильтрацию и поиск по логам
5. ⏳ Реализовать экспорт логов

**Результат:** Полнофункциональная система дебага

---

### Этап 6: Интеграция и тестирование
**Цель:** Полная интеграция всех компонентов

**Задачи:**
1. ⏳ Интеграция всех модулей
2. ⏳ Тестирование на реальном устройстве
3. ⏳ Оптимизация производительности
4. ⏳ Исправление ошибок
5. ⏳ Финальная проверка всех функций

**Результат:** Полностью работающий мост-сервис

---

## 🏗️ Структура проекта

```
TiggoBridgeService/
├── app/
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/desaysv/tiggo/bridge/
│   │   │   │   ├── core/                    # Общие компоненты
│   │   │   │   │   ├── BridgeService.java   # Главный сервис
│   │   │   │   │   ├── ConfigManager.java   # Управление конфигурацией
│   │   │   │   │   ├── Logger.java          # Система логирования
│   │   │   │   │   └── EventBus.java        # Система событий
│   │   │   │   │
│   │   │   │   ├── navigation/              # Модуль навигации
│   │   │   │   │   ├── NavigationMonitor.java
│   │   │   │   │   ├── DataExtractor.java
│   │   │   │   │   ├── MapRenderer.java
│   │   │   │   │   ├── SystemSender.java
│   │   │   │   │   └── NavigationData.java
│   │   │   │   │
│   │   │   │   ├── accessibility/           # Accessibility Service
│   │   │   │   │   └── NavigationAccessibilityService.java
│   │   │   │   │
│   │   │   │   ├── ui/                      # UI компоненты
│   │   │   │   │   ├── MainActivity.java
│   │   │   │   │   ├── SettingsActivity.java
│   │   │   │   │   ├── LogsActivity.java
│   │   │   │   │   └── fragments/
│   │   │   │   │       ├── NavigationSettingsFragment.java
│   │   │   │   │       ├── DisplaySettingsFragment.java
│   │   │   │   │       └── DebugSettingsFragment.java
│   │   │   │   │
│   │   │   │   └── utils/                   # Утилиты
│   │   │   │       ├── DisplayUtils.java
│   │   │   │       ├── IntentUtils.java
│   │   │   │       └── BitmapUtils.java
│   │   │   │
│   │   │   ├── res/
│   │   │   │   ├── layout/
│   │   │   │   │   ├── activity_main.xml
│   │   │   │   │   ├── activity_settings.xml
│   │   │   │   │   ├── activity_logs.xml
│   │   │   │   │   └── fragment_*.xml
│   │   │   │   ├── values/
│   │   │   │   │   ├── strings.xml
│   │   │   │   │   ├── colors.xml
│   │   │   │   │   └── dimens.xml
│   │   │   │   └── drawable/
│   │   │   │
│   │   │   └── AndroidManifest.xml
│   │   │
│   │   └── test/
│   │
│   └── build.gradle
│
└── README.md
```

---

## 📦 Компоненты сервиса

### 1. BridgeService (Главный сервис)

**Назначение:** Foreground Service, который координирует все модули

**Особенности:**
- Запускается при старте системы в фоне (незаметно для пользователя)
- Работает в фоне постоянно
- Управляет жизненным циклом модулей
- Обрабатывает системные события
- Автоматически запускает Яндекс Навигатор при построении маршрута (если включен автозапуск)

**Код:**
```java
public class BridgeService extends Service {
    private NavigationMonitor navigationMonitor;
    private ConfigManager configManager;
    private Logger logger;
    
    @Override
    public void onCreate() {
        super.onCreate();
        
        // Инициализация компонентов
        configManager = new ConfigManager(this);
        logger = new Logger(this);
        navigationMonitor = new NavigationMonitor(this, logger);
        
        // Запуск Foreground Service
        startForeground(NOTIFICATION_ID, createNotification());
    }
    
    @Override
    public int onStartCommand(Intent intent, int flags, int startId) {
        // Запуск модулей
        if (configManager.isAutoStartEnabled()) {
            navigationMonitor.start();
        }
        
        return START_STICKY;
    }
}
```

---

### 2. NavigationMonitor

**Назначение:** Мониторинг Яндекс Навигатора и извлечение данных

**Методы:**
- `start()` - начать мониторинг
- `stop()` - остановить мониторинг
- `isNavigationActive()` - проверка активности навигации
- `getCurrentData()` - получить текущие данные

**Реализация:**
- Использует AccessibilityService для мониторинга UI
- Использует Yandex MapKit SDK для извлечения данных (если доступно)
- Отслеживает изменения через GuidanceListener и WindshieldListener

---

### 3. DataExtractor

**Назначение:** Извлечение навигационных данных

**Методы:**
- `extractFromGuidance(Guidance)` - извлечение из Guidance
- `extractFromWindshield(Windshield)` - извлечение из Windshield
- `extractSpeedLimit(Guidance)` - извлечение ограничения скорости
- `mapActionToTurnType(Action)` - маппинг типов маневров

---

### 4. MapRenderer / StatusBarRenderer

**Назначение:** Отрисовка статус бара для приборной панели

**Важно:** 
- Карта отрисовывается QNX на основе координат маршрута
- Наше приложение отрисовывает только статус бар (который не предусмотрен изначально)

**Методы:**
- `renderStatusBar(StatusBarData, DisplayConfig)` - отрисовка статус бара с учетом настроек
- `renderDistance(Canvas, int)` - отрисовка километров
- `renderTime(Canvas, int)` - отрисовка времени в пути
- `renderArriveTime(Canvas, String)` - отрисовка времени прибытия

**Настройки:**
- Положение статус бара (X, Y координаты)
- Масштаб текста
- Масштаб элементов
- Общий масштаб

---

### 5. SystemSender

**Назначение:** Отправка данных в систему автомобиля

**Методы:**
- `sendNavigationData(NavigationData)` - отправка навигационных данных (координаты маршрута для QNX)
- `sendStatusBar(StatusBarData)` - отправка статус бара (bitmap)
- `sendProjectionData(ProjectionData)` - отправка данных для проекции (стрелочка, расстояние)
- `sendSpeedLimit(int)` - отправка ограничения скорости

**Формат:**
- Broadcast Intent: `turbodog.navigation.system.message`
- Поля: 
  - Для карты: ROUTE_COORDINATES, CURRENT_LAT, CURRENT_LON, BEARING
  - Для маневра: TURN_TYPE, TURN_DIST, TURN_LAT, TURN_LON
  - Для маршрута: REMAINING_DIST, REMAINING_TIME
  - Для статус бара: STATUS_BAR_IMAGE, STATUS_BAR_X, STATUS_BAR_Y
  - Для проекции: PROJECTION_TURN_TYPE, PROJECTION_TURN_DIST, PROJECTION_REMAINING_DIST
  - Для ограничения скорости: SPEED_LIMIT

---

## 🎨 Графический интерфейс

### MainActivity

**Назначение:** Главный экран приложения

**Элементы:**
- Кнопка "Настройки" - переход в SettingsActivity
- Кнопка "Логи" - переход в LogsActivity
- Индикатор статуса сервиса (работает/остановлен)
- Переключатель автозапуска

---

### SettingsActivity

**Назначение:** Настройки сервиса

**Вкладки:**

#### 1. Навигация
- Переключатель автозапуска навигатора
- Выбор package name навигатора
- Настройки мониторинга

#### 2. Отображение
- **Положение элементов:**
  - Статус бар (X, Y координаты) - единственный элемент, который мы позиционируем
  - Карта отрисовывается QNX автоматически
  - Текст и иконки отрисовываются QNX автоматически
- **Масштаб:**
  - Масштаб статус бара (0.5x - 2.0x)
  - Масштаб текста в статус баре (0.5x - 2.0x)
  - Общий масштаб статус бара (0.5x - 2.0x)
- **Предпросмотр:** Мини-превью статус бара с текущими настройками

#### 3. Дебаг
- Уровень логирования (DEBUG, INFO, WARN, ERROR)
- Размер файла логов (MB)
- Автоочистка старых логов
- Экспорт логов

---

### LogsActivity

**Назначение:** Просмотр логов

**Функции:**
- Отображение логов в реальном времени
- Фильтрация по уровню (DEBUG, INFO, WARN, ERROR)
- Поиск по тексту
- Автопрокрутка к новым логам
- Экспорт логов в файл
- Очистка логов

**Интерфейс:**
- RecyclerView для отображения логов
- Фильтры сверху
- Кнопка экспорта/очистки

---

## 🔍 Система дебага

### Logger

**Назначение:** Централизованная система логирования

**Уровни:**
- `DEBUG` - отладочная информация
- `INFO` - информационные сообщения
- `WARN` - предупреждения
- `ERROR` - ошибки

**Функции:**
- Запись в файл
- Запись в Logcat
- Ротация логов (при достижении размера)
- Фильтрация по уровню
- Экспорт логов

**Код:**
```java
public class Logger {
    private static final String LOG_FILE = "tiggo_bridge.log";
    private static final int MAX_LOG_SIZE = 10 * 1024 * 1024; // 10 MB
    
    public void debug(String tag, String message) {
        log(LogLevel.DEBUG, tag, message);
    }
    
    public void info(String tag, String message) {
        log(LogLevel.INFO, tag, message);
    }
    
    public void warn(String tag, String message) {
        log(LogLevel.WARN, tag, message);
    }
    
    public void error(String tag, String message, Throwable throwable) {
        log(LogLevel.ERROR, tag, message, throwable);
    }
    
    private void log(LogLevel level, String tag, String message, Throwable throwable) {
        // Запись в Logcat
        android.util.Log.println(level.getPriority(), tag, message);
        
        // Запись в файл
        writeToFile(level, tag, message, throwable);
    }
}
```

---

### LogEntry

**Структура:**
```java
public class LogEntry {
    private long timestamp;
    private LogLevel level;
    private String tag;
    private String message;
    private String stackTrace;
    
    // Getters and setters
}
```

---

### LogStorage

**Назначение:** Хранение и управление логами

**Функции:**
- Сохранение логов в файл
- Загрузка логов из файла
- Ротация логов
- Очистка старых логов
- Экспорт логов

---

## 📋 Манифест

### Разрешения

```xml
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
<uses-permission android:name="android.permission.REORDER_TASKS" />
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
```

### Сервисы

```xml
<service
    android:name=".core.BridgeService"
    android:enabled="true"
    android:exported="false"
    android:foregroundServiceType="dataSync" />

<service
    android:name=".accessibility.NavigationAccessibilityService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
```

---

## 🔗 Связанные документы

- [Техническое задание](../ТЕХНИЧЕСКОЕ_ЗАДАНИЕ.md)
- [Архитектура Bridge Service](analysis/07_АРХИТЕКТУРА_BRIDGE_SERVICE.md)
- [Анализ NS.apk](analysis/02_АНАЛИЗ_NS_APK.md)
- [Анализ Яндекс Навигатора](analysis/01_АНАЛИЗ_ЯНДЕКС_НАВИГАТОРА.md)

---

**Дата:** 2024  
**Версия:** 1.0  
**Статус:** 🔄 В процессе

