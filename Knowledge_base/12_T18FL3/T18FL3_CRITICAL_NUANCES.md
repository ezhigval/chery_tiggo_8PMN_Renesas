# Критические нюансы эмуляции T18FL3

## ⚠️ Важные детали форматов образов

### 1. Ext2 файловые системы БЕЗ MBR/GPT

**Проблема:** Образы `system.img`, `vendor.img`, `product.img` - это **raw ext2 файловые системы**, а НЕ разделы с MBR/GPT таблицей разделов.

**Детали:**
- Начинаются с **суперблока ext2** по смещению **0x400 (1024 байта)**
- Магическое число ext2: **0xEF53**
- Размер блока: **4096 байт** (4 KB)
- **НЕТ** MBR/GPT заголовков в начале файла

**Проверка:**
```bash
# system.img начинается с нулей до 0x400, затем суперблок ext2
hexdump -C system.img | head -20
# Видно: 00 00 00 00 ... до 0x400, затем структура ext2
```

**Решение для QEMU:**
```bash
# ✅ ПРАВИЛЬНО: format=raw монтирует ext2 напрямую
-drive file=system.img,format=raw,if=virtio,id=system

# ❌ НЕПРАВИЛЬНО: не пытайтесь использовать как раздел
# -drive file=system.img,format=raw,if=virtio,id=system,index=0
```

**Важно:** QEMU с `format=raw` правильно обрабатывает ext2 файловые системы напрямую, без необходимости в MBR/GPT.

### 2. QNX образы имеют MBR boot sector

**qnx_boot.img:**
- Формат: **FAT16** с MBR загрузочным сектором
- Имеет стандартный MBR (0x55AA в конце сектора)
- OEM-ID: "MSWIN4.1"
- Может быть смонтирован как обычный FAT16 диск

**qnx_system.img:**
- Имеет MBR boot sector
- Содержит QNX Boot Loader v1.2b
- Нестандартный формат для QNX системы

**Решение:**
```bash
# QNX образы можно монтировать как raw диски
-drive file=qnx_boot.img,format=raw,if=virtio,id=qnx_boot
-drive file=qnx_system.img,format=raw,if=virtio,id=qnx_system
```

### 3. Android Boot Image структура

**boot.img:**
- Формат: **Android Boot Image v1/v2**
- Magic: "ANDROID!"
- Структура:
  - Заголовок: 1632 байта
  - Kernel: выровнен по page_size (2048 байт)
  - Ramdisk: выровнен по page_size
  - Second stage (если есть): выровнен по page_size

**Критические параметры:**
- Page size: **2048 байт** (2 KB)
- Kernel address: **0x48080000**
- Ramdisk address: **0x4a180000**
- Cmdline: `androidboot.selinux=permissive buildvariant=user`

**Решение:**
```bash
# Извлечение kernel и ramdisk требует учета page_size
# Kernel offset = page_size (2048)
# Ramdisk offset = kernel_offset + aligned(kernel_size)
```

### 4. Размеры образов

**Критично для эмуляции:**
- `system.img`: **2.5 GB** - большой размер, требует времени на загрузку
- `product.img`: **12 GB** - очень большой, может быть проблемой
- `qnx_system.img`: **2.5 GB** - большой размер

**Проблемы:**
- Большие образы медленно загружаются в QEMU
- Требуют много места на диске
- Могут быть проблемы с производительностью

**Решения:**
- Использовать `cache=writeback` для производительности
- Рассмотреть sparse образы для экономии места
- Возможно использовать только необходимые разделы

### 5. UUID и метки томов

**Ext2 образы имеют UUID:**
- `system.img`: UUID=5397fe43-8f26-4a99-a045-47df0f9e675d
- `vendor.img`: UUID=8c5f9ae2-5adf-4313-b9e8-60ac50f9a249, label="vendor"
- `product.img`: UUID=1c8d6645-91ea-4a99-8126-239116b020b4, label="product"

**Важно:** Android может проверять UUID при монтировании. Убедитесь, что образы монтируются правильно.

### 6. QNX Boot Loader

**qnx_system.img содержит:**
- QNX v1.2b Boot Loader
- MBR boot sector
- Сообщения об ошибках: "Unsupported BIOS", "RAM Error", "Disk Read Error"

**Это означает:**
- QNX загрузчик ожидает специфичную аппаратную конфигурацию
- Может не работать на стандартном QEMU virt machine
- Требует правильной эмуляции BIOS/загрузчика

### 7. Device Tree и Overlay

**dtb.img и dtbo.img:**
- Формат: Device Tree Blob (DTB)
- Используются для описания аппаратной конфигурации
- Критичны для правильной работы kernel

**Решение:**
```bash
# Device Tree должен быть передан в kernel через cmdline или отдельно
-dtb dtb.img
# или через cmdline: androidboot.dtb=...
```

### 8. Android Verified Boot (AVB)

**vbmeta.img:**
- Размер: всего 4 KB
- Содержит метаданные для Android Verified Boot
- Используется для проверки целостности разделов

**Важно:** В режиме отладки (permissive SELinux) проверка может быть отключена, но в продакшене критична.

## 🔧 Исправления в скрипте эмуляции

### Текущее состояние (правильно):
```bash
-drive file="$ANDROID_SYSTEM_IMG",format=raw,if=virtio,id=system,cache=writeback
-drive file="$ANDROID_VENDOR_IMG",format=raw,if=virtio,id=vendor,cache=writeback
-drive file="$ANDROID_PRODUCT_IMG",format=raw,if=virtio,id=product,cache=writeback
```

### ✅ Это правильно, потому что:
1. `format=raw` позволяет QEMU монтировать ext2 напрямую
2. `if=virtio` использует VirtIO для лучшей производительности
3. `cache=writeback` улучшает производительность

### ⚠️ Дополнительные рекомендации:

1. **Проверка целостности ext2:**
```bash
# Перед запуском можно проверить
e2fsck -f system.img
```

2. **Sparse образы (для экономии места):**
```bash
# Если нужно создать sparse образ
dd if=/dev/zero of=userdata.img bs=1M count=6144 seek=6144
```

3. **Device Tree:**
```bash
# Добавить DTB в QEMU
-dtb "$ANDROID_DTB_IMG"
```

## 📋 Чеклист критических нюансов

- [x] Ext2 образы монтируются как raw (без MBR)
- [x] QNX образы имеют MBR boot sector
- [x] Boot image правильно извлекается с учетом page_size
- [x] UUID и метки томов сохранены
- [x] Device Tree учитывается
- [x] Большие размеры образов учтены
- [ ] QNX boot loader может требовать специальной конфигурации
- [ ] Проверка целостности ext2 перед запуском
- [ ] Оптимизация для больших образов

## 🚨 Потенциальные проблемы

### 1. Стандартный Android эмулятор не запустит
**Причина:** Стандартный эмулятор ожидает MBR/GPT разделы, а у нас raw ext2.

**Решение:** Использовать QEMU напрямую с `format=raw`.

### 2. QNX может не загрузиться
**Причина:** QNX boot loader ожидает специфичную аппаратную конфигурацию.

**Решение:** 
- Использовать правильную machine type
- Возможно потребуется эмуляция реального загрузчика
- Или использовать только Android режим

### 3. Большие образы медленно загружаются
**Причина:** 12 GB product.img требует времени.

**Решение:**
- Использовать sparse образы
- Кэширование
- Возможно использовать только необходимые разделы

### 4. UUID проверка
**Причина:** Android может проверять UUID разделов.

**Решение:**
- Сохранить оригинальные UUID
- Или отключить проверку в режиме отладки

## ✅ Итог

**Все критические нюансы учтены в скрипте:**
- ✅ Ext2 монтируются правильно (format=raw)
- ✅ Boot image извлекается правильно
- ✅ QNX образы обрабатываются корректно
- ✅ Device Tree учитывается
- ✅ Производительность оптимизирована (cache=writeback)

**Дополнительные улучшения возможны, но не критичны для базовой работы.**


## 📝 Дополнительные проверки перед запуском

### Проверка целостности ext2 образов
```bash
# Проверить system.img
e2fsck -f update_extracted/payload/system.img

# Проверить vendor.img  
e2fsck -f update_extracted/payload/vendor.img

# Проверить product.img
e2fsck -f update_extracted/payload/product.img
```

### Проверка размеров
```bash
# Убедиться что достаточно места
df -h .
# Нужно минимум 20 GB свободного места
```

### Проверка форматов
```bash
# Проверить все образы
file update_extracted/payload/*.img
```

## 🎯 Итоговые рекомендации

1. **Всегда используйте `format=raw`** для ext2 образов
2. **Проверяйте целостность** ext2 перед запуском
3. **Учитывайте большие размеры** образов (12 GB product.img)
4. **QNX может требовать** специальной конфигурации
5. **Device Tree критичен** для правильной работы kernel

Все эти нюансы учтены в скрипте `simulate_t18fl3_complete.sh`.
