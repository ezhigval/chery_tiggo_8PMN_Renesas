# Анализ интеграции TurboDog с системой автомобиля

## 📊 Результаты анализа

### Обнаруженная информация:

1. **SVMapService (TurboDog)**
   - Пакет: `com.desaysv.mapservice`
   - Версия: T1D_MapService_02_12._250808_
   - API Level: 28 (Android 9)
   - Разрешения: минимальные (3 разрешения)

2. **SVMeterInteraction (CAN-шина)**
   - Отвечает за взаимодействие с приборной панелью
   - Связь с CAN-шиной
   - Разрешения: Bluetooth, Storage, Network

3. **iFlytek интеграция**
   - Домен навигации: `mapU` (в business.ini)
   - Протокол: JSON (naviType: 1)
   - Голосовое управление навигацией через ассистент

4. **QNX система**
   - Параллельная ОС для критических систем
   - Образы: `qnx_boot.img`, `qnx_system.img`
   - Используется для приборной панели и CAN-шины

---

## 🔍 Гипотезы о работе TurboDog

### Каналы коммуникации (предположительно):

1. **Android Broadcast Intents**
   - SVMapService отправляет события навигации через Broadcast
   - SVMeterInteraction получает эти события
   - Формат: вероятно кастомные Intent actions

2. **AIDL интерфейсы**
   - Системные сервисы могут использовать AIDL для IPC
   - Прямое взаимодействие между сервисами

3. **Shared Memory / Unix Socket**
   - Для обмена данными с QNX системой
   - Через `/dev/shm` или `/tmp`
   - QNX читает данные и отображает на приборной панели

4. **CAN-шина (через нативные библиотеки)**
   - JNI библиотеки (.so файлы) для работы с CAN
   - SVMeterInteraction или отдельный сервис отправляет данные в CAN
   - Приборная панель получает данные через CAN

5. **System Properties**
   - Android System Properties для обмена данными
   - QNX может читать через `/proc/sys/` или аналогичные механизмы

---

## 🎯 Рекомендуемый подход

### Вариант: Bridge Service (Промежуточный сервис)

**Архитектура:**
```
Яндекс Навигатор → YandexNaviBridge → Система (SVMeterInteraction/QNX/CAN)
```

**Преимущества:**
- Минимальные изменения в существующей системе
- Яндекс Навигатор работает как обычное приложение
- Bridge Service перехватывает и преобразует данные
- Легко откатить изменения
- Не нарушает лицензию Яндекс Навигатора

**Что нужно сделать:**

1. **Декомпилировать SVMapService**
   - Извлечь DEX из VDEX
   - Декомпилировать в Java
   - Найти классы, отвечающие за отправку данных

2. **Исследовать протоколы обмена**
   - Какие Broadcast Intents отправляет TurboDog
   - Формат данных
   - Частота обновлений

3. **Разработать Bridge Service**
   - Перехватывать события Яндекс Навигатора
   - Преобразовывать в формат TurboDog
   - Отправлять в систему

4. **Интегрировать с QNX/CAN**
   - Определить механизм обмена с QNX
   - Реализовать отправку в CAN через SVMeterInteraction

---

## 📋 План действий

### Этап 1: Декомпиляция и анализ (КРИТИЧНО)

**Задачи:**
1. Установить JADX (jadx-gui) для декомпиляции
2. Извлечь DEX из SVMapService.vdex
3. Декомпилировать в Java исходники
4. Найти классы навигации:
   - Отправка Broadcast Intents
   - Работа с CAN/QNX
   - Формат данных

**Инструменты:**
- JADX: https://github.com/skylot/jadx
- vdexExtractor: нужно установить или собрать

**Ожидаемый результат:**
- Исходный код SVMapService
- Понимание протоколов обмена
- Формат данных для системы

### Этап 2: Исследование событий Яндекс Навигатора

**Задачи:**
1. Установить Яндекс Навигатор на устройство
2. Запустить навигацию
3. Логировать события через `adb logcat`
4. Найти Broadcast Intents
5. Определить формат данных

**Команды:**
```bash
adb logcat | grep -i yandex
adb logcat | grep -i navi
adb shell dumpsys activity broadcasts | grep yandex
```

**Ожидаемый результат:**
- Список событий Яндекс Навигатора
- Формат данных
- Частота обновлений

### Этап 3: Разработка Bridge Service

**Задачи:**
1. Создать Android проект
2. Реализовать перехват событий Яндекс Навигатора
3. Создать адаптер данных (Яндекс → TurboDog формат)
4. Реализовать отправку в систему

**Технологии:**
- Android Studio
- BroadcastReceiver
- Возможно Accessibility Service
- System Service

### Этап 4: Интеграция с системой

**Задачи:**
1. Интеграция с SVMeterInteraction (Broadcast)
2. Интеграция с QNX (Shared Memory / Socket)
3. Интеграция с CAN (через SVMeterInteraction или нативно)

---

## 🔧 Необходимые инструменты

### Установить:
1. **JADX** - декомпиляция DEX
   - Скачать: https://github.com/skylot/jadx/releases
   - Или через: `choco install jadx` (если есть Chocolatey)

2. **vdexExtractor** (если нет в проекте)
   - Собрать из исходников в `vdexExtractor/`
   - Или скачать готовый бинарник

3. **Android Studio** - для разработки Bridge Service

### Уже есть в проекте:
- ✅ `aapt2` - в `jaecoo/adb/`
- ✅ `adb` - в `jaecoo/adb/`
- ✅ `scrcpy` - для работы с устройством

---

## 📝 Следующие шаги (приоритет)

### Немедленно:

1. **Установить JADX**
   ```bash
   # Скачать с GitHub
   # Или через пакетный менеджер
   ```

2. **Декомпилировать SVMapService**
   ```bash
   # Извлечь DEX (если нужно)
   # Декомпилировать
   jadx -d reverse/SVMapService_src/sources reverse/mounted/extracted/SVMapService/oat/arm64/*.dex
   ```

3. **Исследовать события Яндекс Навигатора**
   - Установить на устройство
   - Запустить навигацию
   - Логировать события

### В ближайшее время:

1. Анализ исходного кода SVMapService
2. Определение протоколов обмена
3. Создание прототипа Bridge Service

---

## ⚠️ Важные замечания

1. **Безопасность**
   - Всегда создавать резервные копии
   - Тестировать на тестовом устройстве
   - Иметь возможность отката

2. **Лицензии**
   - Не нарушать лицензию Яндекс Навигатора
   - Использовать официальные API, если доступны

3. **Совместимость**
   - Учитывать обновления системы
   - Обеспечить обратную совместимость

---

## 📚 Полезные ссылки

- JADX: https://github.com/skylot/jadx
- Android Broadcast: https://developer.android.com/guide/components/broadcasts
- Android AIDL: https://developer.android.com/guide/components/aidl
- QNX IPC: https://www.qnx.com/developers/docs/

---

**Статус:** Анализ завершен, план готов
**Дата:** 2024
**Версия:** 1.0

