# Требования к отображению на приборной панели

## 📋 Обзор

При интеграции Яндекс Навигатора необходимо обеспечить отображение следующих элементов на приборной панели:

1. **Карта** - упрощенная версия карты с маршрутом
2. **Стрелка "это вы"** - индикатор текущего местоположения
3. **Маршрут** - линия маршрута на карте
4. **Строка состояния маршрута** - информация о маршруте (требует нестандартного позиционирования)
5. **Информация о маневрах** - отображается в специальном месте на приборной панели
6. **Ограничение скорости** - берется из Яндекс Навигатора и подставляется вместо значения из "настроек системы"

---

## 🗺️ 1. Карта на приборной панели

### Требования:
- Упрощенная версия карты
- Отображение маршрута
- Индикатор текущего местоположения (стрелка "это вы")
- Обновление в реальном времени

### Реализация:
- Извлечь данные карты из Яндекс Навигатора
- Отрисовать упрощенную карту через `MapRenderer`
- Отправить изображение/данные на приборную панель

### Формат данных (предположительно):
- Изображение карты (bitmap)
- Или координаты для отрисовки на стороне системы

---

## 🧭 2. Стрелка "это вы"

### Требования:
- Индикатор текущего местоположения на карте
- Обновление позиции в реальном времени
- Ориентация по направлению движения

### Реализация:
- Получить текущее местоположение из `Guidance.getLocation()`
- Получить направление движения
- Отправить координаты и угол поворота на приборную панель

---

## 🛣️ 3. Маршрут

### Требования:
- Линия маршрута на карте
- Обновление при изменении маршрута

### Реализация:
- Получить геометрию маршрута из `Guidance.getCurrentRoute().getGeometry()`
- Отправить полилинию маршрута на приборную панель

---

## 📊 4. Строка состояния маршрута

### Требования:
- Информация о маршруте (оставшееся расстояние, время, текущая дорога)
- **Нестандартное позиционирование** - нужно вписать в интерфейс приборной панели

### Данные для отображения:
- Оставшееся расстояние: `RoutePosition.distanceToFinish()`
- Оставшееся время: `RoutePosition.timeToFinish()`
- Текущая дорога: `Guidance.getRoadName()`
- Следующая дорога: из маршрута

### Реализация:
- Извлечь данные из Яндекс Навигатора
- Отправить через Broadcast Intent в формате TurboDog:
  - `MESSAGE_REMAINING_DIST` - оставшееся расстояние
  - `MESSAGE_REMAINING_TIME` - оставшееся время
  - `MESSAGE_CURRENT_ROAD` - текущая дорога
  - `MESSAGE_NEXT_ROAD` - следующая дорога

### Формат отправки:
```java
Intent intent = new Intent("turbodog.navigation.system.message");
intent.putExtra("CODE", 200);
intent.putExtra("REMAINING_DIST", remainingDistance); // в метрах
intent.putExtra("REMAINING_TIME", remainingTime); // в секундах
intent.putExtra("CURRENT_ROAD", currentRoad);
intent.putExtra("NEXT_ROAD", nextRoad);
sendBroadcast(intent);
```

---

## 🚦 5. Информация о маневрах

### Требования:
- Отображается в специальном месте на приборной панели
- Тип маневра (поворот налево/направо и т.д.)
- Расстояние до маневра
- Время до маневра

### Реализация:
- Получить маневры из `Windshield.getManoeuvres()`
- Извлечь данные из первого маневра (ближайший)
- Отправить через Broadcast Intent:
  - `MESSAGE_TURN_TYPE` - тип маневра
  - `MESSAGE_TURN_DIST` - расстояние до маневра
  - `MESSAGE_TURN_TIME` - время до маневра

### Формат отправки:
```java
Intent intent = new Intent("turbodog.navigation.system.message");
intent.putExtra("CODE", 200);
intent.putExtra("TURN_TYPE", turnType); // код типа поворота
intent.putExtra("TURN_DIST", turnDistance); // в метрах
intent.putExtra("TURN_TIME", turnTime); // в секундах
sendBroadcast(intent);
```

### Маппинг типов маневров:
- `Action.LEFT` → код поворота налево
- `Action.RIGHT` → код поворота направо
- `Action.STRAIGHT` → код "прямо"
- `Action.UTURN_LEFT` / `Action.UTURN_RIGHT` → код разворота
- `Action.HARD_LEFT` → код резкого поворота налево
- `Action.HARD_RIGHT` → код резкого поворота направо
- и т.д.

---

## 🚨 6. Ограничение скорости

### Требования:
- Брать ограничение скорости из Яндекс Навигатора
- Подставлять вместо значения из "настроек системы"
- Отображать в специальном "окошке" на приборной панели
- Отображать на спидометре (красная полоска по периметру)
- Возможно отображать на проекции

### Источник данных:
- `Guidance.getSpeedLimit()` - возвращает `LocalizedValue` с ограничением скорости
- `Guidance.getSpeedLimitStatus()` - статус ограничения скорости

### Реализация:
1. **Получить ограничение скорости из Яндекс Навигатора:**
   ```java
   Guidance guidance = getGuidanceInstance();
   LocalizedValue speedLimit = guidance.getSpeedLimit();
   int speedLimitValue = speedLimit.getValue(); // значение в км/ч
   ```

2. **Отправить в систему:**
   - Нужно найти, как система получает ограничение скорости из "настроек"
   - Подставить значение из Яндекс Навигатора
   - Возможно, через Broadcast Intent или System Properties

### Места отображения:
1. **Окошко на приборной панели:**
   - Специальное место для отображения ограничения скорости
   - Обновляется в реальном времени

2. **Спидометр:**
   - Красная полоска по периметру спидометра
   - Показывает ограничение скорости

3. **Проекция (HUD):**
   - Возможно отображение на проекции
   - Нужно найти механизм отправки данных на проекцию

### Исследование:
- Изучить, как система получает ограничение скорости из настроек
- Найти Broadcast Intent или System Property для отправки ограничения скорости
- Изучить SVMeterInteraction.apk для понимания механизма отображения

---

## 🔍 Исследование системы

### Компоненты для изучения:

1. **SVMapService.apk:**
   - Основной сервис навигации TurboDog
   - Отправляет данные на приборную панель
   - Формат данных для карты

2. **SVMeterInteraction.apk:**
   - Взаимодействие с приборной панелью
   - Отображение данных на спидометре
   - Ограничение скорости

3. **Формат данных:**
   - Broadcast Intent: `turbodog.navigation.system.message`
   - System Properties (возможно)
   - Shared Memory / Unix Sockets (для QNX)

### Следующие шаги:

1. ⏳ Декомпилировать SVMapService.apk
2. ⏳ Изучить, как отправляется карта на приборную панель
3. ⏳ Изучить, как отправляется ограничение скорости
4. ⏳ Найти формат данных для карты
5. ⏳ Изучить SVMeterInteraction.apk для понимания механизма отображения

---

## 📝 Выводы

### Известно:
- ✅ Формат Broadcast Intent для навигационных данных
- ✅ Методы получения данных из Яндекс Навигатора
- ✅ Структура данных (маневры, маршрут, ограничение скорости)

### Требует исследования:
- ⏳ Механизм отправки карты на приборную панель
- ⏳ Механизм отправки ограничения скорости
- ⏳ Формат данных для карты (изображение или координаты)
- ⏳ Позиционирование строки состояния маршрута
- ⏳ Механизм отображения на проекции

---

**Дата:** 2024  
**Версия:** 1.0

