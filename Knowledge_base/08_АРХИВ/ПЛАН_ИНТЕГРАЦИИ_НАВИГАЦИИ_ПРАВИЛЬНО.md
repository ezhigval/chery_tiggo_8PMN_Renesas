# План правильной интеграции Яндекс Навигатора

## 🎯 Правильный подход (как TurboDog)

Вместо простого запуска Activity на втором дисплее, нужно:

1. **Мониторить Яндекс Навигатор** - получать данные о навигации
2. **Извлекать данные** - маршрут, повороты, карта, расстояние
3. **Отрисовывать упрощенную карту** - создавать упрощенную версию для приборной панели
4. **Отправлять на приборную панель** - через систему проекции

---

## 🏗️ Архитектура Bridge Service

```
Яндекс Навигатор
    ↓
Bridge Service (мониторинг)
    ├── Accessibility Service (получение данных)
    ├── Broadcast Receiver (события навигации)
    └── MediaProjection API (скриншот карты)
    ↓
Извлечение данных:
    ├── Текущая позиция
    ├── Маршрут
    ├── Ближайший поворот
    ├── Расстояние до поворота
    ├── Оставшееся расстояние
    └── Карта (изображение)
    ↓
Отрисовка упрощенной карты:
    ├── Canvas/Bitmap
    ├── Упрощенная карта (только маршрут)
    ├── Стрелка направления
    ├── Индикатор поворота
    └── Текст (расстояние, время)
    ↓
Отправка в систему:
    ├── Broadcast Intent ("turbodog.navigation.system.message")
    ├── Изображение карты
    └── Данные о навигации
    ↓
Система автомобиля
    ├── Отображает на приборной панели
    └── Отображает на HUD
```

---

## 📡 Способы получения данных из Яндекс Навигатора

### 1. Accessibility Service (Рекомендуется)

**Преимущества:**
- ✅ Полный доступ к UI элементам
- ✅ Можно читать текст, координаты
- ✅ Работает в фоне
- ✅ Не требует root

**Как работает:**
```java
public class NavigationAccessibilityService extends AccessibilityService {
    @Override
    public void onAccessibilityEvent(AccessibilityEvent event) {
        // Получаем данные из UI Яндекс Навигатора
        if (event.getPackageName().equals("ru.yandex.yandexnavi")) {
            // Извлекаем данные о навигации
            extractNavigationData(event);
        }
    }
    
    private void extractNavigationData(AccessibilityEvent event) {
        // Получаем текст (расстояние, время, направление)
        String text = event.getText().toString();
        
        // Получаем координаты элементов
        // Анализируем структуру UI
    }
}
```

**Что можно получить:**
- Текст с расстоянием до поворота
- Текст с оставшимся временем
- Тип поворота (иконка)
- Текущая скорость
- Название улицы

**Ограничения:**
- ⚠️ Зависит от структуры UI Яндекс Навигатора
- ⚠️ Может сломаться при обновлении приложения

---

### 2. MediaProjection API (Скриншот карты)

**Преимущества:**
- ✅ Получаем реальное изображение карты
- ✅ Не зависит от структуры UI
- ✅ Можно обработать изображение

**Как работает:**
```java
public class MapCaptureService {
    private MediaProjection mediaProjection;
    
    public void captureMap() {
        // Запрашиваем разрешение на скриншот
        MediaProjectionManager manager = getSystemService(MediaProjectionManager.class);
        Intent intent = manager.createScreenCaptureIntent();
        startActivityForResult(intent, REQUEST_CODE);
    }
    
    @Override
    protected void onActivityResult(int requestCode, int resultCode, Intent data) {
        mediaProjection = manager.getMediaProjection(resultCode, data);
        
        // Создаем ImageReader для захвата экрана
        ImageReader imageReader = ImageReader.newInstance(
            width, height, PixelFormat.RGBA_8888, 1
        );
        
        // Начинаем захват
        VirtualDisplay virtualDisplay = mediaProjection.createVirtualDisplay(
            "MapCapture",
            width, height, dpi,
            DisplayManager.VIRTUAL_DISPLAY_FLAG_AUTO_MIRROR,
            imageReader.getSurface(),
            null, null
        );
        
        // Получаем изображение
        Image image = imageReader.acquireLatestImage();
        Bitmap bitmap = imageToBitmap(image);
        
        // Обрабатываем изображение
        processMapImage(bitmap);
    }
}
```

**Что можно получить:**
- Полное изображение карты
- Можно распознать маршрут
- Можно извлечь данные через OCR

**Ограничения:**
- ⚠️ Требует разрешение пользователя
- ⚠️ Ресурсоемко
- ⚠️ Нужна обработка изображения

---

### 3. Broadcast Receiver (События навигации)

**Преимущества:**
- ✅ Если Яндекс Навигатор отправляет Broadcast
- ✅ Простота реализации

**Как работает:**
```java
public class NavigationReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        // Слушаем события от Яндекс Навигатора
        String action = intent.getAction();
        
        // Возможные события:
        // - ru.yandex.yandexnavi.NAVIGATION_STARTED
        // - ru.yandex.yandexnavi.NAVIGATION_UPDATED
        // - ru.yandex.yandexnavi.TURN_INSTRUCTION
        
        // Получаем данные
        Bundle extras = intent.getExtras();
        if (extras != null) {
            int turnType = extras.getInt("turn_type");
            int distance = extras.getInt("distance");
            // ...
        }
    }
}
```

**Ограничения:**
- ⚠️ Яндекс Навигатор может не отправлять Broadcast
- ⚠️ Нужно проверить документацию/исследовать приложение

---

### 4. Анализ логов и системных свойств

**Преимущества:**
- ✅ Может содержать данные о навигации

**Ограничения:**
- ⚠️ Не надежно
- ⚠️ Может не работать

---

## 🎨 Отрисовка упрощенной карты

### Что нужно отрисовать:

1. **Упрощенная карта:**
   - Только маршрут (линия)
   - Текущая позиция (точка)
   - Ближайший поворот (иконка)

2. **Информация:**
   - Стрелка направления
   - Расстояние до поворота
   - Тип поворота (иконка)
   - Оставшееся расстояние
   - Оставшееся время

3. **Стиль:**
   - Минималистичный
   - Читаемый на приборной панели
   - Контрастный

### Реализация:

```java
public class SimplifiedMapRenderer {
    public Bitmap renderMap(NavigationData data) {
        // Создаем Bitmap
        Bitmap bitmap = Bitmap.createBitmap(
            WIDTH, HEIGHT, Bitmap.Config.ARGB_8888
        );
        Canvas canvas = new Canvas(bitmap);
        
        // Фон
        canvas.drawColor(Color.BLACK);
        
        // Рисуем маршрут
        Path route = data.getRoute();
        Paint routePaint = new Paint();
        routePaint.setColor(Color.WHITE);
        routePaint.setStrokeWidth(3);
        canvas.drawPath(route, routePaint);
        
        // Рисуем текущую позицию
        Point currentPos = data.getCurrentPosition();
        Paint posPaint = new Paint();
        posPaint.setColor(Color.BLUE);
        canvas.drawCircle(currentPos.x, currentPos.y, 10, posPaint);
        
        // Рисуем поворот
        TurnInfo turn = data.getNextTurn();
        if (turn != null) {
            // Иконка поворота
            Bitmap turnIcon = getTurnIcon(turn.getType());
            canvas.drawBitmap(turnIcon, turn.getX(), turn.getY(), null);
            
            // Расстояние до поворота
            String distance = formatDistance(turn.getDistance());
            Paint textPaint = new Paint();
            textPaint.setColor(Color.WHITE);
            textPaint.setTextSize(24);
            canvas.drawText(distance, turn.getX(), turn.getY() + 50, textPaint);
        }
        
        // Стрелка направления
        drawDirectionArrow(canvas, data.getBearing());
        
        return bitmap;
    }
}
```

---

## 📤 Отправка данных в систему

### Формат данных (как TurboDog):

```java
public class NavigationDataSender {
    public void sendNavigationData(NavigationData data) {
        Intent intent = new Intent("turbodog.navigation.system.message");
        
        // Основные данные
        intent.putExtra("CODE", 200);
        intent.putExtra("DATA", 0);
        
        // Поворот
        intent.putExtra("TURN_TYPE", data.getTurnType());
        intent.putExtra("TURN_DIST", data.getTurnDistance());
        intent.putExtra("TURN_TIME", data.getTurnTime());
        
        // Маршрут
        intent.putExtra("REMAINING_DIST", data.getRemainingDistance());
        intent.putExtra("REMAINING_TIME", data.getRemainingTime());
        intent.putExtra("ARRIVE_TIME", data.getArriveTime());
        
        // Дороги
        intent.putExtra("CURRENT_ROAD", data.getCurrentRoad());
        intent.putExtra("NEXT_ROAD", data.getNextRoad());
        
        // Изображение карты (если поддерживается)
        if (data.getMapImage() != null) {
            intent.putExtra("MAP_IMAGE", bitmapToByteArray(data.getMapImage()));
        }
        
        sendBroadcast(intent);
    }
}
```

---

## 🔍 Исследование Яндекс Навигатора

### Что нужно сделать:

1. **Декомпилировать Яндекс Навигатор:**
   ```powershell
   .\jadx\bin\jadx.bat -d development\sources\YandexNavi "CHERY Tiggo 8 Pro Max New _ programs\YandexNavi.apk"
   ```
   - Изучить структуру
   - Найти Broadcast Intent
   - Найти Accessibility Events
   - Найти API для получения данных
   - Найти классы работы с навигацией

2. **Изучить UI:**
   - Какие элементы отображают данные
   - Как получить доступ к данным
   - Структура Activity
   - Layout файлы с навигационными данными

3. **Протестировать на реальном устройстве:**
   ```bash
   # Логирование событий
   adb logcat | grep -i yandex
   adb logcat | grep -i navi
   
   # Broadcast Intent
   adb shell dumpsys activity broadcasts | grep yandex
   
   # Accessibility Events
   adb shell dumpsys accessibility | grep yandex
   ```
   - Какие события отправляет
   - Какие Broadcast доступны
   - Можно ли получить данные через Accessibility
   - Какие данные доступны в UI

4. **Исследовать MediaProjection:**
   - Можно ли делать скриншот карты
   - Как обработать изображение
   - Извлечение данных из изображения

---

## 📋 План реализации

### Этап 1: Исследование

1. ✅ Декомпилировать Яндекс Навигатор
2. ⏳ Изучить структуру приложения
3. ⏳ Найти способы получения данных
4. ⏳ Протестировать Accessibility Service
5. ⏳ Протестировать MediaProjection API

### Этап 2: Разработка Bridge Service

1. ⏳ Создать Accessibility Service
2. ⏳ Реализовать извлечение данных
3. ⏳ Создать рендерер упрощенной карты
4. ⏳ Реализовать отправку данных

### Этап 3: Интеграция

1. ⏳ Интеграция с системой автомобиля
2. ⏳ Тестирование на реальном устройстве
3. ⏳ Оптимизация производительности

---

## 🎯 Преимущества правильного подхода

1. ✅ **Красиво:** Упрощенная карта выглядит профессионально
2. ✅ **Надежно:** Не зависит от UI Яндекс Навигатора
3. ✅ **Гибко:** Можно настроить отображение
4. ✅ **Совместимо:** Использует тот же формат что TurboDog

---

**Дата:** 2024  
**Версия:** 1.0

