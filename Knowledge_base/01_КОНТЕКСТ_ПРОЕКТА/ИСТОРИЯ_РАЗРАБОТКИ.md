# История разработки и принятых решений

## Хронология попыток решения задачи

### Этап 1: Reflection подход
**Цель**: Получить прямой доступ к `Guidance` объекту из Yandex Navigator через Reflection.

**Попытки**:
1. Получение `GenericGuidanceComponent.INSTANCE` ✅
2. Вызов `getGenericGuidance()` как статического метода ❌
3. Вызов `getGenericGuidance()` как метода экземпляра ✅ (но возвращает `GenericGuidance`, не `Guidance`)
4. Анализ полей `GenericGuidanceImpl`:
   - `handler` - null ❌
   - `consumer` - null ❌
   - Другие поля не содержат `Guidance`
5. Создание dynamic proxy для `GenericGuidanceConsumer` и `GenericGuidanceHandler`:
   - Proxy создан ✅
   - Но callback'и не вызываются (handler/consumer остаются null) ❌

**Вывод**: Reflection не работает, так как внутренние компоненты Yandex Navigator не инициализированы или недоступны извне.

### Этап 2: AccessibilityService подход
**Цель**: Извлечение данных навигации через UI элементы.

**Реализация**:
- Создан `NavigationAccessibilityService`
- Рекурсивный обход accessibility tree
- Поиск текстовых узлов с данными навигации
- Парсинг расстояний, времени, типов маневров

**Проблема**: В автомобильной системе нет стандартных настроек Android, поэтому AccessibilityService нельзя активировать.

**Вывод**: Метод не применим в данной системе.

### Этап 3: Альтернативные методы перехвата
**Цель**: Найти надежный способ перехвата данных без Reflection и AccessibilityService.

**Создан `NavigationDataInterceptor`** с поддержкой:
1. **BroadcastReceiver**:
   - Перехват системных broadcast'ов
   - Обработка `turbodog.navigation.system.message`
   - Обработка broadcast'ов от Yandex Navigator
   - Парсинг данных из Intent extras

2. **ContentProvider** (планируется):
   - Чтение данных через ContentProvider API

3. **SharedPreferences** (планируется):
   - Мониторинг SharedPreferences навигатора

4. **File Monitoring** (планируется):
   - Периодическая проверка файлов навигатора

**Текущий статус**: BroadcastReceiver реализован, остальные методы в разработке.

### Этап 4: Reverse Engineering
**Цель**: Понять, как работает штатная навигация TurboDog и как она передает данные.

**Действия**:
1. ✅ Подключение устройства через ADB
2. ✅ Извлечение APK:
   - `com.astrob.turbodog` (TurboDog)
   - Системные приложения DesaySV
   - Медиа, телефон, Bluetooth
   - Руководство пользователя
3. 🔄 Декомпиляция APK с помощью JADX

**Следующий шаг**: Анализ декомпилированного кода для понимания:
- Какие broadcast'ы отправляет TurboDog
- Формат данных
- Интеграция с системой

## Ключевые технические детали

### Проблема с типами данных
**Ошибка**: `getRemainingDistance()` и `getSpeedLimit()` возвращают `int`, но код пытался сравнить с `null`.

**Решение**: Использовать wrapper типы (`Double`, `Integer`) для проверки на `null`, затем работать с примитивами.

### Проблема с ClassLoader
**Ошибка**: `ClassNotFoundException` при загрузке классов Yandex Navigator через стандартный ClassLoader.

**Решение**: Создать `Context` для пакета навигатора и использовать его `ClassLoader`:
```java
Context naviContext = context.createPackageContext(navPackage,
    Context.CONTEXT_INCLUDE_CODE | Context.CONTEXT_IGNORE_SECURITY);
ClassLoader naviClassLoader = naviContext.getClassLoader();
```

### Проблема с отслеживанием состояния навигатора
**Проблема**: Статус навигатора не обновлялся в реальном времени.

**Решение**: 
- Реализован периодический мониторинг (каждые 5 секунд)
- Использование `UsageStatsManager` для более надежного определения запущенного приложения
- Проверка `lastTimeUsed` с порогом 3 секунды

### Формат данных TurboDog
Обнаружены константы в `Constants.java`:
- `EXTRA_TURN_DIST` - расстояние до маневра
- `EXTRA_TURN_TYPE` - тип маневра
- `EXTRA_REMAINING_DIST` - оставшееся расстояние
- `EXTRA_REMAINING_TIME` - оставшееся время
- `EXTRA_ARRIVE_TIME` - время прибытия
- `EXTRA_CURRENT_ROAD` / `EXTRA_NEXT_ROAD` - названия дорог
- `EXTRA_CURRENT_LAT` / `EXTRA_CURRENT_LON` - координаты
- `EXTRA_SPEED_LIMIT` - ограничение скорости

## Уроки и выводы

1. **Reflection ограничен**: Внутренние компоненты приложений могут быть недоступны или не инициализированы.

2. **AccessibilityService не универсален**: Требует системных настроек, которых может не быть в кастомных системах.

3. **Reverse Engineering необходим**: Для понимания закрытых систем нужно анализировать их код.

4. **Broadcast'ы - надежный метод**: Системные broadcast'ы - стандартный способ коммуникации в Android.

5. **Мониторинг состояния важен**: Нужно отслеживать изменения состояния навигатора для своевременного перехвата данных.

## Планы на будущее

1. Завершить анализ декомпилированного кода TurboDog
2. Реализовать все методы перехвата в `NavigationDataInterceptor`
3. Протестировать на реальном устройстве
4. Оптимизировать производительность
5. Добавить обработку ошибок и восстановление после сбоев

