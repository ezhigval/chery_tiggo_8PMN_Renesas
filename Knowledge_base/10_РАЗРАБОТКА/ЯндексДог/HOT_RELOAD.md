# 🔥 Hot Reload - Быстрая переустановка приложения

## Использование

### Обычный Hot Reload (полная пересборка)

```bash
./hot_reload.sh
```

- Полная пересборка проекта (Java + нативный код)
- Переустановка APK на устройство
- Автоматический перезапуск приложения

### Быстрый Hot Reload (только Java изменения)

```bash
./hot_reload_fast.sh
```

- Использует кэш Gradle для ускорения
- Параллельная сборка
- Подходит для изменений только в Java коде

## Интеграция с VS Code

В VS Code можно использовать задачу "Hot Reload":

1. Нажмите `Cmd+Shift+B` (Mac) или `Ctrl+Shift+B` (Windows/Linux)
2. Выберите "Hot Reload"
3. Или используйте Command Palette (`Cmd+Shift+P`) → "Tasks: Run Task" → "Hot Reload"

## Настройки для ускорения

В `gradle.properties` включены:

- Параллельная сборка (`org.gradle.parallel=true`)
- Кэширование (`org.gradle.caching=true`)
- Daemon процесс (`org.gradle.daemon=true`)
- Увеличенная память JVM (4GB)

## Устройство

По умолчанию используется устройство `00002260`. Для изменения отредактируйте переменную `DEVICE_ID` в скриптах.

## Альтернативные команды

```bash
# Только сборка
./gradlew assembleDebug

# Только установка
adb -s 00002260 install -r app/build/outputs/apk/debug/app-debug.apk

# Только перезапуск
adb -s 00002260 shell am force-stop com.tiggo.navigator
adb -s 00002260 shell am start -n com.tiggo.navigator/.MainActivity
```
