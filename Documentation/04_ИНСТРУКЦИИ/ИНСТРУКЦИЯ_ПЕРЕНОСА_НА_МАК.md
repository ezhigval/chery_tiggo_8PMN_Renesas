# Инструкция по переносу проекта на Mac

## Что уже готово для переноса

### ✅ Документация создана:
1. **КОНТЕКСТ_ПРОЕКТА.md** - полный контекст проекта, структура, текущий статус
2. **ИСТОРИЯ_РАЗРАБОТКИ.md** - хронология попыток и решений
3. **БЫСТРЫЙ_СТАРТ.md** - краткая инструкция для быстрого восстановления контекста
4. **ИДЕИ_ПО_ДИЗАЙНУ.md** - напоминание об идеях по внешнему виду

### ✅ Файлы проекта:
- Весь код Android приложения в `development/projects/TiggoBridgeService/`
- Извлеченные APK в `development/extracted_from_device/*.apk`
- **Декомпилированный код** (63886 Java файлов) в `development/extracted_from_device/decompiled/`

### ✅ Скрипты:
- `extract_apks_from_device.sh` - для Mac/Linux
- `decompile_apks.sh` - для Mac/Linux
- `extract_images_from_apk.sh` - для Mac/Linux

## Шаги для переноса

### 1. Синхронизация файлов
Если используете Git:
```bash
git add .
git commit -m "Добавлена документация и декомпилированные APK"
git push
```

Если используете облачное хранилище (Dropbox, iCloud, Google Drive):
- Просто скопируйте папку `development/` целиком

### 2. На Mac - клонирование/копирование
```bash
# Если Git
git clone <repository-url>
cd Tiggo

# Или просто скопируйте папку development/
```

### 3. Установка инструментов на Mac

#### ADB:
```bash
brew install android-platform-tools
```

#### JADX:
```bash
# Вариант 1: через Homebrew (если есть cask)
brew install --cask jadx

# Вариант 2: вручную
# Скачать с https://github.com/skylot/jadx/releases
# Распаковать и добавить в PATH или использовать полный путь
```

### 4. Проверка работоспособности

#### Проверить ADB:
```bash
adb version
adb devices
```

#### Проверить JADX:
```bash
jadx --version
# или
jadx-gui
```

#### Проверить декомпилированный код:
```bash
ls -la development/extracted_from_device/decompiled/
# Должно быть много папок с декомпилированным кодом
```

### 5. Восстановление контекста в Cursor

**🎯 ПРОСТОЙ СПОСОБ - используй кодовое слово:**

В Cursor на Mac просто напиши AI:
```
прочитай файл
```
или
```
восстанови контекст
```

AI автоматически прочитает все необходимые файлы и восстановит полный контекст проекта!

**📋 Ручной способ:**
1. Откройте проект в Cursor на Mac
2. Прочитайте `development/БЫСТРЫЙ_СТАРТ.md` для быстрого понимания
3. При необходимости углубитесь в:
   - `development/КОНТЕКСТ_ПРОЕКТА.md` - полный контекст
   - `development/ИСТОРИЯ_РАЗРАБОТКИ.md` - история решений

### 6. Продолжение работы

#### Следующие задачи:
1. Анализ декомпилированного кода TurboDog
2. Поиск broadcast'ов, ContentProvider'ов, файлов
3. Реализация перехвата данных на основе анализа

#### Полезные команды для анализа:
```bash
# Поиск broadcast'ов
grep -r "sendBroadcast" development/extracted_from_device/decompiled/com.astrob.turbodog/

# Поиск ContentProvider
grep -r "extends ContentProvider" development/extracted_from_device/decompiled/

# Поиск SharedPreferences
grep -r "getSharedPreferences" development/extracted_from_device/decompiled/com.astrob.turbodog/

# Поиск системного сообщения
grep -r "turbodog.navigation.system.message" development/extracted_from_device/decompiled/
```

## Важные заметки

### Облачная синхронизация Cursor
Cursor **не имеет встроенной облачной синхронизации** для истории диалогов. Однако:
- ✅ Весь код и файлы проекта синхронизируются через Git/облако
- ✅ Вся документация сохранена в файлах
- ✅ Контекст можно восстановить через документы

### Что НЕ синхронизируется автоматически:
- История диалогов с AI (но она сохранена в документации)
- Настройки Cursor (но они не критичны)
- Локальные кэши

### Рекомендации:
1. Используйте Git для версионирования кода
2. Документируйте важные решения (уже сделано)
3. При необходимости можно скопировать `.cursor/` папку (если есть), но это не обязательно

## Структура для проверки на Mac

После переноса должны быть:
```
development/
├── README_AI.md                   ✅ (кодовое слово для AI)
├── ВОССТАНОВЛЕНИЕ_КОНТЕКСТА_AI.md ✅ (инструкция для AI)
├── КОНТЕКСТ_ПРОЕКТА.md            ✅
├── ИСТОРИЯ_РАЗРАБОТКИ.md          ✅
├── БЫСТРЫЙ_СТАРТ.md               ✅
├── ИДЕИ_ПО_ДИЗАЙНУ.md             ✅
├── ИНСТРУКЦИЯ_ПЕРЕНОСА_НА_МАК.md  ✅ (этот файл)
├── projects/
│   └── TiggoBridgeService/        ✅ (весь код)
├── extracted_from_device/
│   ├── *.apk                      ✅ (8 APK файлов)
│   └── decompiled/                ✅ (63886 Java файлов)
└── scripts/
    ├── *.ps1                      (для Windows)
    └── *.sh                       ✅ (для Mac)
```

**💡 Важно**: Файлы `README_AI.md` и `ВОССТАНОВЛЕНИЕ_КОНТЕКСТА_AI.md` содержат инструкции для AI - они должны быть перенесены обязательно!

## Если что-то пошло не так

1. **Файлы не синхронизировались**:
   - Проверьте размер папки `decompiled/` (может быть большой)
   - Используйте `.gitignore` для исключения больших файлов, если нужно
   - Или используйте Git LFS для больших файлов

2. **Скрипты не работают**:
   - Проверьте права на выполнение: `chmod +x development/scripts/*.sh`
   - Проверьте пути (Windows vs Mac)

3. **Контекст потерян**:
   - Начните с `БЫСТРЫЙ_СТАРТ.md`
   - Затем `КОНТЕКСТ_ПРОЕКТА.md`
   - Изучите `ИСТОРИЯ_РАЗРАБОТКИ.md` для понимания решений

## Контакты и помощь

### Восстановление контекста в Cursor на Mac:

**🎯 Самый простой способ:**
Просто напишите AI:
```
прочитай файл
```

AI автоматически прочитает `development/ВОССТАНОВЛЕНИЕ_КОНТЕКСТА_AI.md` и все связанные файлы, полностью восстановив контекст проекта!

**📋 Альтернативный способ:**
1. Откройте любой из документов в `development/`
2. Задайте вопрос AI в Cursor, ссылаясь на конкретный документ
3. AI сможет восстановить контекст из документации

---

**Статус**: Готово к переносу ✅
**Дата**: $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")

