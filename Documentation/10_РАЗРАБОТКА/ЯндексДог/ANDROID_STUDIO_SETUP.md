# 🚀 Настройка и запуск в Android Studio

## 📋 Быстрый старт

### 1. Открытие проекта

1. **Запустите Android Studio**
2. **File → Open...** (или `Cmd+O` / `Ctrl+O`)
3. Выберите папку: `/Users/valentinezov/Projects/Tiggo/development/ЯндексДог`
4. Нажмите **OK**

### 2. Синхронизация Gradle

**Вариант 1: Автоматическая синхронизация**

- Android Studio автоматически предложит синхронизировать проект
- Нажмите **"Sync Now"** в верхней панели

**Вариант 2: Вручную**

- **File → Sync Project with Gradle Files** (`Cmd+Shift+O` / `Ctrl+Shift+O`)
- Или нажмите на **🔄 Sync** иконку в панели инструментов (обычно справа вверху)

**Вариант 3: Через меню**

- **File → Invalidate Caches / Restart**
- Выберите **"Invalidate and Restart"** (если синхронизация не работает)

### 3. Ожидание синхронизации

Во время синхронизации вы увидите:

- Индикатор прогресса внизу: `Gradle: Executing tasks...`
- Загрузку зависимостей (Yandex MapKit SDK и др.)
- Проверку структуры проекта

⏱️ **Время синхронизации:** обычно 1-3 минуты при первом запуске

## ✅ Проверка успешной синхронизации

После синхронизации проверьте:

1. **Панель "Build"** (внизу) - не должно быть ошибок
2. **Структура проекта** - видна папка `app` с исходниками
3. **Зависимости** - Project → External Libraries должны быть видны:
   - `androidx.appcompat`
   - `com.yandex.android:mapkit`
   - и другие

## 🔧 Если возникли проблемы

### Проблема: Gradle не синхронизируется

**Решение 1: Проверьте настройки Gradle**

```
File → Settings (Preferences) → Build, Execution, Deployment → Gradle
- Use Gradle from: 'gradle-wrapper.properties' file
- Gradle JDK: (выберите JDK 11 или выше)
```

**Решение 2: Очистите кеш**

```bash
cd /Users/valentinezov/Projects/Tiggo/development/ЯндексДог
./gradlew clean
```

**Решение 3: Пересоздайте Gradle Wrapper**

```
File → Settings → Build, Execution, Deployment → Gradle
- Gradle version: 7.5 (или выше)
```

### Проблема: Не найдены зависимости

**Решение:**

1. Проверьте интернет-соединение
2. Убедитесь, что в `build.gradle` правильно указаны repositories:
   ```gradle
   repositories {
       google()
       mavenCentral()
   }
   ```

### Проблема: Ошибки компиляции декомпилированных файлов

**Решение: Уже исправлено!**

- В `app/build.gradle` добавлены `exclude` для `turbodog_analysis/**`
- Если ошибки остаются, выполните:
  ```
  File → Invalidate Caches / Restart → Invalidate and Restart
  ```

## 🎯 После успешной синхронизации

### 1. Проверка структуры проекта

В Android Studio вы должны видеть:

```
ЯндексДог/
├── app/
│   ├── manifests/
│   │   └── AndroidManifest.xml
│   ├── java/
│   │   └── com.tiggo.navigator
│   │       ├── MainActivity.java
│   │       ├── PresentationActivity.java
│   │       ├── NavigationService.java
│   │       └── ... (все Java файлы)
│   └── res/
├── build.gradle
├── settings.gradle
└── turbodog_analysis/ (должна быть исключена)
```

### 2. Сборка проекта

**Вариант 1: Через меню**

- **Build → Make Project** (`Cmd+F9` / `Ctrl+F9`)

**Вариант 2: Через терминал**

```bash
cd /Users/valentinezov/Projects/Tiggo/development/ЯндексДог
./gradlew assembleDebug
```

### 3. Запуск на устройстве/эмуляторе

1. Подключите Android устройство или запустите эмулятор
2. **Run → Run 'app'** (`Shift+F10` / `Ctrl+R`)
3. Или нажмите кнопку ▶️ в панели инструментов

## 📱 Подготовка устройства/эмулятора

### Реальное устройство:

1. Включите режим разработчика:
   - Настройки → О телефоне → нажмите 7 раз на "Номер сборки"
2. Включите отладку по USB:
   - Настройки → Для разработчиков → Отладка по USB
3. Подключите устройство по USB
4. Разрешите отладку на устройстве

### Эмулятор:

1. **Tools → Device Manager**
2. Нажмите **"Create Device"**
3. Выберите устройство (например, Pixel 6)
4. Выберите системный образ (API 21+)
5. Запустите эмулятор

## 🎓 Полезные сочетания клавиш

| Действие             | macOS          | Windows/Linux   |
| -------------------- | -------------- | --------------- |
| Синхронизация Gradle | `Cmd+Shift+O`  | `Ctrl+Shift+O`  |
| Сборка проекта       | `Cmd+F9`       | `Ctrl+F9`       |
| Запуск приложения    | `Shift+F10`    | `Ctrl+R`        |
| Очистить проект      | `Cmd+Shift+F9` | `Ctrl+Shift+F9` |
| Открыть настройки    | `Cmd+,`        | `Ctrl+Alt+S`    |

## ✅ Чеклист запуска

- [ ] Android Studio установлен
- [ ] Проект открыт
- [ ] Gradle синхронизирован (без ошибок)
- [ ] Зависимости загружены
- [ ] Проект собирается (`Build → Make Project`)
- [ ] Устройство/эмулятор подключен
- [ ] Приложение запускается (`Run → Run 'app'`)

## 🆘 Быстрая помощь

Если что-то не работает:

1. **Перезапустите синхронизацию:**

   ```
   File → Sync Project with Gradle Files
   ```

2. **Очистите кеш:**

   ```
   File → Invalidate Caches / Restart → Invalidate and Restart
   ```

3. **Проверьте логи:**

   - View → Tool Windows → Build (для ошибок сборки)
   - View → Tool Windows → Logcat (для ошибок выполнения)

4. **Проверьте Gradle консоль:**
   - View → Tool Windows → Build → вкладка "Gradle Console"

## 📞 Дополнительная информация

- См. `BUILD_README.md` - подробная инструкция по сборке
- См. `GRADLE_SETUP.md` - настройка Gradle
- См. `FIX_COMPILATION.md` - исправление ошибок компиляции
