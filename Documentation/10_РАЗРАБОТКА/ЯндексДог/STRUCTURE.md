# Структура проекта ЯндексДог

## 📁 Полная структура проекта

```
development/ЯндексДог/
│
├── 📄 README.md                        # Основная документация проекта
├── 📄 IMPLEMENTATION_STATUS.md         # Текущий статус реализации
├── 📄 NEXT_STEPS.md                    # Следующие шаги
├── 📄 STRUCTURE.md                     # Этот файл
├── 📄 CMakeLists.txt                   # CMake конфигурация
├── 📄 analyze_turbodog.sh              # Скрипт анализа TurboDog APK
│
├── 📁 native/                          # Нативный код на C (стиль TurboDog)
│   │
│   ├── 📁 core/                        # Основной движок
│   │   ├── tiggo_engine.h              # Заголовок движка (CTiggoEngine)
│   │   └── tiggo_engine.c              # Реализация движка
│   │
│   ├── 📁 render/                      # OpenGL ES рендеринг
│   │   ├── render_gl.h                 # Заголовок рендеринга
│   │   └── render_gl.c                 # Реализация OpenGL ES
│   │
│   ├── 📁 jni/                         # JNI слой
│   │   ├── jni_tiggo_bridge.c          # JNI мост (Java ↔ Native)
│   │   └── jni_utils.h                 # Утилиты JNI
│   │
│   ├── 📁 data/                        # Обработка данных (TODO)
│   │   ├── yandex_bridge.c             # Мост к Yandex данным
│   │   ├── data_converter.c            # Конвертер данных
│   │   └── cache_manager.c             # Кеширование
│   │
│   ├── 📁 async/                       # Асинхронная архитектура (TODO)
│   │   ├── task_queue.c                # Очередь задач
│   │   ├── thread_pool.c               # Пул потоков
│   │   └── async_loader.c              # Асинхронная загрузка
│   │
│   ├── 📁 utils/                       # Утилиты (TODO)
│   │   ├── math_utils.c                # Математические утилиты
│   │   ├── geometry_utils.c            # Геометрические утилиты
│   │   └── log_utils.c                 # Логирование
│   │
│   └── 📁 render/                      # Рендеринг карты (TODO)
│       ├── map_renderer.c              # Рендеринг тайлов карты
│       ├── route_renderer.c            # Рендеринг маршрута
│       └── ui_renderer.c               # Рендеринг UI
│
├── 📁 java/                            # Java код (Android)
│   └── com/tiggo/navigator/
│       ├── TiggoJavaToJni.java         # JNI слой (вызовы нативного кода)
│       ├── TiggoJniToJava.java         # Обратный JNI слой (вызовы Java)
│       ├── TiggoSecondaryRenderThread.java  # Поток рендеринга Display 1
│       ├── YandexMapKitBridge.java     # Мост к Yandex MapKit API
│       ├── MainActivity.java           # Главная Activity (TODO)
│       ├── NavigationService.java      # Сервис навигации (TODO)
│       └── PresentationActivity.java   # Activity для Display 1 (TODO)
│
└── 📁 turbodog_analysis/               # Анализ TurboDog APK
    ├── extracted/                      # Извлеченный APK
    │   └── lib/
    │       └── arm64-v8a/
    │           └── libFunctionLayer.so # Нативная библиотека TurboDog
    │
    ├── decompiled/                     # Декомпилированный Java код
    │   └── sources/
    │       └── com/astrob/
    │           ├── turbodog/           # TurboDog Java код
    │           └── navi/               # Навигационная библиотека
    │
    ├── ANALYSIS_RESULTS.md             # Детальные результаты анализа
    ├── ANALYSIS_SUMMARY.md             # Краткое резюме анализа
    └── analysis_report.txt             # Краткий отчет
```

## 🎯 Ключевые компоненты

### 1. Нативный код (C)

**Основной движок:**
- `tiggo_engine.h/c` - Главный класс движка (`CTiggoEngine`)
- Стиль TurboDog: префиксы функций (`Tiggo_*`), структуры (`C*`), венгерская нотация

**OpenGL ES рендеринг:**
- `render_gl.h/c` - OpenGL ES функции
- `Tiggo_CreateGL()` - создание контекста
- `Tiggo_RenderGL()` - рендеринг Display 0
- `Tiggo_RenderSecondaryWndGL()` - рендеринг Display 1

**JNI слой:**
- `jni_tiggo_bridge.c` - JNI мост между Java и Native
- Все функции имеют префикс `Java_com_tiggo_navigator_TiggoJavaToJni_`

### 2. Java код (Android)

**JNI слой:**
- `TiggoJavaToJni.java` - вызовы нативного кода из Java
- `TiggoJniToJava.java` - обратные вызовы из нативного кода в Java

**Рендеринг:**
- `TiggoSecondaryRenderThread.java` - отдельный поток для Display 1
- Аналог `SecondaryRenderThread` из TurboDog

**Интеграция с Yandex:**
- `YandexMapKitBridge.java` - мост к Yandex MapKit API
- Получает данные от Yandex и передает в нативный код

### 3. Анализ TurboDog

**Результаты анализа:**
- Найдена нативная библиотека `libFunctionLayer.so` (14 MB)
- Декомпилирован Java код TurboDog
- Понят стиль кода и архитектура
- Понят протокол обмена данными (JSON)

## 📊 Статус файлов

### ✅ Готово

- ✅ `native/core/tiggo_engine.h/c` - Основной движок
- ✅ `native/render/render_gl.h/c` - OpenGL ES рендеринг
- ✅ `native/jni/jni_tiggo_bridge.c` - JNI слой
- ✅ `java/com/tiggo/navigator/TiggoJavaToJni.java` - JNI слой
- ✅ `java/com/tiggo/navigator/TiggoJniToJava.java` - Обратный JNI
- ✅ `java/com/tiggo/navigator/TiggoSecondaryRenderThread.java` - Поток рендеринга
- ✅ `java/com/tiggo/navigator/YandexMapKitBridge.java` - Мост к Yandex

### ⏳ В процессе

- ⏳ `native/render/map_renderer.c` - Рендеринг тайлов карты
- ⏳ `native/render/route_renderer.c` - Рендеринг маршрута
- ⏳ `native/render/ui_renderer.c` - Рендеринг UI

### 📝 TODO

- 📝 `java/com/tiggo/navigator/MainActivity.java` - Главная Activity
- 📝 `java/com/tiggo/navigator/NavigationService.java` - Сервис навигации
- 📝 `java/com/tiggo/navigator/PresentationActivity.java` - Activity для Display 1
- 📝 `native/data/yandex_bridge.c` - Мост к Yandex данным
- 📝 `native/async/task_queue.c` - Очередь задач
- 📝 `native/async/thread_pool.c` - Пул потоков

## 🔗 Связи между компонентами

```
┌─────────────────────────────────────────────────────────┐
│  Android Layer (Java)                                    │
│                                                           │
│  ┌─────────────────┐    ┌────────────────────────────┐ │
│  │ MainActivity    │    │ PresentationActivity       │ │
│  │ (Display 0)     │    │ (Display 1)                │ │
│  └────────┬────────┘    └────────────┬───────────────┘ │
│           │                          │                  │
│           │                          │                  │
│  ┌────────▼────────┐    ┌───────────▼────────────────┐ │
│  │ YandexMapKit    │    │ TiggoSecondaryRenderThread │ │
│  │ Bridge          │    │ (отдельный поток)          │ │
│  └────────┬────────┘    └───────────┬────────────────┘ │
│           │                          │                  │
│           │                          │                  │
└───────────┼──────────────────────────┼──────────────────┘
            │                          │
            │ JNI                      │ JNI
            ▼                          ▼
┌─────────────────────────────────────────────────────────┐
│  Native Layer (C)                                        │
│                                                           │
│  ┌─────────────────┐    ┌────────────────────────────┐ │
│  │ TiggoJavaToJni  │    │ TiggoJniToJava             │ │
│  │ (JNI Bridge)    │    │ (Reverse JNI)              │ │
│  └────────┬────────┘    └────────────┬───────────────┘ │
│           │                          │                  │
│           │                          │                  │
│  ┌────────▼────────┐    ┌───────────▼────────────────┐ │
│  │ CTiggoEngine    │◄───┤ TiggoNavigationCallback   │ │
│  │ (Основной движок)│    │ (Broadcast)                │ │
│  └────────┬────────┘    └────────────────────────────┘ │
│           │                                              │
│           │                                              │
│  ┌────────▼────────┐                                    │
│  │ RenderGL        │                                    │
│  │ (OpenGL ES)     │                                    │
│  └─────────────────┘                                    │
└─────────────────────────────────────────────────────────┘
```

## 📝 Примечания

1. **Стиль кода:**
   - Все нативные функции имеют префикс `Tiggo_`
   - Все структуры имеют префикс `C`
   - Используется венгерская нотация (`m_`, `n_`, `f_`, `p_`, `b_`)

2. **JNI соглашения:**
   - Имена функций: `Java_com_tiggo_navigator_TiggoJavaToJni_<MethodName>`
   - Обратные вызовы: `TiggoJniToJava.jniCall<MethodName>()`

3. **Двухдисплейный режим:**
   - Display 0: Полноценная карта (рендерится в основном потоке)
   - Display 1: Упрощенная карта (рендерится в отдельном потоке)

4. **Протокол обмена данными:**
   - JSON формат (как в TurboDog)
   - Broadcast сообщения в формате TurboDog
   - Совместимость с `com.desaysv.mapservice`

