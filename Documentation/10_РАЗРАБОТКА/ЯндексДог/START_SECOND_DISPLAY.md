# 📱 Запуск второго дисплея через скрипт

## 📋 Описание

Скрипт `start_second_display.sh` запускается **на вашем Mac** и использует `adb` для:
- Поиска подключенного Android устройства
- Создания второго дисплея (Display 1) на устройстве
- Захвата скриншотов второго дисплея
- Отображения скриншотов на Mac в реальном времени

## 🚀 Запуск скрипта

### Быстрый запуск

Когда нужно запустить скрипт, просто выполните:

```bash
cd /Users/valentinezov/Projects/Tiggo/development/ЯндексДог
./start_second_display.sh
```

### Что делает скрипт

1. ✅ **Ищет устройство** (до 60 секунд ожидания)
2. ✅ **Проверяет второй дисплей** на устройстве
3. ✅ **Создает второй дисплей** если его нет (1920x720/320)
4. ✅ **Захватывает скриншоты** каждую секунду
5. ✅ **Отображает скриншоты** на Mac через Quick Look

## ⚙️ Требования

### Перед запуском:

1. **Android устройство подключено:**
   ```bash
   adb devices
   ```
   Должно показать устройство в статусе "device"

2. **Отладка по USB включена:**
   - Настройки → Для разработчиков → Отладка по USB ✅

3. **ADB установлен:**
   - Проверьте: `which adb`
   - Должен вернуть путь к adb

## 📱 Использование

### Запуск скрипта

```bash
./start_second_display.sh
```

### Что происходит

1. Скрипт автоматически найдет подключенное устройство
2. Проверит наличие второго дисплея
3. Создаст второй дисплей если нужно
4. Начнет захватывать скриншоты
5. Откроет окно Quick Look на Mac с изображением
6. Будет обновлять изображение каждую секунду

### Остановка скрипта

Нажмите **Ctrl+C** для остановки

## 🔍 Проверка

### Проверка подключения устройства

```bash
adb devices
```

Ожидаемый вывод:
```
List of devices attached
ABC123XYZ    device
```

### Проверка дисплеев на устройстве

```bash
adb shell dumpsys display | grep "mDisplayId"
```

Должно показать минимум 2 дисплея:
```
mDisplayId=0
mDisplayId=1
```

## 🛠️ Отладка

### Если устройство не найдено

1. Проверьте USB подключение
2. Включите отладку по USB на устройстве
3. Разрешите отладку (если появится запрос на устройстве)

### Если второй дисплей не создается

Скрипт автоматически попытается создать второй дисплей:
```bash
adb shell settings put global overlay_display_devices "1920x720/320"
```

Если не помогает, можно создать вручную:
1. Настройки → Для разработчиков
2. Включите "Simulate secondary displays"
3. Выберите разрешение

### Просмотр логов

```bash
# В отдельном терминале
adb logcat | grep -i "display\|presentation"
```

## 📝 Важные замечания

1. **Скрипт запускается на Mac**, не на Android устройстве
2. Скрипт **использует adb** для взаимодействия с устройством
3. Скриншоты отображаются через **macOS Quick Look**
4. Для остановки нажмите **Ctrl+C**

## 🎯 Когда использовать

Скрипт используется когда нужно:
- ✅ Протестировать второй дисплей (Display 1)
- ✅ Просмотреть упрощенную карту на приборной панели
- ✅ Проверить работу PresentationActivity
- ✅ Отладить рендеринг на втором дисплее

## 📚 Связанные файлы

- `start_second_display.sh` - основной скрипт (4127 байт)
- `PresentationActivity.java` - Activity для второго дисплея
- `TiggoSecondaryRenderThread.java` - поток рендеринга для Display 1

## ✅ Готово к использованию

Скрипт готов! Когда нужно будет запустить второй дисплей, просто попросите:
> "Запусти start_second_display.sh"

И я выполню команду:
```bash
cd /Users/valentinezov/Projects/Tiggo/development/ЯндексДог && ./start_second_display.sh
```

