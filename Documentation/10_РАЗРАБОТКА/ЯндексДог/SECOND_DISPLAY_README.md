# 📱 Запуск скрипта второго дисплея через ADB

## 📋 Описание

Скрипт `start_second_display.sh` используется для запуска второго дисплея (Display 1 / Presentation Window) на Android устройстве.

## 🚀 Быстрый запуск

### Вариант 1: Автоматический скрипт (Рекомендуется)

Используйте готовый скрипт-обертку:

```bash
cd /Users/valentinezov/Projects/Tiggo/development/ЯндексДог
chmod +x run_second_display.sh
./run_second_display.sh
```

Скрипт автоматически:
- ✅ Проверит подключение устройства
- ✅ Скопирует `start_second_display.sh` на устройство
- ✅ Установит права на выполнение
- ✅ Запустит скрипт

### Вариант 2: Вручную через ADB

```bash
# 1. Проверьте подключение устройства
adb devices

# 2. Скопируйте скрипт на устройство
adb push start_second_display.sh /data/local/tmp/

# 3. Установите права на выполнение
adb shell chmod 755 /data/local/tmp/start_second_display.sh

# 4. Запустите скрипт
adb shell /data/local/tmp/start_second_display.sh
```

### Вариант 3: Одной командой

```bash
adb push start_second_display.sh /data/local/tmp/ && \
adb shell chmod 755 /data/local/tmp/start_second_display.sh && \
adb shell /data/local/tmp/start_second_display.sh
```

## 📱 Требования

### Перед запуском:

1. **Устройство подключено:**
   ```bash
   adb devices
   ```
   Должно показать устройство в статусе "device"

2. **Отладка по USB включена:**
   - Настройки → Для разработчиков → Отладка по USB ✅

3. **Права на выполнение скриптов:**
   - Устройство должно быть с root доступом
   - Или скрипт должен быть адаптирован для работы без root

## 🔍 Проверка подключения

```bash
# Проверить подключенные устройства
adb devices

# Ожидаемый вывод:
# List of devices attached
# ABC123XYZ    device
```

Если устройство не найдено:
1. Проверьте USB подключение
2. Включите отладку по USB
3. Разрешите отладку на устройстве (если появится запрос)

## 🛠️ Отладка

### Просмотр логов

Во время выполнения скрипта можно следить за логами:

```bash
# В отдельном терминале
adb logcat | grep -i "presentation\|display\|second"
```

### Проверка выполнения

```bash
# Проверить, запущен ли процесс
adb shell ps | grep -i "presentation\|navigator"
```

### Просмотр ошибок

```bash
# Посмотреть последние ошибки
adb logcat -d | grep -i "error\|exception"
```

## 📝 Использование в проекте

### Запуск после установки APK

```bash
# 1. Установите APK
adb install app/build/outputs/apk/debug/app-debug.apk

# 2. Запустите скрипт второго дисплея
./run_second_display.sh
```

### Интеграция в workflow

Можно добавить запуск скрипта в процесс разработки:

**Вариант A: После сборки (build.gradle)**
```gradle
android {
    // ...
    applicationVariants.all { variant ->
        variant.assemble.doLast {
            exec {
                commandLine './run_second_display.sh'
            }
        }
    }
}
```

**Вариант B: Через Makefile или скрипт сборки**

## ⚠️ Важные замечания

1. **Путь к скрипту:**
   - Абсолютный путь: `/Users/valentinezov/Projects/Tiggo/development/ЯндексДог/start_second_display.sh`
   - Относительный путь: `./start_second_display.sh` (из корня проекта)

2. **Права на выполнение:**
   - Скрипт должен быть исполняемым на хосте: `chmod +x start_second_display.sh`
   - Скрипт должен быть исполняемым на устройстве: `chmod 755 /data/local/tmp/start_second_display.sh`

3. **Зависимости:**
   - Скрипт может требовать наличие приложения на устройстве
   - Проверьте, что `PresentationActivity` установлен и доступен

## 🎯 Когда использовать

Скрипт запускается когда нужно:
- ✅ Протестировать второй дисплей (Display 1)
- ✅ Запустить Presentation Window
- ✅ Проверить работу упрощенной карты на приборной панели

## 📚 Связанные файлы

- `start_second_display.sh` - основной скрипт для запуска
- `run_second_display.sh` - обертка для запуска через adb
- `PresentationActivity.java` - Activity для второго дисплея
- `TiggoSecondaryRenderThread.java` - поток рендеринга для Display 1

## 🆘 Помощь

Если что-то не работает:

1. **Проверьте подключение:**
   ```bash
   adb devices
   ```

2. **Проверьте права:**
   ```bash
   chmod +x start_second_display.sh run_second_display.sh
   ```

3. **Проверьте логи:**
   ```bash
   adb logcat | grep -i "second\|presentation"
   ```

4. **Попробуйте запустить вручную:**
   ```bash
   adb shell /data/local/tmp/start_second_display.sh
   ```

## ✅ Готово к использованию

Скрипт готов к запуску. Когда нужно будет запустить второй дисплей, просто выполните:

```bash
./run_second_display.sh
```

Или попросите запустить скрипт через adb! 📱

