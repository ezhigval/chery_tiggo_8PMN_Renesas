# Технический анализ модификации APK Yandex Navigator

## Цель анализа

Оценить техническую сложность модификации APK Yandex Navigator для добавления функционала отправки данных в наш мост, не фокусируясь на лицензионных вопросах.

## Структура проекта

### Размер кодовой базы

- **Количество Java файлов:** ~65,869 файлов
- **Основные пакеты:**
  - `com.yandex.navikit.*` - NaviKit SDK
  - `com.yandex.mapkit.*` - MapKit SDK
  - `ru.yandex.yandexmaps.*` - Приложение Yandex Navigator

### Обфускация

**Статус:** Код **НЕ обфусцирован** (или минимально обфусцирован)

**Доказательства:**
- Читаемые имена классов: `GuidanceListener`, `NotificationDataManager`, `GenericGuidanceComponent`
- Читаемые имена методов: `getNextManeuver()`, `onRoutePositionUpdated()`, `notificationData()`
- Структура пакетов понятна

**Вывод:** ✅ Обфускация не является препятствием

## Точки внедрения кода

### Вариант 1: Модификация GuidanceListener

**Локация:** Классы, реализующие `GuidanceListener`

**Найденные классы:**
- `com.yandex.mapkit.navigation.transport.internal.GuidanceBinding`
- `ru.yandex.yandexmaps.multiplatform.camera.scenario.navi.internal.assistant.zoom.b`

**Сложность:** ⭐⭐⭐ (Средняя)

**Проблемы:**
1. Нужно найти все места, где регистрируются listeners
2. Может быть несколько реализаций
3. Нужно понять, какая реализация используется для основной навигации

**Решение:**
- Создать wrapper для `GuidanceListener`
- Перехватывать все вызовы методов
- Отправлять данные в наш мост

### Вариант 2: Модификация NotificationDataManager

**Локация:** Классы, работающие с `NotificationDataManager`

**Сложность:** ⭐⭐ (Низкая-Средняя)

**Преимущества:**
- `NotificationDataManager` уже агрегирует данные навигации
- Есть метод `notificationData()` который возвращает готовые данные
- Можно добавить listener, который будет вызываться при обновлении

**Решение:**
- Найти класс, который подписывается на `NotificationDataManager`
- Добавить наш listener
- В listener отправлять данные в мост

### Вариант 3: Добавление нового класса-моста

**Локация:** Создать новый класс в пакете `ru.yandex.yandexmaps`

**Сложность:** ⭐ (Низкая)

**Преимущества:**
- Не нужно модифицировать существующий код
- Можно использовать reflection для получения данных
- Легко добавить и удалить

**Решение:**
```java
package ru.yandex.yandexmaps.bridge;

public class TiggoBridgeSender {
    private static TiggoBridgeSender instance;
    private Context context;
    
    public static void init(Context context) {
        if (instance == null) {
            instance = new TiggoBridgeSender(context);
        }
    }
    
    public void sendNavigationData(NotificationData data) {
        // Отправка в наш мост
    }
}
```

Затем вызвать `TiggoBridgeSender.init()` в `Application.onCreate()` или в главной Activity.

## Технические сложности

### 1. Подпись APK

**Проблема:** Android требует подпись APK для установки

**Решение:**
- ✅ Использовать debug-ключ для подписи
- ✅ Или создать собственный ключ
- ✅ Или использовать инструменты для удаления подписи и добавления новой

**Сложность:** ⭐ (Низкая)

**Инструменты:**
- `apksigner` (встроен в Android SDK)
- `jarsigner` (старый способ)
- `apktool` + `signapk`

### 2. Ре-компиляция APK

**Проблема:** Нужно декомпилировать, модифицировать и пересобрать

**Решение:**
- ✅ Использовать `apktool` для декомпиляции
- ✅ Модифицировать `.smali` файлы (ассемблер для DEX)
- ✅ Или использовать `jadx` + `dex2jar` + `jar2dex` для работы с Java

**Сложность:** ⭐⭐⭐ (Средняя)

**Процесс:**
```bash
# Декомпиляция
apktool d yandexnavi.apk

# Модификация
# ... редактирование файлов ...

# Компиляция
apktool b yandexnavi

# Подпись
apksigner sign --ks keystore.jks yandexnavi/dist/yandexnavi.apk
```

### 3. Зависимости и библиотеки

**Проблема:** APK может использовать нативные библиотеки (.so файлы)

**Анализ:**
- Yandex Navigator использует MapKit SDK (нативные библиотеки)
- Нужно убедиться, что все `.so` файлы включены

**Решение:**
- ✅ `apktool` сохраняет все ресурсы и библиотеки
- ✅ При ре-компиляции все включается автоматически

**Сложность:** ⭐ (Низкая)

### 4. Проверка целостности

**Проблема:** Приложение может проверять свою подпись

**Анализ:**
- Нужно проверить, есть ли проверка подписи в коде
- Обычно проверяется через `PackageManager.getPackageInfo().signatures`

**Решение:**
- ✅ Найти проверки подписи в коде
- ✅ Удалить или обойти их
- ✅ Или использовать оригинальную подпись (если удастся получить ключ)

**Сложность:** ⭐⭐⭐⭐ (Высокая, если есть проверка)

### 5. Защита от модификации

**Проблема:** Приложение может проверять целостность APK

**Анализ:**
- Проверка хеша APK
- Проверка наличия файлов
- Проверка версии

**Решение:**
- ✅ Найти проверки в коде
- ✅ Обойти их через патчинг

**Сложность:** ⭐⭐⭐⭐ (Высокая, если есть защита)

### 6. Обновления приложения

**Проблема:** При обновлении через Play Store модификации потеряются

**Решение:**
- ✅ Отключить автоматические обновления
- ✅ Использовать версию APK, которая не обновляется
- ✅ Создать скрипт для автоматической модификации новой версии

**Сложность:** ⭐⭐ (Низкая-Средняя)

## Рекомендуемый подход

### Этап 1: Минимальное внедрение (Рекомендуется)

**Цель:** Добавить минимальный код для отправки данных

**Шаги:**

1. **Декомпиляция:**
   ```bash
   apktool d yandexnavi.apk -o yandexnavi_source
   ```

2. **Создание нового класса:**
   - Создать `ru/yandex/yandexmaps/bridge/TiggoBridgeSender.smali`
   - Или декомпилировать в Java, добавить класс, скомпилировать обратно

3. **Инициализация:**
   - Найти главную Activity или Application класс
   - Добавить вызов `TiggoBridgeSender.init(context)`

4. **Интеграция с навигацией:**
   - Найти класс, который работает с `NotificationDataManager`
   - Добавить наш listener

5. **Ре-компиляция и подпись:**
   ```bash
   apktool b yandexnavi_source -o yandexnavi_modified.apk
   apksigner sign --ks keystore.jks yandexnavi_modified.apk
   ```

### Этап 2: Использование Reflection (Альтернатива)

**Цель:** Использовать reflection для получения данных без модификации логики

**Преимущества:**
- Меньше изменений в коде
- Легче поддерживать

**Недостатки:**
- Reflection может быть медленнее
- Может не работать, если код обфусцирован

## Оценка сложности

| Задача | Сложность | Время | Риск |
|--------|-----------|-------|------|
| Декомпиляция APK | ⭐ | 30 мин | Низкий |
| Поиск точек внедрения | ⭐⭐⭐ | 2-4 часа | Средний |
| Создание класса-моста | ⭐⭐ | 1-2 часа | Низкий |
| Интеграция с навигацией | ⭐⭐⭐ | 2-3 часа | Средний |
| Ре-компиляция | ⭐⭐ | 30 мин | Низкий |
| Подпись APK | ⭐ | 15 мин | Низкий |
| Тестирование | ⭐⭐⭐ | 2-4 часа | Средний |
| Обход защиты (если есть) | ⭐⭐⭐⭐ | 4-8 часов | Высокий |

**Общая оценка:** ⭐⭐⭐ (Средняя сложность)

**Время:** 12-22 часа работы

**Риск:** Средний (может увеличиться, если есть защита)

## Инструменты

### Обязательные:
- `apktool` - декомпиляция/компиляция APK
- `apksigner` - подпись APK
- `jadx` - декомпиляция в Java (для чтения)
- Android SDK - для работы с APK

### Опциональные:
- `dex2jar` - конвертация DEX в JAR
- `jd-gui` - просмотр JAR файлов
- `smali/baksmali` - работа с Smali кодом напрямую

## Потенциальные проблемы

### 1. Проверка подписи

**Вероятность:** Средняя

**Симптомы:**
- Приложение не запускается
- Сообщение об ошибке подписи

**Решение:**
- Найти проверку в коде
- Обойти через патчинг

### 2. Проверка целостности

**Вероятность:** Низкая

**Симптомы:**
- Приложение не запускается
- Сообщение о поврежденном APK

**Решение:**
- Найти проверку в коде
- Обойти через патчинг

### 3. Зависимости от версии

**Вероятность:** Высокая

**Симптомы:**
- При обновлении навигатора модификации теряются

**Решение:**
- Отключить обновления
- Создать скрипт для автоматической модификации

### 4. Несовместимость версий

**Вероятность:** Средняя

**Симптомы:**
- Приложение не работает после модификации
- Ошибки при запуске

**Решение:**
- Тестировать на каждой версии
- Создать процесс валидации

## План действий

### Фаза 1: Подготовка (2-3 часа)

1. ✅ Декомпилировать APK
2. ✅ Изучить структуру проекта
3. ✅ Найти точки внедрения
4. ✅ Создать тестовый APK

### Фаза 2: Разработка (4-6 часов)

1. ✅ Создать класс-мост
2. ✅ Интегрировать с навигацией
3. ✅ Реализовать отправку данных
4. ✅ Тестирование на эмуляторе

### Фаза 3: Тестирование (2-4 часа)

1. ✅ Тестирование на реальном устройстве
2. ✅ Проверка всех функций навигатора
3. ✅ Проверка отправки данных
4. ✅ Оптимизация

### Фаза 4: Поддержка (постоянно)

1. ✅ Создать скрипт для автоматической модификации
2. ✅ Документировать процесс
3. ✅ Подготовить инструкции

## Выводы

### ✅ Преимущества подхода:

1. **Сохраняем весь функционал** оригинального навигатора
2. **Минимальные изменения** в коде
3. **Технически выполнимо** - код не обфусцирован
4. **Быстрая реализация** - можно сделать за 1-2 дня

### ⚠️ Риски:

1. **Защита от модификации** - может потребовать дополнительного времени
2. **Обновления** - нужно модифицировать каждую новую версию
3. **Стабильность** - модификации могут влиять на работу приложения

### 🎯 Рекомендация:

**Попробовать модификацию APK**, так как:
- Код не обфусцирован ✅
- Структура понятна ✅
- Технически выполнимо ✅
- Можно сделать быстро ✅

**Начать с минимального внедрения** - добавить только класс-мост и интегрировать его с `NotificationDataManager`.

## Следующие шаги

1. ✅ Декомпилировать APK Yandex Navigator
2. ✅ Найти точку инициализации приложения
3. ✅ Создать класс-мост
4. ✅ Интегрировать с NotificationDataManager
5. ✅ Ре-скомпилировать и протестировать

