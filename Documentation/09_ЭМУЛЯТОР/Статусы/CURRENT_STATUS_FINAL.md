# Текущий статус эмулятора T18FL3

## ✅ Успехи

1. **Кастомный QEMU собран** с поддержкой машины g6sh
2. **Изоляция настроена** - все порты только на localhost
3. **Serial output появился!** - ядро запускается и выводит логи

## 📊 Текущее состояние

### Ядро запускается:
- ✅ Linux version 4.14.133+
- ✅ Machine model: chery,t18fl3-emulator
- ✅ earlycon работает: `pl11 at MMIO 0x0000000009000000`
- ✅ GICv3 обнаружен
- ✅ Память настроена (4GB)

### Проблема:
- ⚠️ Ядро остановилось после инициализации GICv3
- ⚠️ Размер лога не меняется (4269 bytes)
- ⚠️ Последняя строка: `GICv3: no VLPI support, no direct LPI support`

## 🔍 Диагностика

Ядро загружается до этапа инициализации GICv3, затем останавливается. Возможные причины:

1. **Проблема с конфигурацией GIC** - возможно, ядро ожидает другую конфигурацию
2. **Проблема с DTB** - адреса устройств могут быть неправильными
3. **Ядро ждёт инициализации устройств** - возможно, нужно больше времени

## 📝 Команды для мониторинга

```bash
# Быстрый просмотр
cd /Users/valentinezov/Projects/Tiggo/development/emulator
./monitor_serial_live.sh

# Или вручную
PID=$(ps aux | grep "qemu_custom" | grep -v grep | head -1 | awk '{print $2}')
FILE="/tmp/t18fl3_qemu_serial_raw_${PID}.log"
strings "$FILE" | tail -20
```

## 🔒 Изоляция

Все порты изолированы и слушают только на localhost:
- ADB: `127.0.0.1:5556`
- HTTP: `127.0.0.1:8081`
- Monitor: `127.0.0.1:4445`
- VNC: `127.0.0.1:5910`

## 🚀 Следующие шаги

1. Проверить конфигурацию GIC в DTB
2. Попробовать упростить конфигурацию для диагностики
3. Проверить, не ждёт ли ядро инициализации других устройств

