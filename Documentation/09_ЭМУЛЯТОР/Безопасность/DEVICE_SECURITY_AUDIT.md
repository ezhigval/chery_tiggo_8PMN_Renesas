# Аудит безопасности ГУ (головного устройства)

## Общая информация об устройстве

- **Серийный номер**: `00002260`
- **Тип сборки**: `user` (production build)
- **Дата аудита**: 16 ноября 2025

---

## 1. Конфигурация ADB

### Статус доступа
- **Текущий пользователь ADB shell**: `shell` (непривилегированный)
- **`adb root`**: ❌ **ЗАБЛОКИРОВАН** ("adbd cannot run as root in production builds")
- **Оценка**: ✅ Правильная конфигурация для production-сборки

---

## 2. SELinux статус

### Текущая конфигурация
- **Режим**: 🔴 **`Permissive`** (политики логируются, но НЕ применяются)
- **SELinux**: Активен, но не блокирует нарушения

### 🔴 Критическая проблема безопасности

**SELinux в режиме Permissive** существенно снижает защиту системы:
- Процессы могут обращаться к ресурсам, к которым не должны иметь доступа
- Эскалация привилегий становится проще
- Нет изоляции между доменами

### Рекомендации
1. **Переключить в режим `Enforcing`** для production:
   ```bash
   setenforce 1
   ```
   Или в конфигурации: `BOARD_KERNEL_CMDLINE += androidboot.selinux=enforcing`

2. **Провести аудит политик** — проверить нарушения:
   ```bash
   adb shell dmesg | grep avc
   ```

---

## 3. Процессы, работающие от root

### Системные процессы
- `init` (PID 1) — системный инициализатор
- `ueventd` — обработчик событий устройств
- `zygote64` / `zygote` — процессы запуска Android приложений
- `vold` — Volume Daemon (управление хранилищем)
- `netd` — Network Daemon (сетевые настройки, iptables)
- `installd` — установка/удаление приложений
- `storaged` — мониторинг хранилища
- `lmkd` — Low Memory Killer Daemon
- `update_engine` — обновления системы

### HAL сервисы (Hardware Abstraction Layer)
- `android.hardware.automotive.vehicle@2.0-service.g6`
- `android.hardware.boot@1.0-service.renesas`
- `com.desaysv.vehiclelan.proxy@1.0-service`

### Кастомные сервисы
- `dsv_earlypower_svr`
- `vehicle.shmemslaver`
- `vehicle.linkdevicemanager`

### ⚠️ Потенциальные точки атаки

1. **`netd`** (PID 1681)
   - Управляет iptables и сетевыми интерфейсами
   - Принимает команды через Binder
   - **Риск**: некорректная валидация сетевых команд

2. **Кастомные vehicle-сервисы**
   - `vehicle.shmemslaver` — работа с shared memory
   - `vehicle.linkdevicemanager` — управление устройствами
   - **Риск**: если принимают непроверенный ввод от непривилегированных процессов

3. **HAL сервисы**
   - Интерфейсы через Binder/HIDL
   - **Риск**: уязвимости в обработке HIDL-запросов

---

## 4. Рекомендации по улучшению безопасности

### Критичные (высокий приоритет)
1. ✅ **Включить SELinux Enforcing** — немедленно для production
2. 🔍 **Аудит кастомных сервисов** — проверить валидацию ввода в:
   - `dsv_earlypower_svr`
   - `vehicle.shmemslaver`
   - `vehicle.linkdevicemanager`

### Важные (средний приоритет)
3. 🔐 **Минимизация привилегий** — запускать кастомные сервисы под отдельными пользователями
4. 🛡️ **Изоляция процессов** — использовать SELinux домены для разделения
5. 📝 **Логирование и мониторинг** — отслеживать попытки эскалации привилегий

### Дополнительные проверки
6. Проверить права доступа к критичным файлам/директориям
7. Найти setuid-бинарники и проверить их безопасность
8. Аудит Binder/HIDL интерфейсов на предмет уязвимостей
9. Фаззинг IPC-интерфейсов кастомных сервисов

---

## 5. Модель угроз

### Поверхность атаки
- **ADB shell** — доступ с правами `shell` (ограниченный)
- **Приложения** — могут взаимодействовать с системными сервисами через Binder
- **Сетевые интерфейсы** — через `netd` и HAL сервисы
- **Shared memory** — через `vehicle.shmemslaver`

### Пути эскалации привилегий
1. **Уязвимости в root-процессах**:
   - Buffer overflow, format string, integer overflow
   - Некорректная десериализация данных
   - Race conditions в IPC

2. **Неправильные права доступа**:
   - Запись в сокеты/файлы, читаемые root-процессами
   - Небезопасные setuid бинарники

3. **SELinux Permissive**:
   - Отсутствие изоляции между доменами
   - Возможность доступа к ресурсам других процессов

---

## 6. Детальный аудит безопасности

### 6.1 Setuid/Setgid бинарники
- ✅ **Не обнаружено setuid бинарников** (поиск по всей файловой системе)
- ✅ **Не обнаружено setgid бинарников** 
- **Оценка**: Хорошая практика безопасности, снижает риск эскалации привилегий

### 6.2 SELinux нарушения политик (AVC denials)

Обнаружены **множественные нарушения политик SELinux** (режим Permissive позволяет их):

#### Критичные нарушения
- **`direct_network_config`** домен имеет избыточные права:
  - Доступ к `/proc` информации других процессов (kernel, init, vendor_init, vold, zygote)
  - Чтение файлов различных HAL сервисов и system_server
  - Поиск в директориях процессов с разными контекстами безопасности

#### Примеры нарушений:
```
avc: denied { search } for comm="pidof" tcontext=u:r:kernel:s0
avc: denied { read } for comm="pidof" tcontext=u:r:system_server:s0
avc: denied { open } for path="/proc/1237/comm" tcontext=u:r:vendor_init:s0
```

**Вывод**: В режиме `Enforcing` многие операции будут заблокированы. Необходим пересмотр SELinux политик.

### 6.3 Kernel защиты

| Параметр | Значение | Оценка | Описание |
|----------|----------|--------|----------|
| `dmesg_restrict` | `0` | ⚠️ | Непривилегированные пользователи могут читать kernel logs |
| `kptr_restrict` | `2` | ✅ | Адреса ядра скрыты даже от root |
| `ro.debuggable` | `0` | ✅ | Сборка не помечена как отладочная |
| `ro.secure` | `1` | ✅ | Secure mode включён (блокирует adb root) |

**Рекомендация**: Установить `dmesg_restrict=1` для защиты kernel logs.

### 6.4 Права доступа к системным сокетам

#### Критичные сокеты с широкими правами:
```
srw-rw-rw-  logd:logd     /dev/socket/logd
srw-rw-rw-  logd:logd     /dev/socket/logdr
s-w--w--w-  logd:logd     /dev/socket/logdw
srw-rw-rw-  root:root     /dev/socket/property_service
srw-rw-rw-  system:system /dev/socket/tombstoned_*
```

⚠️ **Проблема**: Сокеты с правами `666` (rw-rw-rw-) доступны всем пользователям:
- `logd`, `logdr`, `logdw` — доступ к системным логам
- `property_service` — чтение/запись системных свойств
- `tombstoned_*` — доступ к crash dumps

**Риск**: Утечка информации через логи, возможность манипуляции свойствами системы.

#### Защищённые сокеты (правильная конфигурация):
```
srw-rw----  root:inet     /dev/socket/dnsproxyd
srw-rw----  root:inet     /dev/socket/fwmarkd
srw-rw----  root:system   /dev/socket/netd
srw-rw----  root:system   /dev/socket/zygote
```

### 6.5 Системные сервисы через Binder

Обнаружено **127 системных сервисов**, доступных через Binder IPC.

#### Кастомные сервисы (потенциальные точки атаки):
1. **`linkdevicemanager`** — управление устройствами
2. **`shmemservice`** — shared memory сервис
3. **`dsv_car_power`** — управление питанием автомобиля
4. **`SvAdapterAccess`** — адаптер платформы

**Риск**: Если эти сервисы не проверяют права вызывающего, возможна эскалация привилегий.

### 6.6 Разрешения пользователя shell

Пользователь `shell` (UID 2000) имеет **избыточные разрешения**:

#### Критичные разрешения:
- ✅ `INSTALL_PACKAGES` — установка приложений
- ✅ `DELETE_PACKAGES` — удаление приложений
- ✅ `GRANT_RUNTIME_PERMISSIONS` — выдача разрешений
- ✅ `REVOKE_RUNTIME_PERMISSIONS` — отзыв разрешений
- ✅ `FORCE_STOP_PACKAGES` — остановка приложений
- ✅ `CLEAR_APP_USER_DATA` — очистка данных приложений
- ✅ `MANAGE_DEVICE_ADMINS` — управление администраторами устройства
- ✅ `CREATE_USERS` — создание пользователей
- ✅ `INJECT_EVENTS` — внедрение событий ввода
- ✅ `WRITE_SECURE_SETTINGS` — запись защищённых настроек
- ✅ `MOUNT_UNMOUNT_FILESYSTEMS` — монтирование файловых систем
- ✅ `DUMP` — доступ к дампам системных сервисов
- ✅ `READ_FRAME_BUFFER` — чтение содержимого экрана
- ✅ `INTERACT_ACROSS_USERS_FULL` — взаимодействие между пользователями

#### Runtime разрешения:
- `READ_EXTERNAL_STORAGE` / `WRITE_EXTERNAL_STORAGE`
- `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION`
- `READ_CONTACTS` / `WRITE_CONTACTS`
- `READ_CALENDAR` / `WRITE_CALENDAR`
- `READ_PHONE_STATE` / `CALL_PHONE` / `SEND_SMS`

**Оценка**: 🔴 **Критически избыточные права для production-сборки**

Хотя `adb root` заблокирован, пользователь `shell` может:
- Устанавливать/удалять приложения
- Выдавать себе любые разрешения
- Читать/записывать защищённые настройки
- Получать дампы всех системных сервисов
- Внедрять события ввода (keylogger)
- Читать содержимое экрана

### 6.7 Доступ к debugfs

- **Статус**: ✅ Доступен для чтения (но содержит только базовую информацию)
- **Содержимое**: `binder`, `block`, `dma_buf`, `bcmdhd` и др.
- **Оценка**: Низкий риск, но в production лучше ограничить доступ

---

## 7. Сводная оценка безопасности

### 🔴 Критичные проблемы (требуют немедленного исправления)

1. **SELinux в режиме Permissive**
   - Отсутствует изоляция между процессами
   - Множественные нарушения политик не блокируются
   - **Действие**: Включить Enforcing и исправить политики

2. **Избыточные права пользователя shell**
   - Может устанавливать приложения, выдавать разрешения, читать дампы
   - Фактически имеет почти полный контроль над системой
   - **Действие**: Ограничить разрешения для production-сборки

3. **Широкие права на системные сокеты**
   - `property_service`, `logd` доступны всем (666)
   - Риск утечки информации и манипуляции свойствами
   - **Действие**: Ограничить права до 660 с правильными группами

### ⚠️ Важные проблемы (высокий приоритет)

4. **`dmesg_restrict=0`**
   - Kernel logs доступны непривилегированным пользователям
   - Могут содержать чувствительную информацию
   - **Действие**: Установить `dmesg_restrict=1`

5. **Кастомные сервисы без аудита**
   - `linkdevicemanager`, `shmemservice`, `dsv_car_power`
   - Неизвестно, проверяют ли они права вызывающего
   - **Действие**: Провести code review и фаззинг

### ✅ Положительные аспекты

- ADB root корректно заблокирован
- Отсутствуют setuid/setgid бинарники
- `kptr_restrict=2` (адреса ядра защищены)
- `ro.secure=1` и `ro.debuggable=0`
- Тип сборки: `user` (production)

---

## 8. План действий по улучшению безопасности

### Фаза 1: Критичные исправления (1-2 недели)
1. [ ] Включить SELinux Enforcing
2. [ ] Исправить SELinux политики для устранения AVC denials
3. [ ] Ограничить разрешения пользователя shell
4. [ ] Изменить права на системные сокеты (660 вместо 666)
5. [ ] Установить `dmesg_restrict=1`

### Фаза 2: Аудит кастомных компонентов (2-4 недели)
6. [ ] Code review кастомных сервисов (vehicle.*, dsv_*)
7. [ ] Проверить валидацию входных данных
8. [ ] Убедиться в проверке прав вызывающего (UID/GID/SELinux context)
9. [ ] Добавить логирование попыток несанкционированного доступа

### Фаза 3: Тестирование безопасности (2-3 недели)
10. [ ] Фаззинг Binder/HIDL интерфейсов
11. [ ] Тестирование на проникновение (penetration testing)
12. [ ] Статический анализ кода (SAST)
13. [ ] Динамический анализ (DAST)

### Фаза 4: Мониторинг и защита (ongoing)
14. [ ] Настроить систему мониторинга безопасности
15. [ ] Логирование и алертинг на подозрительную активность
16. [ ] Регулярные обновления безопасности
17. [ ] Периодический аудит (раз в квартал)

---

## 9. История изменений

- **2025-11-16 (14:00)**: Первичный аудит безопасности
  - Обнаружена критическая проблема: SELinux в режиме Permissive
  - Выявлены потенциальные точки атаки в кастомных сервисах
  - ADB root корректно заблокирован для user-сборки

- **2025-11-16 (14:30)**: Детальный аудит
  - Проверены setuid/setgid бинарники (не обнаружено)
  - Анализ SELinux AVC denials (множественные нарушения)
  - Аудит kernel защит (dmesg_restrict=0 - проблема)
  - Проверка прав на системные сокеты (666 на критичных сокетах)
  - Анализ разрешений пользователя shell (избыточные права)
  - Инвентаризация Binder сервисов (127 сервисов, 4 кастомных)
  - Составлен план действий по улучшению безопасности

