# Настройка изоляции T18FL3 эмулятора

## ✅ Выполнено

1. **Остановлены все виртуалки**
   - Все QEMU процессы остановлены
   - Все эмуляторы остановлены
   - ADB сервер остановлен

2. **Настроена изоляция сети**
   - Используется кастомный QEMU: `/Users/valentinezov/Projects/Tiggo/qemu_custom/qemu/build/qemu-system-aarch64`
   - Машина: `g6sh` (кастомная машина вместо virt)
   - Сеть: `restrict=y` - запрещены исходящие подключения
   - Все порты привязаны только к `127.0.0.1` (localhost)

3. **Изолированные порты**
   - ADB: `127.0.0.1:5556` → `:5555` (внутри VM)
   - HTTP: `127.0.0.1:8081` → `:8080` (внутри VM)
   - Monitor: `127.0.0.1:4445` (только localhost)
   - VNC: `127.0.0.1:5910` (только localhost)

## 🔒 Изоляция

### Сеть
- `restrict=y` - запрещает исходящие подключения из VM
- `ipv4=on,ipv6=off` - только IPv4, без IPv6
- `net=10.0.2.0/24` - изолированная подсеть
- Все `hostfwd` привязаны к `127.0.0.1` (не `0.0.0.0`)

### Порты
- Все порты слушают только на `127.0.0.1`
- Нет доступа извне (не из localhost)
- Другие ADB менеджеры не могут подключиться
- Другие виртуалки не могут подключиться

## 🚀 Использование

Эмулятор теперь полностью изолирован:
- Доступ только через localhost
- Нет внешних подключений
- Другие эмуляторы не могут подключиться
- ADB доступен только через `127.0.0.1:5556`

## 📝 Проверка изоляции

```bash
# Проверить, что порты слушают только на localhost
netstat -an | grep -E "(5556|8081|4445|5910)"

# Должно быть:
# tcp4  0  0  127.0.0.1.5556  *.*  LISTEN
# tcp4  0  0  127.0.0.1.8081  *.*  LISTEN
# tcp4  0  0  127.0.0.1.4445  *.*  LISTEN
# tcp4  0  0  127.0.0.1.5910  *.*  LISTEN
```

