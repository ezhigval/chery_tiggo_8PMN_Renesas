# Анализ подходов реализации: Custom Firmware vs App Package

## 📊 Два основных пути реализации

### Путь 1: Custom Firmware (Кастомная прошивка)
**Суть:** Создание собственной прошивки с модифицированными системными приложениями

### Путь 2: App Package + Launcher (Пакет приложений)
**Суть:** Создание пакета приложений и управляющего лаунчера, который скрывает системные приложения

---

## 🔐 Проблема подписи OTA обновлений

### Анализ текущей системы обновлений

#### 1. Структура OTA пакета

**Файлы обновления:**
- `payload.bin` - основной образ системы (1.8 GB)
- `payload_properties.txt` - хеши и размеры файлов
- `META-INF/com/android/otacert` - **сертификат подписи**
- `META-INF/com/android/metadata` - метаданные обновления
- `care_map.txt` - критические блоки для проверки

#### 2. Сертификат подписи

**Из `otacert`:**
```
Issuer: CN=Desay, O=Desay, L=HuiZhou, ST=GuangDong, C=CN
Email: desay@desay-svautomotive.com
Valid: 2020-03-10 to 2047-07-27
```

**Ключевые факты:**
- Сертификат выдан компанией **Desay** (производитель системы)
- Это **dev-keys** (development keys) - видно из `post-build=.../dev-keys`
- Сертификат действителен до 2047 года
- **Приватный ключ НЕ доступен** (он у производителя)

#### 3. Механизм проверки подписи

**Android Recovery проверяет:**
1. **Подпись OTA пакета** - через `otacert`
2. **Хеши файлов** - через `payload_properties.txt`
3. **Совместимость устройства** - через `metadata` (`pre-device=g6sh`)
4. **Версию системы** - через `post-build-incremental`

**Процесс проверки:**
```java
// Recovery проверяет подпись
boolean verifySignature(File otaPackage) {
    // 1. Извлечение сертификата из otacert
    Certificate cert = extractCertificate(otaPackage);
    
    // 2. Проверка подписи пакета
    if (!verifyPackageSignature(otaPackage, cert)) {
        return false; // ОТКЛОНЕНО
    }
    
    // 3. Проверка хешей файлов
    if (!verifyFileHashes(otaPackage)) {
        return false; // ОТКЛОНЕНО
    }
    
    // 4. Проверка совместимости
    if (!verifyDeviceCompatibility(otaPackage)) {
        return false; // ОТКЛОНЕНО
    }
    
    return true; // ПРИНЯТО
}
```

---

## 🛠️ Путь 1: Custom Firmware

### Преимущества:

1. **Полный контроль**
   - Модификация любых системных приложений
   - Изменение системных настроек
   - Полная интеграция с CAN/QNX

2. **Нативная интеграция**
   - Приложения работают как системные
   - Нет ограничений разрешений
   - Прямой доступ к системным API

3. **Стабильность**
   - Нет конфликтов с системными приложениями
   - Единая система обновлений
   - Оптимизация производительности

### Недостатки:

1. **Проблема подписи** ⚠️
   - Нужен приватный ключ от Desay (недоступен)
   - Recovery отклонит неподписанное обновление
   - Невозможно создать валидную подпись

2. **Риски**
   - Блокировка устройства при неудачной прошивке
   - Потеря гарантии
   - Сложность отката

3. **Сложность разработки**
   - Нужно собирать полный образ системы
   - Требуется знание Android build system
   - Долгий цикл разработки

### Решения проблемы подписи:

#### Вариант 1A: Обход проверки подписи (Сложно)

**Методы:**
1. **Модификация Recovery**
   - Замена Recovery на кастомный (TWRP)
   - Отключение проверки подписи
   - **Риск:** Нужен разблокированный bootloader

2. **Патч Recovery в памяти**
   - Временное отключение проверки
   - **Риск:** Требует root доступ

3. **Использование уязвимостей**
   - Эксплойт для обхода проверки
   - **Риск:** Может быть закрыт обновлением

**Вывод:** Сложно, рискованно, требует глубоких знаний

#### Вариант 1B: Получение ключа (Невозможно)

**Проблемы:**
- Приватный ключ хранится у производителя
- Не предоставляется третьим лицам
- Защищен NDA и контрактами

**Вывод:** Невозможно получить легально

#### Вариант 1C: Использование dev-keys (Возможно)

**Идея:** Если система использует dev-keys, возможно:
1. Создать свой dev-keys сертификат
2. Подписать обновление
3. Установить сертификат в систему

**Проблемы:**
- Нужен доступ к системному разделу
- Нужно добавить сертификат в список доверенных
- Может не работать, если проверяется конкретный сертификат

**Вывод:** Теоретически возможно, но сложно

---

## 📦 Путь 2: App Package + Launcher

### Преимущества:

1. **Нет проблем с подписью**
   - Приложения подписываются обычными ключами
   - Устанавливаются как обычные APK
   - Не требуют модификации системы

2. **Безопасность**
   - Не модифицирует системные разделы
   - Легко откатить (удалить приложения)
   - Не влияет на гарантию (если не root)

3. **Простота разработки**
   - Стандартная Android разработка
   - Быстрый цикл разработки
   - Легко тестировать

4. **Гибкость**
   - Можно обновлять приложения отдельно
   - Легко добавлять новые функции
   - Простое распространение

### Недостатки:

1. **Ограничения разрешений**
   - Нужны системные разрешения (QUERY_ALL_PACKAGES, etc.)
   - Могут требовать root или специальной установки
   - Некоторые функции недоступны

2. **Конфликты с системными приложениями**
   - Системные приложения могут перехватывать события
   - Нужно правильно обрабатывать приоритеты
   - Возможны конфликты

3. **Сложность скрытия системных приложений**
   - Нужен системный лаунчер
   - Или overlay поверх системных приложений
   - Может быть нестабильно

### Реализация:

#### Архитектура:

```
┌─────────────────────────────────────┐
│  Custom Launcher (системный)        │
│  - Скрывает системные приложения    │
│  - Показывает только наши           │
└──────────┬──────────────────────────┘
           │
           ├──► Модифицированные приложения:
           │   - SVMedia (с Яндекс Музыкой)
           │   - Bridge Service (для навигации)
           │   - Media Bridge (для метаданных)
           │
           └──► Системные приложения (скрыты)
```

#### Компоненты:

1. **Custom Launcher**
   - Системное приложение
   - Заменяет стандартный лаунчер
   - Скрывает системные приложения
   - Показывает только наши

2. **Модифицированные приложения**
   - SVMedia (с Яндекс Музыкой)
   - Bridge Service (для навигации)
   - Media Bridge (для метаданных)

3. **Overlay приложения** (опционально)
   - Overlay поверх системных приложений
   - Перехват событий
   - Перенаправление на наши приложения

---

## 🎯 Сравнение подходов

| Критерий | Custom Firmware | App Package |
|----------|----------------|-------------|
| **Подпись** | ❌ Проблема (нужен ключ) | ✅ Нет проблем |
| **Сложность** | 🔴 Очень сложно | 🟢 Просто |
| **Риски** | 🔴 Высокие | 🟢 Низкие |
| **Гарантия** | ❌ Потеря | ✅ Сохранена |
| **Откат** | 🔴 Сложно | 🟢 Легко |
| **Контроль** | 🟢 Полный | 🟡 Ограниченный |
| **Интеграция** | 🟢 Нативная | 🟡 Через API |
| **Обновления** | 🔴 Сложно | 🟢 Легко |
| **Разработка** | 🔴 Долго | 🟢 Быстро |

---

## 💡 Рекомендуемый подход: **App Package + Launcher**

### Почему этот подход лучше:

1. **Решает проблему подписи**
   - Не нужен приватный ключ
   - Стандартная установка APK
   - Работает без модификации системы

2. **Безопасность и обратимость**
   - Легко откатить
   - Не влияет на гарантию
   - Не блокирует устройство

3. **Практичность**
   - Быстрая разработка
   - Легкое тестирование
   - Простое распространение

4. **Достаточный функционал**
   - Можно получить нужные разрешения
   - Можно интегрироваться с системой
   - Можно скрыть системные приложения

---

## 🏗️ Детальная архитектура App Package решения

### Компонент 1: Custom Launcher

**Назначение:**
- Замена стандартного лаунчера
- Скрытие системных приложений
- Отображение только наших приложений

**Реализация:**
```xml
<!-- AndroidManifest.xml -->
<activity
    android:name=".MainActivity"
    android:launchMode="singleTask">
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.HOME" />
        <category android:name="android.intent.category.DEFAULT" />
    </intent-filter>
</activity>
```

**Скрытие системных приложений:**
```java
public class CustomLauncher extends Activity {
    private void hideSystemApps() {
        PackageManager pm = getPackageManager();
        
        // Список системных приложений для скрытия
        String[] systemApps = {
            "com.desaysv.mediaapp",      // SVMedia
            "com.desaysv.mapservice",    // SVMapService
            // ... другие
        };
        
        for (String packageName : systemApps) {
            try {
                pm.setComponentEnabledSetting(
                    new ComponentName(packageName, "MainActivity"),
                    PackageManager.COMPONENT_ENABLED_STATE_DISABLED,
                    PackageManager.DONT_KILL_APP
                );
            } catch (Exception e) {
                // Обработка ошибок
            }
        }
    }
}
```

### Компонент 2: Модифицированные приложения

#### 2.1 Модифицированный SVMedia

**Изменения:**
- Добавлен источник "Яндекс Музыка"
- Автоопределение медиа-приложений
- Получение метаданных через MediaSession API

**Установка:**
```bash
# Установка как системное приложение
adb root
adb remount
adb push SVMedia_modified.apk /system/app/SVMedia/
adb shell chmod 644 /system/app/SVMedia/SVMedia_modified.apk
adb reboot
```

**Или через Package Manager:**
```bash
# Установка с заменой системного
adb install -r -d SVMedia_modified.apk
```

#### 2.2 Bridge Service (для навигации)

**Назначение:**
- Запуск Яндекс Навигатора
- Перехват событий
- Преобразование данных
- Отправка в систему

**Установка:**
```bash
# Установка как системное приложение
adb push BridgeService.apk /system/priv-app/BridgeService/
```

#### 2.3 Media Bridge (для метаданных)

**Назначение:**
- Получение метаданных от Яндекс Музыки
- Отправка в SVMeterInteraction
- Отображение на приборной панели

**Установка:**
```bash
# Установка как системное приложение
adb push MediaBridge.apk /system/priv-app/MediaBridge/
```

### Компонент 3: Overlay приложения (опционально)

**Назначение:**
- Overlay поверх системных приложений
- Перехват событий
- Перенаправление на наши приложения

**Реализация:**
```java
public class SystemOverlayService extends Service {
    @Override
    public void onCreate() {
        super.onCreate();
        
        // Создание overlay окна
        WindowManager wm = (WindowManager) getSystemService(WINDOW_SERVICE);
        WindowManager.LayoutParams params = new WindowManager.LayoutParams(
            WindowManager.LayoutParams.MATCH_PARENT,
            WindowManager.LayoutParams.MATCH_PARENT,
            WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY,
            WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE,
            PixelFormat.TRANSLUCENT
        );
        
        // Добавление overlay
        wm.addView(overlayView, params);
    }
}
```

---

## 📋 План реализации App Package решения

### Этап 1: Подготовка (2-3 дня)

1. **Исследование системы**
   - Изучить текущий лаунчер
   - Понять как скрыть системные приложения
   - Проверить доступные разрешения

2. **Подготовка инструментов**
   - Настроить ADB
   - Подготовить скрипты установки
   - Создать резервные копии

### Этап 2: Разработка Custom Launcher (3-5 дней)

1. **Создание лаунчера**
   - Базовый лаунчер
   - Список приложений
   - Скрытие системных приложений

2. **Интеграция с системой**
   - Установка как системного лаунчера
   - Обработка системных событий
   - Управление приложениями

### Этап 3: Модификация приложений (5-7 дней)

1. **Модификация SVMedia**
   - Добавление Яндекс Музыки
   - Автоопределение источников
   - Получение метаданных

2. **Разработка Bridge Service**
   - Запуск Яндекс Навигатора
   - Перехват событий
   - Интеграция с системой

3. **Разработка Media Bridge**
   - Получение метаданных
   - Отправка в систему
   - Отображение на приборной панели

### Этап 4: Установка и тестирование (2-3 дня)

1. **Установка пакета**
   - Установка лаунчера
   - Установка модифицированных приложений
   - Настройка системы

2. **Тестирование**
   - Функциональное тестирование
   - Интеграционное тестирование
   - Тестирование стабильности

---

## 🔧 Технические детали установки

### Установка как системное приложение:

**Метод 1: Через ADB (требует root)**
```bash
# 1. Получить root доступ
adb root

# 2. Перемонтировать /system в режим записи
adb remount

# 3. Установить приложение
adb push MyApp.apk /system/app/MyApp/
adb shell chmod 644 /system/app/MyApp/MyApp.apk

# 4. Перезагрузить
adb reboot
```

**Метод 2: Через Package Manager (без root)**
```bash
# Установка с флагом -r (replace) и -d (downgrade)
adb install -r -d MyApp.apk
```

**Метод 3: Через скрипт установки**
```bash
# Создать скрипт install.sh
#!/bin/bash
adb root
adb remount
adb push CustomLauncher.apk /system/app/CustomLauncher/
adb push SVMedia_modified.apk /system/app/SVMedia/
adb push BridgeService.apk /system/priv-app/BridgeService/
adb push MediaBridge.apk /system/priv-app/MediaBridge/
adb shell chmod 644 /system/app/*/*.apk
adb shell chmod 644 /system/priv-app/*/*.apk
adb reboot
```

### Установка лаунчера:

**После установки Custom Launcher:**
1. При перезагрузке система предложит выбрать лаунчер
2. Выбрать наш Custom Launcher
3. Установить как лаунчер по умолчанию

**Или программно:**
```java
// Установка лаунчера по умолчанию
Intent intent = new Intent(Intent.ACTION_MAIN);
intent.addCategory(Intent.CATEGORY_HOME);
startActivity(intent);
```

---

## ⚠️ Важные замечания

### Разрешения:

**Системные разрешения требуют:**
1. Установки как системное приложение
2. Или root доступа
3. Или специальной подписи

**Критические разрешения:**
- `QUERY_ALL_PACKAGES` - для получения списка приложений
- `REORDER_TASKS` - для переключения приложений
- `SYSTEM_ALERT_WINDOW` - для overlay окон
- `MEDIA_CONTENT_CONTROL` - для получения метаданных

### Безопасность:

1. **Резервные копии**
   - Создать полный бэкап системы
   - Сохранить оригинальные APK
   - Документировать изменения

2. **Откат**
   - Подготовить скрипт отката
   - Сохранить оригинальный лаунчер
   - Документировать процесс восстановления

3. **Тестирование**
   - Тестировать на тестовом устройстве
   - Проверять стабильность
   - Тестировать все функции

---

## 🎯 Итоговые рекомендации

### Выбранный подход: **App Package + Custom Launcher**

**Причины:**
1. ✅ Решает проблему подписи
2. ✅ Безопасно и обратимо
3. ✅ Практично и быстро
4. ✅ Достаточно функционально

### План действий:

1. **Немедленно:**
   - Изучить текущий лаунчер
   - Подготовить инструменты
   - Создать резервные копии

2. **В ближайшее время:**
   - Разработать Custom Launcher
   - Модифицировать SVMedia
   - Разработать Bridge Service

3. **Долгосрочно:**
   - Разработать Media Bridge
   - Создать пакет установки
   - Подготовить документацию

---

## 📚 Полезные ссылки

- **Android System Apps:** https://developer.android.com/guide/topics/manifest/manifest-element#system
- **Custom Launcher:** https://developer.android.com/guide/components/intents-filters#Receiving
- **Package Manager:** https://developer.android.com/reference/android/content/pm/PackageManager
- **ADB Commands:** https://developer.android.com/studio/command-line/adb

---

**Статус:** Анализ завершен, рекомендации готовы  
**Дата:** 2024  
**Версия:** 1.0

