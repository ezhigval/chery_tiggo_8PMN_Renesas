# Анализ решения JAECOO для интеграции Яндекс Навигатора

## 📊 Результаты анализа

### Обнаруженная информация:

1. **YN.apk (Яндекс Навигатор)**
   - Пакет: `ru.yandex.yandexnavi`
   - Версия: 18.8.0 (versionCode: 737093290)
   - API Level: 33 (Android 13)
   - **ВАЖНО:** Это стандартный Яндекс Навигатор, НЕ модифицированная версия!

2. **HUCore.exe**
   - Размер: 28.28 MB
   - Зависимости: libSkiaSharp.dll, libHarfBuzzSharp.dll, av_libglesv2.dll
   - Назначение: Windows приложение для управления установкой и интеграцией

3. **Другие APK в base/**
   - YK.apk - Яндекс Кинопоиск
   - YM.apk - Яндекс Музыка
   - WiFi.apk - WiFi менеджер
   - rustore.apk - RuStore
   - strelka.apk - Стрелка (радар-детектор)

---

## 🔍 Как работает решение JAECOO

### Гипотезы о механизме работы:

#### 1. HUCore.exe - Управляющее приложение

**Возможные функции:**
- Установка APK на устройство через ADB
- Настройка системных разрешений
- Создание системных overlay сервисов
- Настройка проекции/отображения

**Библиотеки:**
- **libSkiaSharp.dll** - Графический рендеринг
  - Возможно используется для:
    - Захвата экрана приложений
    - Рендеринга на проекцию
    - Обработки изображений для HUD
  
- **libHarfBuzzSharp.dll** - Обработка текста
  - Используется для правильного отображения текста
  
- **av_libglesv2.dll** - OpenGL ES
  - Графическая обработка для проекции

#### 2. Механизм проекции (предположительно)

**Вариант A: System Overlay Service**
```
Яндекс Навигатор (обычное приложение)
    ↓
System Overlay Service (созданный HUCore)
    ↓
Захват экрана / Зеркалирование
    ↓
Отправка на проекцию / Приборную панель
```

**Вариант B: Screen Mirroring**
- Захват экрана через MediaProjection API
- Отправка на вторичный дисплей (проекция)
- Возможно через QNX или прямой вывод

**Вариант C: Custom Display Service**
- Создание кастомного сервиса отображения
- Перехват вывода приложений
- Перенаправление на проекцию

#### 3. Интеграция с приборной панелью

**Возможные механизмы:**

1. **Через SVMeterInteraction**
   - HUCore создает промежуточный сервис
   - Сервис перехватывает данные от Яндекс Навигатора
   - Отправляет в SVMeterInteraction через Broadcast
   - SVMeterInteraction передает в CAN-шину

2. **Прямая интеграция с CAN**
   - Если HUCore устанавливает нативные библиотеки
   - Прямая отправка в CAN-шину
   - Обход SVMeterInteraction

3. **Через QNX**
   - Данные отправляются в shared memory
   - QNX читает и отображает на приборной панели

---

## 🎯 Ключевые выводы

### Что делает JAECOO:

1. **Устанавливает стандартный Яндекс Навигатор**
   - Не модифицирует сам APK
   - Использует оригинальную версию

2. **Создает промежуточный слой интеграции**
   - Вероятно через системный сервис или overlay
   - Перехватывает данные/изображение
   - Преобразует и отправляет в систему

3. **Использует HUCore.exe для настройки**
   - Управление установкой
   - Настройка разрешений
   - Создание интеграционных компонентов

### Что НЕ делает JAECOO:

- ❌ Не модифицирует Яндекс Навигатор
- ❌ Не добавляет нативные библиотеки в APK
- ❌ Не изменяет системные приложения

---

## 🏗️ Архитектура решения JAECOO (предположительно)

```
┌─────────────────────┐
│   HUCore.exe        │  ← Windows приложение
│   (управление)      │     для настройки
└──────────┬──────────┘
           │
           │ ADB команды
           │ Установка APK
           │ Настройка разрешений
           ▼
┌─────────────────────┐
│  Android устройство  │
│                      │
│  ┌────────────────┐  │
│  │ Яндекс Навигатор│  │  ← Стандартное приложение
│  │ (ru.yandex...)  │  │
│  └────────┬─────────┘  │
│           │             │
│           │ События / Экран
│           ▼             │
│  ┌────────────────┐     │
│  │ Overlay Service │     │  ← Созданный HUCore
│  │ или Bridge      │     │     (перехват данных)
│  └────────┬─────────┘     │
│           │               │
│           ├──► Broadcast → SVMeterInteraction → CAN
│           │               │
│           ├──► Shared Memory → QNX → Приборная панель
│           │               │
│           └──► Screen Mirroring → Проекция
└───────────────────────────┘
```

---

## 📋 Что нужно сделать для реализации аналогичного решения

### Этап 1: Изучение HUCore.exe

**Задачи:**
1. Анализ HUCore.exe (если возможно)
   - Статический анализ (IDA Pro / Ghidra)
   - Поиск строк и API вызовов
   - Понимание логики работы

2. Мониторинг действий HUCore
   - Запустить HUCore с мониторингом
   - Логировать ADB команды
   - Отследить создаваемые файлы/сервисы

**Инструменты:**
- Process Monitor (Windows)
- ADB logcat
- Статический анализ бинарников

### Этап 2: Поиск создаваемых компонентов

**Что искать на устройстве:**

1. **Системные сервисы**
   ```bash
   adb shell dumpsys activity services | grep -i yandex
   adb shell dumpsys activity services | grep -i overlay
   adb shell dumpsys activity services | grep -i projection
   ```

2. **Разрешения**
   ```bash
   adb shell dumpsys package ru.yandex.yandexnavi | grep permission
   ```

3. **Broadcast Receivers**
   ```bash
   adb shell dumpsys activity broadcasts | grep yandex
   ```

4. **Файлы на устройстве**
   ```bash
   adb shell find /system -name "*yandex*" -o -name "*navi*"
   adb shell find /data -name "*yandex*" -o -name "*navi*"
   ```

### Этап 3: Анализ механизма проекции

**Возможные технологии:**

1. **MediaProjection API**
   - Android API для захвата экрана
   - Требует разрешение пользователя
   - Можно отправлять на вторичный дисплей

2. **Presentation API**
   - Отображение на внешнем дисплее
   - Используется для проекции

3. **System Overlay**
   - TYPE_SYSTEM_OVERLAY
   - Отображение поверх всех окон
   - Может быть использован для проекции

4. **Virtual Display**
   - Создание виртуального дисплея
   - Зеркалирование на проекцию

### Этап 4: Реализация аналогичного решения

**Компоненты для разработки:**

1. **Bridge Service (Android)**
   ```java
   public class YandexNaviBridgeService extends Service {
       // Перехват событий Яндекс Навигатора
       // Преобразование данных
       // Отправка в систему
   }
   ```

2. **Overlay Service (опционально)**
   ```java
   public class NaviOverlayService extends Service {
       // Захват экрана
       // Отправка на проекцию
   }
   ```

3. **Установочный скрипт (Windows/Linux)**
   ```bash
   # Аналог HUCore.exe
   # Установка APK
   # Настройка разрешений
   # Создание сервисов
   ```

---

## 🔧 Практические шаги

### Шаг 1: Установить JAECOO и проанализировать

1. **Запустить HUCore.exe**
   - Подключить устройство
   - Запустить установку
   - Мониторить действия

2. **Логировать ADB команды**
   ```bash
   adb logcat > hucore_install.log
   # Запустить HUCore
   # Остановить логирование
   ```

3. **Проверить изменения на устройстве**
   ```bash
   # До установки
   adb shell dumpsys package ru.yandex.yandexnavi > before.txt
   
   # После установки
   adb shell dumpsys package ru.yandex.yandexnavi > after.txt
   
   # Сравнить
   diff before.txt after.txt
   ```

### Шаг 2: Найти созданные компоненты

1. **Поиск сервисов**
   ```bash
   adb shell dumpsys activity services | grep -A 10 yandex
   ```

2. **Поиск файлов**
   ```bash
   adb shell find /system -type f -name "*navi*" -o -name "*projection*"
   adb shell find /data -type f -name "*navi*" -o -name "*projection*"
   ```

3. **Поиск процессов**
   ```bash
   adb shell ps | grep -i yandex
   adb shell ps | grep -i navi
   ```

### Шаг 3: Анализ работы проекции

1. **Запустить Яндекс Навигатор**
2. **Проверить активные дисплеи**
   ```bash
   adb shell dumpsys display
   ```

3. **Проверить MediaProjection**
   ```bash
   adb shell dumpsys media_projection
   ```

4. **Проверить активные окна**
   ```bash
   adb shell dumpsys window windows | grep -i yandex
   ```

---

## 💡 Рекомендации

### Подход 1: Реверс-инжиниринг JAECOO

**Преимущества:**
- Уже работает
- Можно понять механизм
- Быстрее разработки с нуля

**Недостатки:**
- Может быть закрытый код
- Зависимость от стороннего решения

### Подход 2: Собственная реализация

**Преимущества:**
- Полный контроль
- Адаптация под наши нужды
- Независимость

**Недостатки:**
- Больше времени на разработку
- Нужно тестировать

### Рекомендуемый подход: Гибридный

1. **Изучить JAECOO** - понять механизм
2. **Реализовать свое решение** - на основе понимания
3. **Адаптировать под систему** - интеграция с TurboDog протоколами

---

## 📝 Следующие шаги

### Немедленно:

1. **Запустить HUCore.exe и проанализировать**
   - Мониторить ADB команды
   - Логировать изменения на устройстве
   - Найти созданные компоненты

2. **Установить Яндекс Навигатор через JAECOO**
   - Проверить работу проекции
   - Проанализировать интеграцию

3. **Найти созданные сервисы/компоненты**
   - На устройстве после установки
   - Декомпилировать если возможно

### В ближайшее время:

1. Понять механизм проекции
2. Реализовать аналогичное решение
3. Интегрировать с системой (SVMeterInteraction/QNX/CAN)

---

## 🔗 Полезные ресурсы

- Android MediaProjection: https://developer.android.com/reference/android/media/projection/MediaProjection
- Android Presentation: https://developer.android.com/reference/android/app/Presentation
- Android System Overlay: https://developer.android.com/reference/android/view/WindowManager.LayoutParams#TYPE_SYSTEM_OVERLAY
- Android Virtual Display: https://developer.android.com/reference/android/hardware/display/DisplayManager#createVirtualDisplay

---

**Статус:** Анализ завершен, требуется практическое тестирование
**Дата:** 2024
**Версия:** 1.0

