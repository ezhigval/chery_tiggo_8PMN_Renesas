# План интеграции Яндекс Навигатора вместо TurboDog

## 🎯 Цель проекта

Заменить TurboDog навигацию на Яндекс Навигатор (или 2ГИС) с сохранением всей интеграции с системой автомобиля:
- Отображение на приборной панели
- Фоновый режим работы
- Отправка данных на проекцию (указатели поворота, дистанция до маневра)
- Интеграция через QNX и CAN-шину
- Голосовое управление через iFlytek

---

## 📊 Анализ текущей системы

### Обнаруженная информация:

1. **SVMapService (TurboDog)**
   - Пакет: `com.desaysv.mapservice`
   - Версия: T1D_MapService_02_12._250808_
   - API Level: 28 (Android 9)

2. **SVMeterInteraction (CAN-шина)**
   - Отвечает за взаимодействие с приборной панелью
   - Связь с CAN-шиной
   - Отображение данных на экране

3. **iFlytek интеграция**
   - Домен навигации: `mapU`
   - Протокол: JSON (naviType: 1)
   - Голосовое управление навигацией

4. **QNX система**
   - Параллельная ОС для критических систем
   - Образы: `qnx_boot.img`, `qnx_system.img`
   - Используется для приборной панели и CAN-шины

---

## 🔍 Как работает TurboDog (гипотезы на основе анализа)

### Каналы коммуникации:

1. **Android Broadcast Intents**
   - SVMapService отправляет события навигации через Broadcast
   - SVMeterInteraction получает эти события
   - Другие приложения могут подписываться на события

2. **AIDL интерфейсы (вероятно)**
   - Системные сервисы могут использовать AIDL для IPC
   - Прямое взаимодействие между сервисами

3. **Shared Memory / Unix Socket**
   - Для обмена данными с QNX системой
   - Через `/dev/shm` или `/tmp`
   - QNX читает данные и отображает на приборной панели

4. **CAN-шина (через нативные библиотеки)**
   - JNI библиотеки (.so файлы) для работы с CAN
   - SVMeterInteraction или отдельный сервис отправляет данные в CAN
   - Приборная панель получает данные через CAN

5. **Системные свойства (System Properties)**
   - Android System Properties для обмена данными
   - QNX может читать через `/proc/sys/` или аналогичные механизмы

---

## 🏗️ Рекомендуемая архитектура решения

### Вариант 1: Bridge Service (Рекомендуется)

```
┌─────────────────┐
│ Яндекс Навигатор │
└────────┬─────────┘
         │ Broadcast Intents / AIDL
         ▼
┌─────────────────────┐
│ YandexNaviBridge    │  ← Новый сервис
│ (системный сервис)  │
└────────┬────────────┘
         │
         ├──► Broadcast → SVMeterInteraction → CAN-шина
         │
         ├──► Shared Memory → QNX система
         │
         └──► System Properties → QNX читает
```

**Преимущества:**
- Минимальные изменения в существующей системе
- Яндекс Навигатор работает как обычное приложение
- Bridge Service перехватывает и преобразует данные
- Легко откатить изменения

**Недостатки:**
- Нужно разработать Bridge Service
- Требуется понимание протоколов TurboDog

### Вариант 2: Модификация Яндекс Навигатора

```
┌──────────────────────────┐
│ Яндекс Навигатор (модиф.) │
│ + интеграция с системой   │
└────────┬───────────────────┘
         │
         ├──► Прямая интеграция
         │
         └──► CAN/QNX
```

**Преимущества:**
- Прямая интеграция
- Меньше промежуточных слоев

**Недостатки:**
- Нарушение лицензии Яндекс Навигатора
- Сложность обновления приложения
- Не рекомендуется

### Вариант 3: Плагин для Яндекс Навигатора

Если Яндекс предоставляет SDK или API для интеграции:
- Использовать официальный API
- Разработать плагин/расширение
- Интеграция через официальные механизмы

---

## 📋 Детальный план реализации (Вариант 1)

### Этап 1: Анализ и реверс-инжиниринг

#### 1.1 Декомпиляция SVMapService
- [ ] Извлечь DEX из VDEX (vdexExtractor)
- [ ] Декомпилировать DEX в Java (JADX)
- [ ] Проанализировать исходный код
- [ ] Найти классы, отвечающие за:
  - Отправку данных навигации
  - Broadcast Intents
  - AIDL интерфейсы
  - Работу с CAN/QNX

#### 1.2 Анализ SVMeterInteraction
- [ ] Декомпилировать APK
- [ ] Найти обработчики событий навигации
- [ ] Определить формат данных
- [ ] Найти нативные библиотеки (.so)

#### 1.3 Анализ нативных библиотек
- [ ] Извлечь .so файлы из APK
- [ ] Анализ с помощью IDA Pro / Ghidra
- [ ] Найти функции работы с CAN
- [ ] Найти функции работы с QNX (shared memory, sockets)

#### 1.4 Анализ протоколов обмена
- [ ] Логирование Broadcast Intents
- [ ] Анализ системных свойств
- [ ] Мониторинг shared memory
- [ ] Анализ CAN-сообщений (если есть доступ)

**Инструменты:**
- `vdexExtractor` - извлечение DEX
- `jadx` или `jadx-gui` - декомпиляция в Java
- `aapt2` - анализ AndroidManifest
- `IDA Pro` / `Ghidra` - анализ нативных библиотек
- `adb logcat` - логирование событий
- `adb shell getprop` - системные свойства

### Этап 2: Разработка Bridge Service

#### 2.1 Создание проекта
- [ ] Создать Android проект (Android Studio)
- [ ] Настроить как системное приложение
- [ ] Добавить необходимые разрешения
- [ ] Подписать системным ключом

#### 2.2 Реализация перехвата событий Яндекс Навигатора

**Вариант A: Broadcast Intents**
```java
// Подписка на события Яндекс Навигатора
IntentFilter filter = new IntentFilter();
filter.addAction("ru.yandex.yandexnavi.NAVIGATION_STARTED");
filter.addAction("ru.yandex.yandexnavi.NAVIGATION_UPDATE");
filter.addAction("ru.yandex.yandexnavi.NAVIGATION_ENDED");
registerReceiver(naviReceiver, filter);
```

**Вариант B: Accessibility Service**
- Использовать Accessibility Service для перехвата UI событий
- Парсить данные из интерфейса Яндекс Навигатора

**Вариант C: Yandex Navigator SDK (если есть)**
- Использовать официальный SDK
- Подписаться на события навигации

#### 2.3 Преобразование данных

Создать адаптер, который преобразует данные Яндекс Навигатора в формат TurboDog:

```java
public class NaviDataAdapter {
    // Преобразование направления поворота
    public String convertTurnDirection(String yandexDirection) {
        // "left" -> формат TurboDog
        // "right" -> формат TurboDog
    }
    
    // Преобразование дистанции
    public int convertDistance(int yandexMeters) {
        // Конвертация в формат системы
    }
    
    // Преобразование голосовой подсказки
    public String convertVoicePrompt(String yandexText) {
        // Адаптация текста
    }
}
```

#### 2.4 Отправка данных в систему

**A. Broadcast Intents (как TurboDog)**
```java
Intent intent = new Intent("com.desaysv.mapservice.NAVIGATION_UPDATE");
intent.putExtra("turn_direction", direction);
intent.putExtra("distance", distance);
intent.putExtra("speed", speed);
sendBroadcast(intent);
```

**B. Shared Memory (для QNX)**
```java
// Запись в shared memory
MemoryFile memoryFile = new MemoryFile("navi_data", size);
// QNX читает из /dev/shm/navi_data
```

**C. System Properties**
```java
SystemProperties.set("sys.navi.direction", direction);
SystemProperties.set("sys.navi.distance", String.valueOf(distance));
// QNX читает через /proc/sys/...
```

**D. CAN-шина (через SVMeterInteraction или нативный код)**
```java
// Вызов нативного метода
nativeSendCanMessage(direction, distance);
```

### Этап 3: Интеграция с Яндекс Навигатором

#### 3.1 Определение событий Яндекс Навигатора

Нужно исследовать, какие события отправляет Яндекс Навигатор:

**Методы исследования:**
1. Установить Яндекс Навигатор на устройство
2. Запустить `adb logcat` и фильтровать по пакету `ru.yandex.yandexnavi`
3. Найти Broadcast Intents
4. Проанализировать логи

**Возможные события:**
- Начало навигации
- Обновление маршрута
- Приближение к маневру
- Голосовые подсказки
- Изменение скорости

#### 3.2 Альтернативные методы получения данных

Если Яндекс Навигатор не отправляет Broadcast:

**A. Accessibility Service**
- Перехватывать UI события
- Парсить текст на экране
- Определять направление и дистанцию по UI

**B. Screen Content API (Android 11+)**
- Читать содержимое экрана
- Распознавать элементы навигации

**C. Модификация Яндекс Навигатора (не рекомендуется)**
- Добавить отправку Broadcast
- Нарушение лицензии

#### 3.3 Использование 2ГИС (альтернатива)

Если Яндекс Навигатор не подходит:
- Исследовать события 2ГИС
- Аналогичная интеграция
- Возможно, проще интегрировать

### Этап 4: Интеграция с QNX

#### 4.1 Механизмы обмена данными с QNX

**A. Shared Memory**
```c
// QNX сторона
int shm_fd = shm_open("/navi_data", O_RDONLY, 0666);
void *ptr = mmap(NULL, size, PROT_READ, MAP_SHARED, shm_fd, 0);
// Чтение данных навигации
```

**B. Message Queue**
```c
// QNX Message Queue
mqd_t mq = mq_open("/navi_queue", O_RDONLY);
mq_receive(mq, buffer, size, NULL);
```

**C. Unix Socket**
```c
// Unix Domain Socket
int sock = socket(AF_UNIX, SOCK_DGRAM, 0);
struct sockaddr_un addr;
addr.sun_family = AF_UNIX;
strcpy(addr.sun_path, "/tmp/navi_socket");
sendto(sock, data, size, 0, (struct sockaddr*)&addr, sizeof(addr));
```

**D. System Properties**
- QNX может читать через `/proc/sys/` или аналогичные механизмы

#### 4.2 Формат данных для QNX

Нужно определить формат, который ожидает QNX:

```c
typedef struct {
    int turn_direction;  // 0=прямо, 1=налево, 2=направо
    int distance;       // метры до маневра
    int speed;          // текущая скорость
    char voice_text[256]; // текст подсказки
} navi_data_t;
```

### Этап 5: Интеграция с CAN-шиной

#### 5.1 Анализ CAN-протокола

Нужно найти:
- Какие CAN-сообщения отправляет TurboDog
- ID сообщений
- Формат данных
- Частота отправки

**Методы:**
- Анализ кода SVMeterInteraction
- Анализ нативных библиотек
- Мониторинг CAN-шины (если есть доступ)

#### 5.2 Реализация отправки в CAN

**Через SVMeterInteraction:**
- Отправлять Broadcast Intent
- SVMeterInteraction пересылает в CAN

**Напрямую (если есть доступ):**
```java
// JNI вызов
public native int sendCanMessage(int id, byte[] data);

// C код
JNIEXPORT jint JNICALL
Java_com_example_NaviBridge_sendCanMessage(JNIEnv *env, jobject obj, jint id, jbyteArray data) {
    // Отправка в CAN через драйвер
    can_send(id, data, length);
}
```

### Этап 6: Голосовое управление

#### 6.1 Интеграция с iFlytek

В `business.ini` уже есть домен `mapU` для навигации.

Нужно:
- Настроить iFlytek для работы с Яндекс Навигатором
- Адаптировать команды голосового управления
- Обработка команд через Bridge Service

#### 6.2 Команды навигации

Примеры команд:
- "Построй маршрут до..."
- "Покажи карту"
- "Отмени навигацию"
- "Объезд пробки"

Bridge Service должен:
- Получать команды от iFlytek
- Преобразовывать в команды Яндекс Навигатора
- Отправлять через Intent или Accessibility

### Этап 7: Тестирование и отладка

#### 7.1 Модульное тестирование
- Тестирование адаптера данных
- Тестирование Broadcast Intents
- Тестирование shared memory

#### 7.2 Интеграционное тестирование
- Тестирование на реальном устройстве
- Проверка отображения на приборной панели
- Проверка CAN-сообщений
- Проверка голосового управления

#### 7.3 Отладка
- Логирование всех событий
- Использование `adb logcat`
- Анализ производительности

---

## 🛠️ Необходимые инструменты

### Для анализа:
1. **vdexExtractor** - извлечение DEX из VDEX ✅ (есть в проекте)
2. **JADX** - декомпиляция DEX в Java (нужно установить)
3. **aapt2** - анализ APK ✅ (есть в jaecoo/adb)
4. **IDA Pro / Ghidra** - анализ нативных библиотек
5. **adb** - работа с устройством ✅ (есть в jaecoo/adb)

### Для разработки:
1. **Android Studio** - разработка Bridge Service
2. **JDK** - компиляция Java
3. **Android NDK** - для нативного кода (если нужно)
4. **Системный ключ подписи** - для системного приложения

### Для тестирования:
1. **Реальное устройство** - головное устройство автомобиля
2. **CAN-анализатор** - для проверки CAN-сообщений (опционально)
3. **Логирование** - adb logcat

---

## 📝 Следующие шаги

### Немедленные действия:

1. **Декомпиляция SVMapService**
   ```bash
   # Установить JADX
   # Извлечь DEX из VDEX
   vdexExtractor -i SVMapService.vdex -o output/
   # Декомпилировать DEX
   jadx -d sources/ output/*.dex
   ```

2. **Анализ AndroidManifest**
   - Изучить разрешения
   - Найти Broadcast Intents
   - Найти AIDL интерфейсы

3. **Исследование событий Яндекс Навигатора**
   - Установить на устройство
   - Запустить навигацию
   - Логировать события через `adb logcat`

4. **Анализ SVMeterInteraction**
   - Декомпилировать
   - Найти обработчики событий навигации
   - Определить формат данных

### Среднесрочные задачи:

1. Разработка прототипа Bridge Service
2. Реализация перехвата событий Яндекс Навигатора
3. Реализация преобразования данных
4. Реализация отправки в систему

### Долгосрочные задачи:

1. Полная интеграция с QNX
2. Интеграция с CAN-шиной
3. Голосовое управление
4. Тестирование и оптимизация

---

## ⚠️ Важные замечания

1. **Безопасность**
   - Всегда создавать резервные копии перед изменениями
   - Тестировать на тестовом устройстве
   - Иметь возможность отката

2. **Лицензии**
   - Не нарушать лицензию Яндекс Навигатора
   - Использовать официальные API, если доступны

3. **Совместимость**
   - Учитывать обновления системы
   - Обеспечить обратную совместимость

4. **Производительность**
   - Минимизировать задержки
   - Оптимизировать использование ресурсов

---

## 📚 Полезные ресурсы

- Android Broadcast Intents: https://developer.android.com/guide/components/broadcasts
- Android AIDL: https://developer.android.com/guide/components/aidl
- Android Shared Memory: https://developer.android.com/reference/android/os/MemoryFile
- QNX Inter-Process Communication: https://www.qnx.com/developers/docs/7.1/
- CAN Bus Protocol: https://en.wikipedia.org/wiki/CAN_bus

---

## 🎯 Итоговая архитектура

```
┌──────────────────────┐
│   Яндекс Навигатор   │
│  (обычное приложение) │
└──────────┬────────────┘
           │
           │ События навигации
           │ (Broadcast / Accessibility)
           ▼
┌──────────────────────┐
│  YandexNaviBridge    │
│  (системный сервис)   │
│                      │
│  - Перехват событий  │
│  - Преобразование    │
│  - Отправка в систему│
└──────────┬───────────┘
           │
           ├──► Broadcast → SVMeterInteraction → CAN-шина → Приборная панель
           │
           ├──► Shared Memory → QNX → Приборная панель
           │
           └──► System Properties → QNX
```

---

**Статус:** План готов к реализации
**Дата:** 2024
**Версия:** 1.0

