# Инструкция для AI: Восстановление контекста проекта

## Кодовое слово: "прочитай файл"

**Когда пользователь пишет "прочитай файл" или "восстанови контекст":**

1. **СРОЧНО прочитай эти файлы в указанном порядке:**
   - `development/КОНТЕКСТ_ПРОЕКТА.md` - полный контекст проекта
   - `development/ИСТОРИЯ_РАЗРАБОТКИ.md` - история решений и попыток
   - `development/БЫСТРЫЙ_СТАРТ.md` - краткая сводка для быстрого старта

2. **После чтения ты должен знать:**
   - Цель проекта: перехват данных навигации из Yandex Navigator/2GIS/TurboDog для отображения на приборной панели
   - Текущий статус: декомпиляция APK завершена, нужно анализировать код
   - Все попытки решения (Reflection, AccessibilityService, BroadcastReceiver)
   - Структуру проекта и ключевые классы
   - Формат данных навигации
   - Следующие шаги

3. **Дополнительно прочитай (если нужно углубиться):**
   - `development/НАЙДЕННЫЕ_ПАКЕТЫ.md` - список пакетов
   - `development/КОМАНДЫ_ADB.md` - ADB команды
   - `development/ИДЕИ_ПО_ДИЗАЙНУ.md` - идеи по дизайну UI

## Ключевые факты для быстрого восстановления

### Проект
- **Название**: Tiggo Bridge Service
- **Цель**: Android сервис для перехвата данных навигации и передачи в штатную систему автомобиля
- **Язык**: Java (Android)
- **Пакет**: `com.desaysv.tiggo.bridge`

### Текущий статус (последнее обновление)
- ✅ Декомпиляция APK завершена (63886 Java файлов)
- ✅ Документация создана
- 🔄 Следующий шаг: анализ декомпилированного кода TurboDog

### Ключевые компоненты
- `BridgeService.java` - главный фоновый сервис
- `NavigationDataInterceptor.java` - перехват данных через BroadcastReceiver
- `NavigationMonitor.java` - мониторинг состояния навигатора
- `NavigationData.java` - модель данных навигации

### Важные пакеты
- `com.astrob.turbodog` - штатная навигация (основная цель анализа)
- `ru.yandex.yandexnavi` - Yandex Navigator
- `com.desaysv.tiggo.bridge` - наше приложение

### Что НЕ сработало
1. ❌ Reflection - поля `handler` и `consumer` в `GenericGuidanceImpl` равны null
2. ❌ AccessibilityService - нет настроек в автомобильной системе

### Что работает
- ✅ BroadcastReceiver для перехвата `turbodog.navigation.system.message`
- ✅ Мониторинг состояния навигатора через `UsageStatsManager`

### Формат данных TurboDog
Broadcast содержит extras:
- `EXTRA_TURN_DIST` - расстояние до маневра (метры)
- `EXTRA_TURN_TYPE` - тип маневра (int: 0=STRAIGHT, 1=LEFT, 2=RIGHT, 3=UTURN)
- `EXTRA_REMAINING_DIST` - оставшееся расстояние (метры)
- `EXTRA_REMAINING_TIME` - оставшееся время (секунды)
- `EXTRA_ARRIVE_TIME` - время прибытия (String)
- `EXTRA_CURRENT_ROAD` / `EXTRA_NEXT_ROAD` - названия дорог
- `EXTRA_CURRENT_LAT` / `EXTRA_CURRENT_LON` - координаты
- `EXTRA_SPEED_LIMIT` - ограничение скорости (км/ч)

### Структура проекта
```
development/
├── projects/TiggoBridgeService/     # Android приложение
├── extracted_from_device/            # APK и декомпилированный код
│   ├── *.apk                        # 8 APK файлов
│   └── decompiled/                  # 63886 Java файлов
├── scripts/                         # Скрипты автоматизации
└── [документация].md                # Все документы здесь
```

### Следующие задачи
1. Анализ декомпилированного кода `com.astrob.turbodog`
2. Поиск всех способов передачи данных (broadcast, ContentProvider, файлы)
3. Улучшение `NavigationDataInterceptor` на основе анализа
4. Тестирование на реальном устройстве

## Что делать при восстановлении контекста

1. **Прочитай основные файлы** (см. выше)
2. **Подтверди понимание**: кратко резюмируй текущий статус проекта
3. **Предложи следующие шаги**: что делать дальше
4. **Будь готов к вопросам** о:
   - Архитектуре проекта
   - Проблемах, с которыми столкнулись
   - Решениях, которые были приняты
   - Декомпилированном коде

## Важные напоминания

- **Не забывай**: Reflection не работает, AccessibilityService недоступен
- **Фокус**: Анализ декомпилированного кода TurboDog для понимания механизма передачи данных
- **Цель**: Надежный перехват данных навигации без "слетания"
- **Пользователь упоминал**: Jcartools успешно перехватывает данные из Yandex Navigator - можно изучить их подход

## Если пользователь спрашивает про дизайн

Напомни про файл `development/ИДЕИ_ПО_ДИЗАЙНУ.md` - там сохранены идеи по внешнему виду приложения для будущей переработки UI.

---

**ВАЖНО**: Этот файл должен быть первым, что ты читаешь при восстановлении контекста. Он содержит ссылки на все остальные важные документы.

