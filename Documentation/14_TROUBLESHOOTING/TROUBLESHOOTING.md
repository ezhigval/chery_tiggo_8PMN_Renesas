# Решение проблем запуска

## ✅ Эмулятор запущен!

GUI эмулятор работает. Если окно не видно:

### Проверка 1: Окно может быть скрыто
- Проверьте Dock (нижняя панель macOS)
- Проверьте панель задач
- Попробуйте Alt+Tab (Cmd+Tab на macOS) для переключения окон

### Проверка 2: Запуск в терминале
```bash
cd development/emulator
python3 main.py
```

Окно должно открыться автоматически.

### Проверка 3: Процесс работает?
```bash
ps aux | grep "python.*main.py" | grep -v grep
```

Если процесс есть - окно должно быть видно.

## 🔧 Если QEMU не запускается

### Проблема: Кнопка Start не работает

1. **Проверьте пути к образам:**
   ```bash
   ls -lh update_extracted/payload/*.img
   ```

2. **Проверьте логи:**
   ```bash
   tail -f development/emulator/logs/*.log
   ```

3. **Проверьте QEMU:**
   ```bash
   which qemu-system-aarch64
   /opt/homebrew/bin/qemu-system-aarch64 --version
   ```

### Проблема: Ошибки при запуске QEMU

1. **Недостаточно памяти:**
   - Уменьшите размер product.img (если возможно)
   - Или увеличьте RAM хоста

2. **Образы не найдены:**
   - Проверьте пути в `gui/main_window.py`
   - Убедитесь, что образы распакованы

3. **QEMU ошибки:**
   - Проверьте логи QEMU: `/tmp/t18fl3_qemu_*.log`
   - Убедитесь, что QEMU поддерживает все параметры

## 🚀 Альтернативный запуск (без GUI)

Если GUI не работает, используйте скрипт:

```bash
cd development/scripts
./simulate_t18fl3_complete.sh --android
```

Затем подключитесь через VNC:
```bash
vncviewer localhost:5900  # Display 1
vncviewer localhost:5901  # Display 2
```

## 📝 Логи для отладки

```bash
# GUI логи
tail -f development/emulator/logs/*.log

# QEMU логи
tail -f /tmp/t18fl3_qemu_*.log

# Скрипт логи
tail -f /tmp/t18fl3_launch_full.log
```

## ✅ Текущий статус

- ✅ GUI эмулятор: **РАБОТАЕТ**
- ✅ Импорты: **ИСПРАВЛЕНЫ**
- ✅ Образы: **ГОТОВЫ**
- ⚠️ JSON логирование: отключено (не критично)

**Эмулятор готов к использованию!**

