# План извлечения данных из QNX - G6SH/T18FL3

**Дата:** 2025-11-27
**Цель проекта:** Создание сервиса-моста для передачи данных навигации в QNX для отображения на приборной панели

---

## 🎯 ЦЕЛИ ИЗВЛЕЧЕНИЯ ДАННЫХ

### Основные задачи:

1. **Протоколы обмена данными** - понять, как Android отправляет данные в QNX
2. **API для приборной панели** - найти интерфейсы для отправки навигационных данных
3. **Структура данных** - изучить формат данных, которые принимает QNX
4. **Конфигурация системы** - извлечь настройки и параметры
5. **Логи и диагностика** - получить информацию о работе системы

---

## 📋 ПЛАН ИЗВЛЕЧЕНИЯ ДАННЫХ

### 1. СЕТЕВЫЕ ПРОТОКОЛЫ И ПОРТЫ

**Цель:** Определить протоколы обмена данными между Android и QNX

**Действия:**

```bash
# Проверить активные соединения
adb shell "netstat -an | grep 192.168.2.1"

# Анализ трафика через port forwarding
adb forward tcp:10004 tcp:10004
adb forward tcp:10005 tcp:10005
adb forward tcp:31030 tcp:31030
adb forward tcp:31040 tcp:31040
adb forward tcp:31050 tcp:31050

# Попытка подключения и анализа протокола
nc localhost 10005
telnet localhost 10005
```

**Что извлечь:**
- Протокол обмена (TCP/UDP, бинарный/текстовый)
- Формат сообщений
- Команды и ответы
- Структура пакетов данных

---

### 2. QNX SHARED MEMORY

**Цель:** Изучить shared memory для обмена данными

**Действия:**

```bash
# Информация о shared memory
adb shell "cat /sys/devices/platform/vdevs/1c050000.qnx,guest_shm/uevent"
adb shell "ls -la /sys/devices/platform/vdevs/1c050000.qnx,guest_shm/"

# Попытка чтения (если доступно)
adb shell "cat /dev/qnx_guest_shm 2>/dev/null"
adb shell "hexdump -C /dev/qnx_guest_shm 2>/dev/null | head -50"
```

**Что извлечь:**
- Размер shared memory
- Структура данных
- Формат обмена
- Метки/заголовки данных

---

### 3. VIRTIO SERIAL INTERFACES

**Цель:** Изучить virtio serial для коммуникации

**Действия:**

```bash
# Информация о virtio serial driver
adb shell "ls -la /sys/bus/virtio/drivers/virtio_rproc_serial/"
adb shell "cat /sys/bus/virtio/drivers/virtio_rproc_serial/uevent"

# Поиск устройств virtio
adb shell "find /dev -name '*virtio*' -o -name '*vport*' -o -name '*hvc*' 2>/dev/null"
adb shell "ls -la /sys/class/virtio* 2>/dev/null"
```

**Что извлечь:**
- Доступные virtio устройства
- Конфигурация каналов
- Протокол обмена

---

### 4. СИСТЕМНЫЕ СЕРВИСЫ И ПРОЦЕССЫ

**Цель:** Найти сервисы, отвечающие за коммуникацию с QNX

**Действия:**

```bash
# Список процессов
adb shell "ps -A | grep -iE 'qnx|mcu|cluster|instrument|dashboard'"

# Список сервисов
adb shell "service list | grep -iE 'qnx|mcu|cluster|instrument'"

# Dumpsys для сервисов
adb shell "dumpsys | grep -iE 'qnx|mcu|cluster|instrument' -A 10"
```

**Что извлечь:**
- Имена процессов/сервисов
- PID процессов
- Параметры запуска
- Логи процессов

---

### 5. КОНФИГУРАЦИОННЫЕ ФАЙЛЫ

**Цель:** Извлечь конфигурацию системы и QNX

**Действия:**

```bash
# System properties
adb shell "getprop | grep -iE 'qnx|mcu|cluster|instrument|dashboard'"

# Конфигурационные файлы
adb shell "find /system/etc /vendor/etc /oem /persist -name '*qnx*' -o -name '*mcu*' -o -name '*cluster*' 2>/dev/null"

# Чтение конфигурации
adb shell "cat /system/etc/*qnx* 2>/dev/null"
adb shell "cat /vendor/etc/*qnx* 2>/dev/null"
```

**Что извлечь:**
- Параметры подключения
- Настройки протоколов
- IP адреса и порты
- Форматы данных

---

### 6. БИНАРНИКИ И БИБЛИОТЕКИ

**Цель:** Найти исполняемые файлы и библиотеки для работы с QNX

**Действия:**

```bash
# Поиск бинарников
adb shell "find /system/bin /vendor/bin /oem -name '*qnx*' -o -name '*mcu*' -o -name '*cluster*' 2>/dev/null"

# Поиск библиотек
adb shell "find /system/lib* /vendor/lib* -name '*qnx*' -o -name '*mcu*' -o -name '*cluster*' 2>/dev/null"

# Информация о бинарниках
adb shell "file /system/bin/*qnx* 2>/dev/null"
adb shell "strings /system/bin/*qnx* 2>/dev/null | head -50"
```

**Что извлечь:**
- Исполняемые файлы
- Shared libraries (.so)
- Строки в бинарниках (API, протоколы)
- Зависимости

---

### 7. APK АНАЛИЗ

**Цель:** Изучить APK приложения, работающие с QNX

**Действия:**

```bash
# Список пакетов Desay
adb shell "pm list packages | grep desay"

# Извлечение APK
adb shell "pm path com.desaysv.*" | sed 's/package://' | xargs -I {} adb pull {}

# Анализ манифестов
adb shell "dumpsys package com.desaysv.* | grep -iE 'service|receiver|permission'"
```

**Ключевые пакеты для анализа:**
- `com.desaysv.carlan` - Car LAN Service
- `com.desaysv.service.link` - Link Service
- `com.desaysv.vehicle.carplayapp` - Vehicle Service
- `com.desaysv.setting` - Settings (может содержать QNX настройки)

**Что извлечь:**
- Broadcast receivers
- Services
- Intent filters
- Permissions
- API методы

---

### 8. ЛОГИ И ДИАГНОСТИКА

**Цель:** Получить логи работы системы с QNX

**Действия:**

```bash
# Logcat с фильтрами
adb logcat | grep -iE 'qnx|mcu|cluster|instrument|dashboard'

# Dmesg
adb shell "dmesg | grep -iE 'qnx|mcu|virtio|serial' | tail -100"

# Системные логи
adb shell "logcat -d | grep -iE 'qnx|mcu|cluster' | tail -200"

# Логи сервисов
adb shell "dumpsys | grep -iE 'qnx|mcu|cluster' -A 20"
```

**Что извлечь:**
- Сообщения об обмене данными
- Ошибки подключения
- Протоколы обмена
- Форматы данных

---

### 9. QNX РАЗДЕЛЫ

**Цель:** Изучить QNX разделы (если доступны)

**Действия:**

```bash
# Список разделов
adb shell "ls -la /dev/block/by-name/qnx_*"

# Информация о разделах
adb shell "blkid | grep qnx"

# Попытка монтирования (требует root)
adb shell "mount | grep qnx"
```

**Что извлечь:**
- Размеры разделов
- Типы файловых систем
- Структура данных (если доступно)

---

### 10. BROADCAST И INTENT АНАЛИЗ

**Цель:** Найти broadcast'ы и intent'ы для отправки данных в QNX

**Действия:**

```bash
# Мониторинг broadcast'ов
adb shell "logcat | grep -iE 'broadcast|intent' | grep -iE 'qnx|mcu|cluster|navigation'"

# Dumpsys для receivers
adb shell "dumpsys package | grep -iE 'receiver.*qnx|receiver.*mcu|receiver.*cluster' -A 5"

# Поиск в APK манифестах
# (после извлечения APK)
```

**Что извлечь:**
- Action names для broadcast'ов
- Intent filters
- Параметры данных
- Формат extra данных

---

### 11. СЕТЕВОЙ ТРАФИК АНАЛИЗ

**Цель:** Захватить и проанализировать сетевой трафик

**Действия:**

```bash
# tcpdump (если доступен)
adb shell "tcpdump -i eth0 -w /sdcard/qnx_traffic.pcap host 192.168.2.1"
adb pull /sdcard/qnx_traffic.pcap

# netstat мониторинг
adb shell "watch -n 1 'netstat -an | grep 192.168.2.1'"
```

**Что извлечь:**
- Пакеты данных
- Протоколы обмена
- Структура сообщений
- Частота обновлений

---

### 12. ФАЙЛЫ ДАННЫХ И КЕШИ

**Цель:** Найти файлы с данными навигации или конфигурацией

**Действия:**

```bash
# Поиск файлов
adb shell "find /data /cache /sdcard -name '*qnx*' -o -name '*navigation*' -o -name '*cluster*' 2>/dev/null"

# Проверка shared preferences
adb shell "run-as com.desaysv.* cat /data/data/com.desaysv.*/shared_prefs/*.xml 2>/dev/null"
```

**Что извлечь:**
- Конфигурационные файлы
- Кеши данных
- Логи приложений
- Базы данных

---

## 🎯 ПРИОРИТЕТНЫЕ ЗАДАЧИ

### Высокий приоритет:

1. ✅ **Сетевые протоколы** - понять, как происходит обмен
2. ✅ **Broadcast/Intent анализ** - найти способ отправки данных
3. ✅ **APK анализ** - изучить системные приложения
4. ✅ **Системные сервисы** - найти сервисы коммуникации

### Средний приоритет:

5. ⚠️ **Shared Memory** - изучить структуру данных
6. ⚠️ **Конфигурационные файлы** - параметры системы
7. ⚠️ **Логи** - диагностика работы

### Низкий приоритет:

8. ⚠️ **QNX разделы** - если потребуется глубокий анализ
9. ⚠️ **Бинарники** - reverse engineering (если нужно)

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Что мы должны получить:

1. **Протокол обмена данными:**
   - TCP/UDP порты
   - Формат сообщений
   - Структура пакетов

2. **API для отправки данных:**
   - Broadcast actions
   - Intent filters
   - Service interfaces
   - Методы отправки

3. **Структура данных навигации:**
   - Формат данных для приборной панели
   - Поля данных
   - Типы значений

4. **Конфигурация:**
   - Параметры подключения
   - Настройки протоколов
   - IP адреса и порты

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Начать с сетевого анализа** - подключиться к портам и изучить протокол
2. **Анализ APK** - извлечь и декомпилировать системные приложения
3. **Мониторинг broadcast'ов** - найти способ отправки данных
4. **Документирование** - записать все найденные интерфейсы и протоколы

---

## 📝 ЗАМЕТКИ

- Все команды выполняются через ADB (уже подключен)
- Port forwarding установлен для всех портов QNX
- Engineering mode активирован
- QNX Serial/UART включены

---

**Статус:** План готов к выполнению

