# План установки модифицированного образа - G6SH/T18FL3

**Дата:** 2025-11-27
**Статус:** ✅ Загрузчик разблокирован! Готов к установке

---

## ✅ КРИТИЧЕСКАЯ НАХОДКА

```bash
sys.user_unlocked=1                # ✅✅✅ ПОЛЬЗОВАТЕЛЬ РАЗБЛОКИРОВАН!
ro.boot.veritymode=eio              # ✅✅✅ Verity игнорирует ошибки!
ro.build.ab_update=true             # ✅ A/B обновления поддерживаются
```

**Это означает:**
- ✅ **Можно устанавливать модифицированные образы!**
- ✅ Проверка подписи может быть обойдена (EIO режим)
- ✅ Есть A/B разделы для безопасной установки

---

## 🎯 РЕКОМЕНДУЕМЫЕ МЕТОДЫ УСТАНОВКИ

### Метод 1: Через приложение обновления (⭐ РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Безопаснее (меньше риск "кирпича")
- ✅ Поддержка USB обновлений
- ✅ Автоматическая проверка и установка
- ✅ Поддержка A/B разделов

**Шаги:**

1. **Извлечь и декомпилировать APK:**
   ```bash
   adb pull /product/app/SVUpdateApp/SVUpdateApp.apk
   adb pull /product/app/SVLocalUpdateApp/SVLocalUpdateApp.apk

   jadx -d decompiled_update SVUpdateApp.apk
   jadx -d decompiled_upgrade SVLocalUpdateApp.apk
   ```

2. **Найти формат обновления:**
   - Изучить код установки
   - Найти проверку подписи
   - Найти способ обхода

3. **Подготовить образ:**
   - Создать update.zip в нужном формате
   - Возможно, нужно подписать своим ключом (или обойти проверку)

4. **Установить:**
   ```bash
   # Через USB
   adb push modified_update.zip /sdcard/
   adb shell am start -n com.desaysv.update/.MainActivity

   # Или локально
   adb push modified_update.zip /data/update/
   adb shell am start -n com.desaysv.upgrade/.MainActivity
   ```

---

### Метод 2: Через Fastboot (если доступен)

**Проверка доступности:**
```bash
# Перезагрузка в bootloader
adb reboot bootloader

# Проверка подключения
fastboot devices

# Проверка статуса
fastboot getvar all
fastboot oem device-info
```

**Установка:**
```bash
# Установка в slot A (безопасно, slot B останется рабочим)
fastboot flash boot_a modified_boot.img
fastboot flash system_a modified_system.img
fastboot flash vendor_a modified_vendor.img

# Переключение на slot A
fastboot set_active a

# Перезагрузка
fastboot reboot
```

**Откат (если не работает):**
```bash
fastboot set_active b
fastboot reboot
```

---

### Метод 3: Прямая запись через DD (требует root)

**⚠️ ОПАСНО! Высокий риск "кирпича"!**

**Требования:**
- Root доступ
- Полный бэкап разделов

**Шаги:**
```bash
# 1. Бэкап оригинальных разделов
adb shell su -c "dd if=/dev/block/by-name/boot_b of=/sdcard/boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/system_b of=/sdcard/system_b_backup.img"

# 2. Запись модифицированных образов
adb push modified_boot.img /sdcard/
adb push modified_system.img /sdcard/

adb shell su -c "dd if=/sdcard/modified_boot.img of=/dev/block/by-name/boot_b"
adb shell su -c "dd if=/sdcard/modified_system.img of=/dev/block/by-name/system_b"

# 3. Перезагрузка
adb reboot
```

---

### Метод 4: Через инженерное меню

**Проверка функций:**
- Декомпилировать `SVEngMode.apk`
- Найти скрытые функции установки
- Активировать режим установки

**Активация:**
```bash
adb shell am start -n com.android.engmode/.MainActivity
# Изучить меню на устройстве
```

---

## 📋 СТРУКТУРА РАЗДЕЛОВ

**A/B разделы:**
```
boot_a / boot_b          - Boot разделы
system_a / system_b      - System разделы
vendor_a / vendor_b      - Vendor разделы
qnx_boot_a / qnx_boot_b  - QNX Boot разделы
qnx_system_a / qnx_system_b - QNX System разделы
```

**Текущий активный slot:** `_b`

**Стратегия:**
1. Установить модифицированный образ в slot A
2. Переключиться на slot A
3. Если не работает - вернуться на slot B

---

## 🔧 ОБХОД ПРОВЕРКИ ПОДПИСИ

### Использование EIO режима

**Текущий статус:**
```bash
ro.boot.veritymode=eio              # ✅ Errors Ignored
```

**Это означает:**
- Ошибки верификации игнорируются
- Можно устанавливать неподписанные образы
- Но нужно отключить `persist.sys.flash_write_protect`

**Отключение защиты записи:**
```bash
# Попытка отключения (может не работать без root)
adb shell setprop persist.sys.flash_write_protect false
```

### Модификация boot.img

**Шаги:**
1. Извлечь boot.img
2. Распаковать
3. Модифицировать cmdline:
   - Удалить: `androidboot.veritymode=eio`
   - Добавить: `androidboot.veritymode=disabled`
4. Пересобрать
5. Установить

---

## ⚠️ ЗАЩИТЫ И ОГРАНИЧЕНИЯ

**Найденные защиты:**
```bash
persist.sys.flash_write_protect=true  # ⚠️  Защита от записи
ro.adb.secure=1                       # ⚠️  ADB secure
ro.secure=1                           # ⚠️  Secure режим
```

**Обход:**
- Использовать приложение обновления (может обходить защиту)
- Использовать инженерное меню
- Получить root доступ

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Этап 1: Подготовка (✅ Выполнено)
- [x] Проверка статуса загрузчика
- [x] Нахождение приложений обновления
- [x] Анализ разделов

### Этап 2: Анализ приложений обновления
- [ ] Извлечь APK приложений обновления
- [ ] Декомпилировать APK
- [ ] Найти формат обновления
- [ ] Найти проверку подписи
- [ ] Найти способ обхода

### Этап 3: Подготовка образа
- [ ] Создать модифицированный образ
- [ ] Подготовить в нужном формате
- [ ] Протестировать на эмуляторе (если возможно)

### Этап 4: Установка
- [ ] Выбрать метод установки
- [ ] Создать бэкап оригинальных разделов
- [ ] Установить модифицированный образ
- [ ] Проверить работоспособность

### Этап 5: Откат (если нужно)
- [ ] Вернуться на оригинальный образ
- [ ] Восстановить из бэкапа

---

## 📝 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

### Проверка статуса:
```bash
adb shell getprop sys.user_unlocked
adb shell getprop ro.boot.veritymode
adb shell getprop ro.build.ab_update
```

### Извлечение APK обновления:
```bash
adb pull /product/app/SVUpdateApp/SVUpdateApp.apk
adb pull /product/app/SVLocalUpdateApp/SVLocalUpdateApp.apk
```

### Запуск приложения обновления:
```bash
adb shell am start -n com.desaysv.update/.MainActivity
adb shell am start -n com.desaysv.upgrade/.MainActivity
```

### Бэкап разделов:
```bash
adb shell su -c "dd if=/dev/block/by-name/boot_b of=/sdcard/boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/system_b of=/sdcard/system_b_backup.img"
```

---

## 🎉 ВЫВОД

**✅ ХОРОШИЕ НОВОСТИ:**
- Загрузчик **УЖЕ РАЗБЛОКИРОВАН** (`sys.user_unlocked=1`)
- Verity в режиме **EIO** (игнорирует ошибки)
- Есть **A/B разделы** для безопасной установки
- Есть **приложения обновления** с поддержкой USB

**⚠️ ВНИМАНИЕ:**
- Защита от записи включена (`persist.sys.flash_write_protect=true`)
- Нужно найти способ обхода проверки подписи
- Требуется анализ приложений обновления

**🎯 РЕКОМЕНДАЦИЯ:**
Использовать **Метод 1** (через приложение обновления) - самый безопасный способ.

---

**Следующий шаг:** Декомпилировать приложения обновления и найти способ установки модифицированного образа.

