# План установки полной системы (QNX + Android) с оригинальными подписями - G6SH/T18FL3

**Дата:** 2025-11-27
**Цель:** Установить полную модифицированную систему (QNX + Android) с оригинальными подписями производителя

---

## 🎯 ЗАДАЧА

Установить модифицированные образы:
- ✅ QNX Boot (`qnx_boot_b`)
- ✅ QNX System (`qnx_system_b`)
- ✅ Android Boot (`boot_b`)
- ✅ Android System (`system_b`)
- ✅ Android Vendor (`vendor_b`)

**С оригинальными подписями производителя!**

---

## ✅ ПРЕИМУЩЕСТВА

### Текущий статус системы:

```bash
sys.user_unlocked=1                # ✅ Загрузчик разблокирован
ro.boot.veritymode=eio              # ✅ Verity игнорирует ошибки
ro.build.tags=dev-keys              # ✅ Dev keys
ro.build.ab_update=true             # ✅ A/B обновления
```

**Это означает:**
- ✅ Можно устанавливать модифицированные образы
- ✅ Проверка подписи может быть обойдена
- ✅ Есть A/B разделы для безопасной установки

---

## 🔧 СПОСОБЫ УСТАНОВКИ

### Метод 1: Через приложение обновления (⭐ РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Поддержка полных обновлений (QNX + Android)
- ✅ Автоматическая проверка и установка
- ✅ Поддержка оригинальных подписей
- ✅ Безопасная установка через A/B разделы

**Формат обновления:**
- Вероятно, ZIP архив с образами
- Содержит: QNX образы, Android образы, метаданные
- Подписан оригинальным ключом производителя

**Шаги:**

1. **Подготовить образы:**
   ```bash
   # Создать структуру обновления
   mkdir -p update_package/
   cp modified_qnx_boot.img update_package/qnx_boot_b.img
   cp modified_qnx_system.img update_package/qnx_system_b.img
   cp modified_boot.img update_package/boot_b.img
   cp modified_system.img update_package/system_b.img
   cp modified_vendor.img update_package/vendor_b.img
   ```

2. **Создать update.zip:**
   ```bash
   # Создать ZIP архив
   cd update_package/
   zip -r ../update.zip .

   # Подписать оригинальным ключом (если есть)
   # Или использовать способ обхода проверки
   ```

3. **Установить:**
   ```bash
   # Через USB
   adb push update.zip /sdcard/
   adb shell am start -n com.desaysv.update/.MainActivity

   # Или локально
   adb push update.zip /data/update/
   adb shell am start -n com.desaysv.upgrade/.MainActivity
   ```

---

### Метод 2: Через Fastboot (если доступен)

**Проверка:**
```bash
adb reboot bootloader
fastboot devices
fastboot getvar all
```

**Установка:**
```bash
# QNX образы
fastboot flash qnx_boot_b modified_qnx_boot.img
fastboot flash qnx_system_b modified_qnx_system.img

# Android образы
fastboot flash boot_b modified_boot.img
fastboot flash system_b modified_system.img
fastboot flash vendor_b modified_vendor.img

# Перезагрузка
fastboot reboot
```

---

### Метод 3: Прямая запись через DD (требует root)

**⚠️ ОПАСНО! Высокий риск "кирпича"!**

**Требования:**
- Root доступ
- Отключенная защита от записи
- Полный бэкап всех разделов

**Шаги:**
```bash
# 1. Бэкап всех разделов
adb shell su -c "dd if=/dev/block/by-name/qnx_boot_b of=/sdcard/qnx_boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/qnx_system_b of=/sdcard/qnx_system_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/boot_b of=/sdcard/boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/system_b of=/sdcard/system_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/vendor_b of=/sdcard/vendor_b_backup.img"

# 2. Запись модифицированных образов
adb push modified_qnx_boot.img /sdcard/
adb push modified_qnx_system.img /sdcard/
adb push modified_boot.img /sdcard/
adb push modified_system.img /sdcard/
adb push modified_vendor.img /sdcard/

adb shell su -c "dd if=/sdcard/modified_qnx_boot.img of=/dev/block/by-name/qnx_boot_b"
adb shell su -c "dd if=/sdcard/modified_qnx_system.img of=/dev/block/by-name/qnx_system_b"
adb shell su -c "dd if=/sdcard/modified_boot.img of=/dev/block/by-name/boot_b"
adb shell su -c "dd if=/sdcard/modified_system.img of=/dev/block/by-name/system_b"
adb shell su -c "dd if=/sdcard/modified_vendor.img of=/dev/block/by-name/vendor_b"

# 3. Перезагрузка
adb reboot
```

---

## 🔐 РАБОТА С ОРИГИНАЛЬНЫМИ ПОДПИСЯМИ

### Вариант 1: Использовать оригинальные подписи

**Если есть доступ к оригинальным ключам:**
- Подписать модифицированные образы оригинальными ключами
- Система примет образы как оригинальные

**Проблема:**
- Обычно нет доступа к оригинальным ключам производителя
- Ключи хранятся в секрете

### Вариант 2: Обход проверки подписи

**Использовать EIO режим:**
```bash
ro.boot.veritymode=eio              # ✅ Errors Ignored
```

**Это означает:**
- Ошибки верификации игнорируются
- Можно установить неподписанные образы
- Но нужно отключить `persist.sys.flash_write_protect`

### Вариант 3: Модификация проверки подписи

**В приложениях обновления:**
- Найти код проверки подписи
- Обойти проверку
- Или использовать режим разработчика

---

## 📋 СТРУКТУРА РАЗДЕЛОВ

### QNX разделы:
```
/dev/block/vda1  - qnx_boot_a
/dev/block/vda2  - qnx_boot_b      ← УСТАНОВКА СЮДА
/dev/block/vda3  - qnx_system_a
/dev/block/vda4  - qnx_system_b    ← УСТАНОВКА СЮДА
```

### Android разделы:
```
/dev/block/vda19 - boot_a
/dev/block/vda20 - boot_b           ← УСТАНОВКА СЮДА
/dev/block/vda24 - system_a
/dev/block/vda25 - system_b         ← УСТАНОВКА СЮДА
/dev/block/vda26 - vendor_a
/dev/block/vda27 - vendor_b         ← УСТАНОВКА СЮДА
```

**Текущий активный slot:** `_b`

**Стратегия:**
1. Установить в slot A (безопасно)
2. Переключиться на slot A
3. Если не работает - вернуться на slot B

---

## 🎯 ПЛАН ДЕЙСТВИЙ

### Этап 1: Подготовка (⏳ В процессе)

- [x] Проверка статуса загрузчика
- [x] Анализ разделов
- [x] Извлечение приложений обновления
- [ ] Декомпиляция приложений обновления
- [ ] Нахождение формата обновления

### Этап 2: Отключение защиты

- [ ] Найти источник `persist.sys.flash_write_protect`
- [ ] Отключить защиту от записи
- [ ] Проверить возможность записи

### Этап 3: Подготовка образов

- [ ] Создать модифицированные образы
- [ ] Подготовить в нужном формате
- [ ] Подписать (или обойти проверку)

### Этап 4: Установка

- [ ] Создать бэкап всех разделов
- [ ] Установить через выбранный метод
- [ ] Проверить работоспособность

### Этап 5: Откат (если нужно)

- [ ] Вернуться на оригинальные образы
- [ ] Восстановить из бэкапа

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### QNX контроль:

1. **QNX загружается первым:**
   - QNX контролирует запуск Android
   - Нужно установить QNX образы правильно

2. **QNX может проверять Android:**
   - QNX может проверять подписи Android
   - Нужно найти способ обхода

3. **Синхронизация slot:**
   - QNX и Android должны использовать один slot
   - Нужно установить в оба slot одновременно

### Защита от записи:

1. **✅ ЗАЩИТА ОТКЛЮЧЕНА:**
   ```bash
   persist.sys.flash_write_protect=false  # ✅ ОТКЛЮЧЕНО!
   ```
   - ✅ Защита успешно отключена через `setprop`
   - ✅ Можно записывать в системные разделы
   - ⚠️ Может быть перезаписано при перезагрузке (но работает сейчас)

2. **Обход через приложения:**
   - Приложения обновления могут обходить защиту
   - Использовать их для установки
   - Или использовать прямо сейчас (защита отключена)

---

## 📝 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

### Проверка статуса:
```bash
adb shell getprop sys.user_unlocked
adb shell getprop ro.boot.veritymode
adb shell getprop persist.sys.flash_write_protect
```

### Бэкап разделов:
```bash
# QNX
adb shell su -c "dd if=/dev/block/by-name/qnx_boot_b of=/sdcard/qnx_boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/qnx_system_b of=/sdcard/qnx_system_b_backup.img"

# Android
adb shell su -c "dd if=/dev/block/by-name/boot_b of=/sdcard/boot_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/system_b of=/sdcard/system_b_backup.img"
adb shell su -c "dd if=/dev/block/by-name/vendor_b of=/sdcard/vendor_b_backup.img"
```

### Извлечение приложений:
```bash
adb pull /product/app/SVUpdateApp/SVUpdateApp.apk
adb pull /product/app/SVLocalUpdateApp/SVLocalUpdateApp.apk
```

---

## 🎉 ВЫВОД

**✅ ВОЗМОЖНОСТИ:**
- Загрузчик разблокирован
- Verity в режиме EIO
- Есть A/B разделы
- Есть приложения обновления

**⚠️ ТРЕБУЕТСЯ:**
- Отключить защиту от записи
- Найти формат обновления
- Подготовить образы правильно
- Обойти проверку подписи (или подписать оригинальными ключами)

**🎯 РЕКОМЕНДАЦИЯ:**
Использовать приложение обновления для установки полной системы с оригинальными подписями.

---

**Следующий шаг:** Декомпилировать приложения обновления и найти формат установки полной системы.

