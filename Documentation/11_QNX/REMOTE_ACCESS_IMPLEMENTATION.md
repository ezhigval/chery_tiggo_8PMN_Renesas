# Реализация удаленного доступа - G6SH/T18FL3

**Дата:** 2025-11-27
**Цель:** Создать автоматический удаленный доступ к системе

---

## 🎯 РЕШЕНИЕ: ADB over TCP/IP + Автоматизация

### Преимущества:
- ✅ Простое в реализации
- ✅ Использует существующий ADB
- ✅ Не требует дополнительных приложений
- ✅ Работает через WiFi или Ethernet

---

## 🛠️ РЕАЛИЗАЦИЯ

### Вариант 1: Скрипт автоматизации (быстро)

**Файл:** `remote_access_service.sh`

**Использование:**
```bash
./remote_access_service.sh
```

**Что делает:**
1. Включает ADB TCP/IP на порту 5555
2. Получает IP адрес устройства
3. Подключается по TCP/IP
4. Выводит информацию для подключения

**Автоматизация:**
- Запускать при каждой загрузке
- Или интегрировать в init.rc

---

### Вариант 2: Android Service (надежно)

**Файл:** `RemoteAccessService.java`

**Функции:**
- Автоматически включается при загрузке
- Мониторит изменения сети
- Автоматически включает ADB TCP/IP
- Получает и логирует IP адрес

**Установка:**
1. Скомпилировать в APK
2. Установить как системное приложение
3. Зарегистрировать в AndroidManifest.xml

**AndroidManifest.xml:**
```xml
<service
    android:name=".RemoteAccessService"
    android:enabled="true"
    android:exported="false" />

<receiver android:name=".BootReceiver">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>
```

---

### Вариант 3: Init Script (системный уровень)

**Файл:** `auto_remote_access.sh`

**Размещение:**
- `/system/etc/init.d/auto_remote_access.sh`
- Или в `/vendor/etc/init/remote_access.rc`

**Интеграция в init.rc:**
```bash
# В /vendor/etc/init/remote_access.rc
service remote_access /system/bin/auto_remote_access.sh
    class late_start
    user root
    oneshot
    disabled

on property:sys.boot_completed=1
    start remote_access
```

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Проверка сети

```bash
# Проверить WiFi
adb shell settings get global wifi_on

# Проверить IP адрес
adb shell ip addr show wlan0
# Или
adb shell getprop dhcp.wlan0.ipaddress
```

### Шаг 2: Включение ADB TCP/IP

```bash
# Включить
adb shell setprop service.adb.tcp.port 5555
adb tcpip 5555

# Проверить
adb shell getprop service.adb.tcp.port
adb shell netstat -an | grep 5555
```

### Шаг 3: Подключение

```bash
# Узнать IP
IP=$(adb shell "getprop dhcp.wlan0.ipaddress" | tr -d '\r\n')

# Подключиться
adb connect $IP:5555

# Проверить
adb devices
```

### Шаг 4: Автоматизация

**Вариант A: Скрипт при загрузке**
```bash
# Добавить в init.rc
on property:sys.boot_completed=1
    exec_background /system/bin/remote_access_service.sh
```

**Вариант B: Android Service**
- Установить APK с RemoteAccessService
- Сервис автоматически запустится при загрузке

---

## 🔧 РАСШИРЕННЫЕ ВОЗМОЖНОСТИ

### 1. Reverse SSH Tunnel

**Если есть SSH сервер:**
```bash
# На устройстве (автоматически)
ssh -R 2222:localhost:22 user@your-server.com -N -f

# С ноутбука
ssh -p 2222 user@your-server.com
```

### 2. VPN Tunnel

**Если есть VPN клиент:**
- Настроить VPN сервер
- Подключить устройство к VPN
- Использовать VPN IP для ADB

### 3. Dynamic DNS

**Для стабильного доступа:**
- Настроить Dynamic DNS
- Использовать доменное имя вместо IP
- Автоматически обновлять IP

### 4. WebSocket Proxy

**Для доступа через браузер:**
- Создать WebSocket сервер
- Проксировать ADB команды
- Веб-интерфейс для управления

---

## ⚠️ БЕЗОПАСНОСТЬ

### Рекомендации:

1. **Ограничить доступ:**
   - Использовать firewall
   - Ограничить по IP адресу
   - Использовать VPN

2. **Шифрование:**
   - Использовать SSH туннель
   - Использовать VPN
   - Не использовать открытый ADB в интернете

3. **Аутентификация:**
   - Настроить ADB ключи
   - Использовать пароли
   - Регулярно менять ключи

---

## 📝 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

### Включение удаленного доступа:
```bash
cd Knowledge_base/11_QNX
./remote_access_service.sh
```

### Проверка статуса:
```bash
adb shell getprop service.adb.tcp.port
adb shell netstat -an | grep 5555
```

### Подключение:
```bash
# Узнать IP
IP=$(adb shell "getprop dhcp.wlan0.ipaddress" | tr -d '\r\n')
echo "IP: $IP"

# Подключиться
adb connect $IP:5555
```

---

## 🎯 РЕКОМЕНДАЦИИ

### Для локального доступа (одна сеть):
- ✅ ADB over TCP/IP через WiFi
- ✅ Простое и быстрое
- ✅ Не требует дополнительных сервисов

### Для удаленного доступа (интернет):
- ✅ Reverse SSH Tunnel
- ✅ VPN Tunnel
- ✅ Custom Remote Access Service

### Для автоматизации:
- ✅ Android Service (автозапуск)
- ✅ Init Script (системный уровень)
- ✅ Комбинированный подход

---

**Статус:** Решения готовы. Требуется тестирование и выбор оптимального варианта.

