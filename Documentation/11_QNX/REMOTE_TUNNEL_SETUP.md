# Настройка Reverse Tunnel: ГУ → Mac через интернет

**Дата:** 2025-11-27
**Цель:** Автоматическое подключение ГУ к Mac через интернет для удаленного доступа

---

## 🎯 ЗАДАЧА

1. **ГУ при включении** автоматически подключается к Mac через интернет
2. **На Mac** сервер принимает соединения и предоставляет доступ к ADB/QNX
3. **Удаленный доступ** к ГУ из любой точки мира

---

## 🏗️ АРХИТЕКТУРА

```
┌─────────────┐         Интернет          ┌─────────────┐
│     ГУ      │ ────────────────────────> │     Mac     │
│  (Клиент)   │    Reverse Connection    │  (Сервер)   │
└─────────────┘                           └─────────────┘
     │                                           │
     │                                           │
     └─── ADB/QNX ───────────────────────────────┘
```

**Принцип работы:**
1. ГУ (клиент) подключается к Mac (сервер) через интернет
2. Mac принимает соединение и устанавливает туннель
3. Через туннель передаются ADB/QNX команды

---

## 🖥️ НАСТРОЙКА MAC (СЕРВЕР)

### Шаг 1: Установка зависимостей

```bash
# Python3 (обычно уже установлен на Mac)
python3 --version

# ADB (если еще не установлен)
brew install android-platform-tools
```

### Шаг 2: Настройка файрвола

```bash
# Разрешить входящие соединения на порт 22222
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/bin/python3
```

### Шаг 3: Получение публичного IP или настройка Dynamic DNS

**Вариант A: Публичный IP (если есть)**
```bash
# Узнать текущий IP
curl ifconfig.me
```

**Вариант B: Dynamic DNS (рекомендуется)**
- Зарегистрироваться на noip.com, duckdns.org или подобном сервисе
- Настроить доменное имя (например: `your-mac.ddns.net`)
- Обновлять IP автоматически

**Вариант C: VPN (самый безопасный)**
- Настроить VPN сервер на Mac
- ГУ подключается к VPN
- Использовать VPN IP для соединения

### Шаг 4: Настройка роутера (если Mac за NAT)

```bash
# Пробросить порт 22222 на Mac
# В настройках роутера:
# Port Forwarding: 22222 -> Mac IP:22222
```

### Шаг 5: Запуск сервера

```bash
cd Knowledge_base/11_QNX/mac_server
./start_tunnel_server.sh 22222
```

**Или вручную:**
```bash
python3 reverse_tunnel_server.py 22222
```

**Для автозапуска при загрузке Mac:**
```bash
# Создать LaunchAgent
cat > ~/Library/LaunchAgents/com.tiggo.tunnel.server.plist << EOF
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.tiggo.tunnel.server</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX/mac_server/reverse_tunnel_server.py</string>
        <string>22222</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
EOF

# Загрузить
launchctl load ~/Library/LaunchAgents/com.tiggo.tunnel.server.plist
```

---

## 📱 НАСТРОЙКА ГУ (КЛИЕНТ)

### Шаг 1: Подготовка Android Service

1. **Изменить настройки сервера в `ReverseTunnelClient.java`:**
   ```java
   private static final String SERVER_HOST = "your-mac-ip-or-domain.com"; // Заменить!
   private static final int SERVER_PORT = 22222;
   ```

2. **Скомпилировать APK:**
   ```bash
   # Использовать Android Studio или командную строку
   # Создать проект, добавить ReverseTunnelClient.java
   # Собрать APK
   ```

3. **Установить на ГУ:**
   ```bash
   adb install ReverseTunnelClient.apk
   ```

### Шаг 2: Настройка AndroidManifest.xml

```xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desaysv.remotetunnel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <service
            android:name=".ReverseTunnelClient"
            android:enabled="true"
            android:exported="false" />

        <receiver android:name=".BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
```

### Шаг 3: Проверка интернет-соединения

```bash
# Проверить, что ГУ имеет доступ в интернет
adb shell ping -c 3 8.8.8.8

# Проверить DNS
adb shell ping -c 3 google.com
```

---

## 🔧 АЛЬТЕРНАТИВНОЕ РЕШЕНИЕ: SSH Reverse Tunnel

Если не хотите создавать свой протокол, можно использовать SSH:

### На Mac:

```bash
# Установить SSH сервер (обычно уже установлен)
sudo systemsetup -setremotelogin on

# Настроить автопринятие соединений (опционально)
# В ~/.ssh/authorized_keys добавить ключ от ГУ
```

### На ГУ:

**Создать SSH клиент сервис:**
```java
// В ReverseTunnelClient.java использовать SSH библиотеку
// Например, JSch или Apache MINA SSHD
```

**Или использовать скрипт:**
```bash
# /system/bin/reverse_ssh_tunnel.sh
#!/system/bin/sh

MAC_HOST="your-mac-ip-or-domain.com"
MAC_USER="your-username"
SSH_PORT=22
LOCAL_ADB_PORT=5555
REMOTE_ADB_PORT=5555

# Подключение с reverse port forwarding
ssh -R ${REMOTE_ADB_PORT}:localhost:${LOCAL_ADB_PORT} \
    -N -f \
    ${MAC_USER}@${MAC_HOST} -p ${SSH_PORT}
```

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Проверка сервера на Mac

```bash
# Запустить сервер
./start_tunnel_server.sh 22222

# Должно вывести:
# [2025-11-27 ...] Сервер запущен на 0.0.0.0:22222
# [2025-11-27 ...] Ожидание подключений от ГУ...
```

### 2. Проверка подключения ГУ

```bash
# На ГУ проверить логи
adb logcat | grep ReverseTunnelClient

# Должно показать:
# D/ReverseTunnelClient: Connecting to server ...
# D/ReverseTunnelClient: Server greeting: TUNNEL_READY
```

### 3. Проверка туннеля

```bash
# На Mac должно появиться:
# [2025-11-27 ...] Новое соединение от (IP, PORT)
# [2025-11-27 ...] Информация от клиента: DEVICE_INFO:...
```

---

## 📋 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

### На Mac:

```bash
# 1. Перейти в директорию
cd Knowledge_base/11_QNX/mac_server

# 2. Запустить сервер
./start_tunnel_server.sh 22222

# 3. Проверить, что слушает порт
lsof -i :22222
```

### На ГУ:

```bash
# 1. Установить APK
adb install ReverseTunnelClient.apk

# 2. Запустить сервис
adb shell am startservice com.desaysv.remotetunnel/.ReverseTunnelClient

# 3. Проверить логи
adb logcat | grep ReverseTunnelClient
```

---

## ⚠️ БЕЗОПАСНОСТЬ

### Рекомендации:

1. **Использовать VPN:**
   - Самый безопасный вариант
   - Шифрование трафика
   - Защита от атак

2. **Использовать SSH:**
   - Встроенное шифрование
   - Аутентификация по ключам
   - Проверенный протокол

3. **Ограничить доступ:**
   - Firewall на Mac
   - Whitelist IP адресов
   - Использовать нестандартный порт

4. **Аутентификация:**
   - Добавить пароль или ключ
   - Проверка подлинности клиента

---

## 🎯 РЕКОМЕНДАЦИИ

### Для быстрого старта:
- ✅ Использовать готовый Python сервер
- ✅ Установить Android Service на ГУ
- ✅ Настроить Dynamic DNS

### Для продакшена:
- ✅ Использовать VPN
- ✅ Добавить аутентификацию
- ✅ Настроить мониторинг
- ✅ Логирование всех операций

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ Настроить Mac сервер
2. ⏳ Скомпилировать и установить Android Service
3. ⏳ Настроить Dynamic DNS или VPN
4. ⏳ Протестировать подключение
5. ⏳ Настроить автозапуск

---

**Статус:** Решение готово. Требуется настройка и тестирование.

