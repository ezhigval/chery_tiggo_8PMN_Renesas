# Быстрый старт Remote Desktop

## 🚀 ЗАПУСК (2 команды)

### 1. Запустить сервер на Mac:

```bash
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX/mac_server
python3 remote_desktop_server.py 22222
```

**Или через скрипт:**
```bash
./start_remote_desktop.sh 22222
```

### 2. Установить клиент на ГУ:

```bash
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX
./install_remote_tunnel.sh
```

**Скрипт запросит IP Mac - введите: `192.168.0.117` (локальный) или `178.178.245.0` (публичный)**

---

## ✅ ПРОВЕРКА

### После установки:

1. **Проверить сервер:**
   ```bash
   lsof -i :22222
   # Должен быть процесс python3
   ```

2. **Проверить клиент:**
   ```bash
   adb shell 'ps | grep remote_tunnel'
   adb shell 'tail -20 /data/local/tmp/tunnel_client.log'
   ```

3. **В GUI должно появиться:**
   - Окно с кнопками
   - После подключения ГУ - нажать "Включить экран"

---

## 🔌 ОТКЛЮЧЕНИЕ USB

**Можно отключать USB когда:**

1. ✅ Сервер запущен (`lsof -i :22222` показывает процесс)
2. ✅ В логах клиента есть "Подключено к серверу"
3. ✅ В GUI виден экран ГУ (если включен)
4. ✅ ГУ подключен к WiFi/интернету

**После отключения:**
- ГУ автоматически переподключится через WiFi
- Экран продолжит обновляться
- Touch события будут работать

---

## 📋 ЛОГИ

**Сервер (Mac):**
- Вывод в консоль
- Или: `./start_remote_desktop.sh 22222 > server.log 2>&1`

**Клиент (ГУ):**
```bash
adb shell 'tail -f /data/local/tmp/tunnel_client.log'
```

---

## ⚠️ ЕСЛИ НЕ РАБОТАЕТ

1. **Проверить firewall:**
   ```bash
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
   ```

2. **Проверить IP:**
   - Локальный: `192.168.0.117`
   - Публичный: `178.178.245.0` (может измениться)

3. **Проверить порт:**
   ```bash
   lsof -i :22222
   ```

4. **Перезапустить клиент:**
   ```bash
   adb shell 'killall sh'
   adb shell '/data/local/tmp/remote_tunnel_client.sh &'
   ```

---

**Готово! Запускайте и тестируйте!**
