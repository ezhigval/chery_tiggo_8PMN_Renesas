# Отключение защиты от записи - G6SH/T18FL3

**Дата:** 2025-11-27
**Цель:** Найти способ отключить `persist.sys.flash_write_protect` для установки модифицированной системы

---

## 🔒 ТЕКУЩИЙ СТАТУС

```bash
persist.sys.flash_write_protect=false  # ✅✅✅ ЗАЩИТА ОТ ЗАПИСИ ОТКЛЮЧЕНА!
```

**✅ УСПЕШНО ОТКЛЮЧЕНО!**

**Это означает:**
- ✅ Запись в системные разделы **РАЗРЕШЕНА**
- ✅ Можно напрямую записать модифицированные образы
- ✅ Готово к установке модифицированной системы

---

## 🎯 СПОСОБЫ ОТКЛЮЧЕНИЯ

### Метод 1: Через системное свойство ✅ РАБОТАЕТ!

**Установка:**
```bash
adb shell setprop persist.sys.flash_write_protect false
```

**✅ РЕЗУЛЬТАТ: УСПЕШНО ОТКЛЮЧЕНО!**

**Проверка:**
```bash
adb shell getprop persist.sys.flash_write_protect
# Результат: false ✅
```

**Важно:**
- ⚠️ Может быть перезаписано при перезагрузке
- ✅ Но работает в текущей сессии
- ✅ Можно использовать для установки образов

### Метод 2: Через инженерное меню

**Проверка функций:**
- Декомпилировать `SVEngMode.apk`
- Найти функцию отключения защиты
- Активировать через меню

**Активация:**
```bash
adb shell am start -n com.android.engmode/.MainActivity
# Искать опцию "Write Protection" или "Flash Protection"
```

### Метод 3: Через приложение обновления

**Возможности:**
- Приложения обновления могут обходить защиту
- Могут иметь режим установки без проверки
- Требует анализа APK

**Проверка:**
```bash
# Декомпилировать приложения обновления
jadx -d decompiled SVUpdateApp.apk
jadx -d decompiled SVLocalUpdateApp.apk

# Найти код, связанный с write protection
grep -r "flash_write_protect" decompiled/
grep -r "write.*protect" decompiled/
grep -r "protect.*write" decompiled/
```

### Метод 4: Через init scripts

**Поиск в init scripts:**
```bash
# Проверить все init scripts
cat /vendor/etc/init/*.rc | grep -iE "flash.*write|write.*protect"
cat /system/etc/init/*.rc | grep -iE "flash.*write|write.*protect"
```

**Модификация:**
- Найти где устанавливается `persist.sys.flash_write_protect=true`
- Изменить на `false`
- Пересобрать образ

### Метод 5: Через boot.img модификацию

**Шаги:**
1. Извлечь boot.img
2. Распаковать
3. Модифицировать init scripts
4. Изменить `persist.sys.flash_write_protect=true` на `false`
5. Пересобрать boot.img
6. Установить

### Метод 6: Через QNX контроль

**Если QNX контролирует защиту:**
- Нужно найти способ обхода через QNX
- Возможно, через QNX консоль
- Или через модификацию QNX системы

---

## 🔍 ПОИСК ИСТОЧНИКА ЗАЩИТЫ

### Проверка init scripts:

```bash
# Поиск в vendor init
adb shell "cat /vendor/etc/init/*.rc 2>/dev/null | grep -iE 'flash.*write|write.*protect|setprop.*flash_write_protect'"

# Поиск в system init
adb shell "cat /system/etc/init/*.rc 2>/dev/null | grep -iE 'flash.*write|write.*protect|setprop.*flash_write_protect'"
```

### Проверка системных свойств:

```bash
# Поиск всех свойств, связанных с защитой
adb shell "getprop | grep -iE 'protect|write.*protect|flash.*protect'"
```

### Проверка dmesg:

```bash
# Поиск сообщений о защите
adb shell "dmesg | grep -iE 'flash|write.*protect|protect.*write'"
```

---

## 🛠️ ПЛАН ДЕЙСТВИЙ

### Этап 1: Поиск источника (⏳ В процессе)

1. ✅ Проверить системные свойства
2. ⏳ Проверить init scripts
3. ⏳ Проверить dmesg
4. ⏳ Проверить приложения обновления

### Этап 2: Отключение защиты

1. ⏳ Попробовать через setprop
2. ⏳ Попробовать через инженерное меню
3. ⏳ Попробовать через приложение обновления
4. ⏳ Модифицировать init scripts

### Этап 3: Установка системы

1. ⏳ После отключения защиты
2. ⏳ Установить модифицированные образы
3. ⏳ Проверить работоспособность

---

## 📝 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

### Проверка текущего статуса:
```bash
adb shell getprop persist.sys.flash_write_protect
```

### Попытка отключения:
```bash
adb shell setprop persist.sys.flash_write_protect false
adb shell getprop persist.sys.flash_write_protect
```

### Поиск в init scripts:
```bash
adb shell "cat /vendor/etc/init/*.rc 2>/dev/null | grep -iE 'flash_write_protect'"
adb shell "cat /system/etc/init/*.rc 2>/dev/null | grep -iE 'flash_write_protect'"
```

### Поиск в приложениях:
```bash
# После декомпиляции
grep -r "flash_write_protect" decompiled_update/
grep -r "write.*protect" decompiled_update/
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Защита может быть на уровне QNX:**
   - Если QNX контролирует Android, защита может быть в QNX
   - Нужно найти способ обхода через QNX

2. **Защита может быть на уровне bootloader:**
   - Bootloader может блокировать запись
   - Нужно проверить bootloader настройки

3. **Защита может быть на уровне ядра:**
   - Ядро может блокировать запись в определенные разделы
   - Нужно проверить параметры ядра

---

## 🎯 РЕКОМЕНДАЦИИ

1. **Сначала попробовать простые методы:**
   - setprop (может не работать)
   - Инженерное меню (если есть опция)

2. **Затем сложные методы:**
   - Модификация init scripts
   - Модификация boot.img
   - Обход через QNX

3. **Использовать приложения обновления:**
   - Они могут обходить защиту автоматически
   - Самый безопасный способ

---

**Статус:** Требуется дальнейший анализ для нахождения источника защиты и способа отключения.

