# Запуск Remote Desktop - Пошаговая инструкция

**Дата:** 2025-11-27

---

## 🚀 БЫСТРЫЙ СТАРТ

### Шаг 1: Запустить сервер на Mac

```bash
cd Knowledge_base/11_QNX/mac_server
./start_remote_desktop.sh 22222
```

**Должно появиться:**
- Окно GUI с кнопками
- Сообщение "Сервер запущен на 0.0.0.0:22222"
- "Ожидание подключений от ГУ..."

### Шаг 2: Установить клиент на ГУ

```bash
cd Knowledge_base/11_QNX
./install_remote_tunnel.sh
```

**Скрипт запросит:**
- IP адрес или домен Mac
- Порт сервера (по умолчанию 22222)

### Шаг 3: Проверить подключение

**На Mac:**
- Должно появиться сообщение "Новое соединение от ..."
- В GUI появится экран ГУ (после нажатия "Включить экран")

**На ГУ:**
```bash
# Проверить логи
adb shell 'tail -f /data/local/tmp/tunnel_client.log'
```

### Шаг 4: Отключить USB и проверить

**Когда все работает:**
1. Отключить USB кабель
2. ГУ должен автоматически переподключиться через WiFi/интернет
3. Экран должен продолжать обновляться

---

## 📋 ПРОВЕРКА РАБОТЫ

### Проверка сервера на Mac:

```bash
# Проверить, что сервер слушает порт
lsof -i :22222

# Должно показать процесс python3
```

### Проверка клиента на ГУ:

```bash
# Проверить процесс
adb shell 'ps | grep remote_tunnel'

# Проверить логи
adb shell 'cat /data/local/tmp/tunnel_client.log | tail -20'
```

### Проверка соединения:

```bash
# На Mac должно быть видно подключение
# В логах сервера должно быть:
# "Новое соединение от (IP, PORT)"
# "Информация от клиента: DEVICE_INFO:..."
```

---

## ⚠️ ВАЖНО

1. **Перед отключением USB:**
   - Убедитесь, что сервер на Mac запущен
   - Убедитесь, что ГУ подключен к интернету
   - Проверьте, что клиент работает (логи)

2. **Если не подключается:**
   - Проверьте firewall на Mac
   - Проверьте, что порт проброшен (если Mac за NAT)
   - Проверьте IP адрес Mac (может измениться)

3. **Для постоянного доступа:**
   - Настроить Dynamic DNS
   - Или использовать VPN
   - Или настроить статический IP

---

## 🔧 РУЧНОЙ ЗАПУСК

### Если автоматическая установка не работает:

```bash
# 1. Скопировать скрипт на ГУ
adb push Knowledge_base/11_QNX/remote_tunnel_client.sh /data/local/tmp/
adb shell chmod 755 /data/local/tmp/remote_tunnel_client.sh

# 2. Настроить IP Mac в скрипте
adb shell "sed -i 's|your-mac-ip-or-domain.com|YOUR_MAC_IP|g' /data/local/tmp/remote_tunnel_client.sh"

# 3. Запустить вручную
adb shell '/data/local/tmp/remote_tunnel_client.sh &'
```

---

## 📊 МОНИТОРИНГ

### Логи на Mac:
- Выводятся в консоль
- Можно перенаправить в файл:
  ```bash
  ./start_remote_desktop.sh 22222 > server.log 2>&1
  ```

### Логи на ГУ:
```bash
# Смотреть логи в реальном времени
adb shell 'tail -f /data/local/tmp/tunnel_client.log'

# Последние 50 строк
adb shell 'tail -50 /data/local/tmp/tunnel_client.log'
```

---

## ✅ ГОТОВНОСТЬ К ОТКЛЮЧЕНИЮ USB

**Можно отключать USB когда:**
1. ✅ Сервер на Mac запущен и слушает порт
2. ✅ Клиент на ГУ запущен и подключен
3. ✅ В GUI виден экран ГУ (если включен)
4. ✅ Touch события работают
5. ✅ ГУ подключен к интернету (WiFi или мобильная сеть)

**После отключения:**
- ГУ должен автоматически переподключиться
- Экран должен продолжать обновляться
- Touch события должны работать

---

**Готово к тестированию!**

