# Полный анализ подключения к QNX/MCU - G6SH/T18FL3

**Дата:** 2025-11-27
**Статус:** ✅ ADB подключен, QNX Serial активирован, но USB Serial порт на Mac не обнаружен

---

## ✅ РЕЗУЛЬТАТЫ АНАЛИЗА

### 1. ADB ПОДКЛЮЧЕНИЕ

**Статус:** ✅ **ПОДКЛЮЧЕНО**

```
Устройство: 00002260
Модель: G6SH_r8a7795
Продукт: g6sh_t18fl3_international
Транспорт: USB (usb:0-0)
```

**USB устройство на Mac:**

- **G6SH-r8a7795** (Renesas, idVendor: 6353, idProduct: 19985)
- Serial Number: 00002260
- Location ID: 1179648
- USB Speed: 2 (High Speed)
- **UsbExclusiveOwner:** `pid 17537, adb` ✅

---

### 2. USB SERIAL GADGET

**Статус:** ✅ **АКТИВИРОВАН**

```bash
sys.usb.config: mtp,adb,serial
```

**USB Gadget:**

- Путь: `/sys/devices/platform/soc/e6590000.usb/gadget/`
- UDC: не определён (возможно, не активен)

**Проблема:** Несмотря на активацию `serial` в USB конфигурации, на Mac не обнаружен USB Serial порт (нет `/dev/tty.*` или `/dev/cu.*` для QNX).

**Возможные причины:**

1. QNX порт не подключен физически
2. QNX порт требует специальных драйверов
3. QNX порт работает через другой интерфейс (не USB Serial)

---

### 3. QNX CONFIGURATION

**Статус:** ✅ **НАСТРОЕН**

**Properties:**

```bash
persist.qnx.serial.enable: 1          ✅
persist.sys.qnx: 1                    ✅
sys.qnx.uart.enable: 1                ✅
ro.boot.slot_suffix_qnx: _b           ✅
```

**QNX Shared Memory:**

- Путь: `/sys/devices/platform/vdevs/1c050000.qnx,guest_shm`
- Driver: `qnx-virtio-du` ✅
- Uevent: найден и читается ✅

**QNX Virtio Serial Driver:**

- Путь: `/sys/bus/virtio/drivers/virtio_rproc_serial/`
- Статус: найден ✅
- Устройства: не привязаны (пустая директория)

---

### 4. QNX PARTITIONS

**Статус:** ✅ **НАЙДЕНЫ**

```bash
/dev/block/by-name/qnx_boot_a
/dev/block/by-name/qnx_system_a
/dev/block/by-name/qnx_userdata
/dev/block/by-name/qnx_appcfg
/dev/block/by-name/qnx_appdata
/dev/block/by-name/qnx_system_b
/dev/block/by-name/qnx_boot_b
/dev/block/by-name/qnx_firmware
```

**Virtio Block Devices:**

- `/dev/block/platform/vdevs/1c0d0000.virtio_blk` (QNX разделы)
- `/dev/block/platform/vdevs/1c0e0000.virtio_blk`
- `/dev/block/platform/vdevs/1c0f0000.virtio_blk`

---

### 5. TTY/UART УСТРОЙСТВА НА ANDROID

**Статус:** ✅ **НАЙДЕНЫ**

```bash
/dev/ttyAMA0  (crw------- root root 204, 64)  # UART для QNX?
/dev/ttySC1   (crw-rw---- gps system 204, 9)   # GPS?
/dev/ttySC6   (crw-rw---- bluetooth bluetooth 204, 14)  # Bluetooth
```

**Проблема:** Нет доступа к `/dev/ttyAMA0` (Permission denied для shell пользователя).

---

### 6. СЕТЕВЫЕ ПОДКЛЮЧЕНИЯ

**Статус:** ⚠️ **ЧАСТИЧНО ДОСТУПНО**

**IP адреса:**

- Android: `192.168.2.2/24` (eth0) ✅
- QNX/MCU: `192.168.2.1` (предположительно) ❌ (не отвечает на ping)

**Открытые порты:**

- `10005` (TCP, LISTEN) - возможно, QNX сервис ✅
- `2121` (TCP6, LISTEN) - FTP?
- `31030, 31040, 31050` (ESTABLISHED к 192.168.2.1) - соединения с QNX/MCU ✅

**Попытки подключения:**

- `192.168.2.1:10004` - Operation timed out ❌
- `192.168.2.1:10005` - Operation timed out ❌
- `192.168.2.2:10005` - недоступен снаружи ❌

---

### 7. ENGINEERING MODE

**Статус:** ✅ **АКТИВИРОВАН**

```bash
am broadcast -a com.desaysv.engineering.START
# Результат: Broadcast completed: result=0 ✅
```

---

## 🔍 ДИАГНОСТИКА QNX ПОРТА

### Проблема: QNX USB Serial порт не обнаружен на Mac

**Проверено:**

1. ✅ USB Serial активирован в Android (`sys.usb.config: mtp,adb,serial`)
2. ✅ QNX Serial включен (`persist.qnx.serial.enable: 1`)
3. ✅ QNX UART включен (`sys.qnx.uart.enable: 1`)
4. ❌ USB Serial порт не появился на Mac (`/dev/tty.*` или `/dev/cu.*`)

**Возможные причины:**

1. **Физическое подключение:** QNX порт не подключен к Mac
2. **Драйверы:** Требуются драйверы для USB-to-Serial адаптера (CP2102, FTDI, CH340, PL2303)
3. **Режим работы:** QNX порт может работать не как USB Serial, а через другой интерфейс
4. **Активация:** QNX порт может требовать дополнительной активации через ADB

---

## 📋 РЕКОМЕНДАЦИИ

### Немедленные действия:

1. **Проверьте физическое подключение:**

   - Убедитесь, что второй mini-USB кабель подключен к порту "QNX" на устройстве
   - Убедитесь, что кабель подключен к Mac

2. **Проверьте драйверы USB-to-Serial:**

   ```bash
   # Проверьте, установлены ли драйверы
   system_profiler SPUSBDataType | grep -i "serial\|cp210\|ftdi\|ch340\|pl2303"

   # Если нет - установите драйверы:
   # - CP210x: https://www.silabs.com/developers/usb-to-uart-bridge-vcp-drivers
   # - FTDI: https://ftdichip.com/drivers/vcp-drivers/
   # - CH340: https://github.com/WCHSoftGroup/ch34xser_macos
   ```

3. **Попробуйте переподключить USB:**

   ```bash
   # На устройстве через ADB:
   adb shell "setprop sys.usb.config none"
   sleep 2
   adb shell "setprop sys.usb.config mtp,adb,serial"

   # На Mac - проверьте порты:
   ls -la /dev/tty.* /dev/cu.*
   ```

4. **Проверьте через сеть:**

   ```bash
   # Попробуйте подключиться к QNX через сеть:
   telnet 192.168.2.1 10005
   nc 192.168.2.1 10005
   ```

5. **Попробуйте активировать QNX через ADB:**
   ```bash
   adb shell "am broadcast -a com.desaysv.engineering.START"
   adb shell "setprop persist.qnx.serial.enable 1"
   adb shell "setprop sys.qnx.uart.enable 1"
   ```

---

## 📊 ИТОГОВАЯ СВОДКА

| Компонент                 | Статус         | Детали                                               |
| ------------------------- | -------------- | ---------------------------------------------------- |
| ADB                       | ✅ Подключен   | G6SH-r8a7795, Serial: 00002260                       |
| USB Serial Gadget         | ✅ Активирован | `mtp,adb,serial`                                     |
| QNX Serial Enable         | ✅ Включен     | `persist.qnx.serial.enable: 1`                       |
| QNX UART Enable           | ✅ Включен     | `sys.qnx.uart.enable: 1`                             |
| QNX Shared Memory         | ✅ Найден      | `/sys/devices/platform/vdevs/1c050000.qnx,guest_shm` |
| QNX Virtio Serial         | ✅ Найден      | `/sys/bus/virtio/drivers/virtio_rproc_serial/`       |
| QNX Partitions            | ✅ Найдены     | 8 разделов через virtio_blk                          |
| USB Serial Port (Mac)     | ❌ Не найден   | Нет `/dev/tty.*` или `/dev/cu.*` для QNX             |
| QNX Network (192.168.2.1) | ❌ Недоступен  | Не отвечает на ping/порты                            |
| Engineering Mode          | ✅ Активирован | Broadcast выполнен успешно                           |

---

## 🔗 ССЫЛКИ

- Полный отчёт: `/Knowledge_base/11_QNX/REVERSE_ENGINEERING_REPORT.md`
- Скрипты подключения: `/Knowledge_base/11_QNX/`

---

## 🔄 РЕЗУЛЬТАТЫ ПРОВЕРКИ ДРАЙВЕРОВ И ПЕРЕПОДКЛЮЧЕНИЯ USB

**Дата проверки:** 2025-11-27

### 1. Проверка драйверов USB-to-Serial на Mac

**Результат:** ❌ Драйверы USB-to-Serial не обнаружены

- Проверены: CP210x, FTDI, CH340, PL2303, Prolific
- На Mac обнаружены только стандартные USB устройства (USB Hub, USB Storage)
- **Вывод:** QNX порт не подключен как USB Serial адаптер, или требует специальных драйверов

### 2. Переподключение USB (сброс конфигурации)

**Выполненные действия:**

```bash
# Сброс USB конфигурации
adb shell "setprop sys.usb.config none"
sleep 3

# Активация USB Serial
adb shell "setprop sys.usb.config mtp,adb,serial"
sleep 5
```

**Результат:** ⚠️ Устройство отключилось и не переподключилось автоматически

- После сброса USB конфигурации устройство отключилось от ADB
- Через 10 секунд устройство не переподключилось
- **Требуется:** Физическое переподключение USB кабеля пользователем

### 3. Проверка USB Serial портов на Mac

**Результат:** ❌ USB Serial порты не обнаружены

- Проверены `/dev/tty.*` и `/dev/cu.*`
- Новые порты не появились после активации `serial` в USB конфигурации
- **Вывод:** USB Serial Gadget на устройстве не создаёт видимый порт на Mac

### 4. Анализ USB устройств через ioreg

**Найдено:**

- `AppleUSBSerial` класс присутствует в системе (0 экземпляров)
- `AppleUSBACMData` класс присутствует в системе (0 экземпляров)
- Устройство G6SH-r8a7795 видно только как ADB устройство (Renesas, idVendor: 6353)

**Вывод:** Mac поддерживает USB Serial, но устройство не предоставляет Serial интерфейс через USB Gadget

---

## 📋 РЕКОМЕНДАЦИИ

### Немедленные действия:

1. **Физическое переподключение USB:**

   - Отключите и подключите USB кабель ADB
   - Проверьте, что устройство появилось: `adb devices -l`

2. **Проверка второго USB порта (QNX):**

   - Убедитесь, что второй mini-USB кабель подключен к порту "QNX" на устройстве
   - Проверьте, что кабель подключен к Mac
   - Проверьте наличие драйверов для USB-to-Serial адаптера (если используется)

3. **Альтернативные способы доступа к QNX:**
   - Через сеть: `telnet 192.168.2.1 10005` (после восстановления ADB)
   - Через virtio интерфейсы (требует root доступ)
   - Через QNX Shared Memory (требует анализ)

---

---

## ✅ РЕЗУЛЬТАТЫ ПОДКЛЮЧЕНИЯ (2025-11-27)

### 1. ADB Подключение

**Статус:** ✅ **ПОДКЛЮЧЕНО**

- Устройство: `00002260`
- Модель: `G6SH_r8a7795`
- Транспорт: USB (usb:0-0)
- IP адрес: `192.168.2.2/24` (eth0)

### 2. QNX Сетевые соединения

**Статус:** ✅ **АКТИВНЫЕ СОЕДИНЕНИЯ ОБНАРУЖЕНЫ**

**Установленные соединения к QNX/MCU (192.168.2.1):**

- `192.168.2.2:50622 → 192.168.2.1:10004` (ESTABLISHED)
- `192.168.2.2:53378 → 192.168.2.1:31040` (ESTABLISHED)
- `192.168.2.2:54868 → 192.168.2.1:31050` (ESTABLISHED)
- `192.168.2.2:57824 → 192.168.2.1:31030` (ESTABLISHED)

**Слушающие порты:**

- `0.0.0.0:10005` (TCP, LISTEN) ✅

### 3. ADB Port Forwarding

**Статус:** ✅ **УСТАНОВЛЕН**

```bash
adb forward tcp:10005 tcp:10005
```

- Порт 10005 проброшен на localhost
- Подключение к `localhost:10005` возможно, но сервис не отвечает

### 4. QNX Properties

**Статус:** ✅ **ВСЕ ВКЛЮЧЕНО**

```bash
persist.qnx.serial.enable: 1          ✅
sys.qnx.uart.enable: 1                 ✅
persist.sys.qnx: 1                     ✅
```

### 5. QNX Shared Memory

**Статус:** ✅ **НАЙДЕН**

- Путь: `/sys/devices/platform/vdevs/1c050000.qnx,guest_shm`
- Driver: `qnx-virtio-du` ✅
- Uevent: читается успешно ✅

### 6. QNX Virtio Serial Driver

**Статус:** ✅ **НАЙДЕН**

- Путь: `/sys/bus/virtio/drivers/virtio_rproc_serial/`
- Файлы: `bind`, `uevent`, `unbind`

### 7. USB Gadget

**Статус:** ⚠️ **АКТИВЕН, НО SERIAL НЕ РАБОТАЕТ**

- UDC: `e6590000.usb` ✅
- Конфигурация: `adb` (не `mtp,adb,serial`)
- USB Serial порт на Mac: ❌ не обнаружен

### 8. TTY Устройства на Android

**Найдены:**

- `/dev/ttyAMA0` (root, 204:64) - Permission denied для shell пользователя
- `/dev/ttySC1` (gps:system, 204:9)
- `/dev/ttySC6` (bluetooth:bluetooth, 204:14)

### 9. Engineering Mode

**Статус:** ✅ **АКТИВИРОВАН**

```bash
adb shell "am broadcast -a com.desaysv.engineering.START"
# Broadcast completed: result=0
```

---

## 📊 ИТОГОВАЯ СВОДКА ПОДКЛЮЧЕНИЯ

| Компонент               | Статус         | Детали                                                        |
| ----------------------- | -------------- | ------------------------------------------------------------- |
| ADB                     | ✅ Подключен   | G6SH-r8a7795, Serial: 00002260, IP: 192.168.2.2               |
| QNX Network Connections | ✅ Активны     | 4 соединения к 192.168.2.1 (порты 10004, 31030, 31040, 31050) |
| QNX Port 10005          | ✅ Слушает     | 0.0.0.0:10005 (TCP, LISTEN)                                   |
| ADB Port Forwarding     | ✅ Установлен  | localhost:10005 → device:10005                                |
| QNX Serial Enable       | ✅ Включен     | `persist.qnx.serial.enable: 1`                                |
| QNX UART Enable         | ✅ Включен     | `sys.qnx.uart.enable: 1`                                      |
| QNX Shared Memory       | ✅ Найден      | `/sys/devices/platform/vdevs/1c050000.qnx,guest_shm`          |
| QNX Virtio Serial       | ✅ Найден      | `/sys/bus/virtio/drivers/virtio_rproc_serial/`                |
| USB Serial Gadget       | ⚠️ Частично    | UDC активен, но Serial порт на Mac не создаётся               |
| USB Serial Port (Mac)   | ❌ Не найден   | Нет `/dev/tty.*` или `/dev/cu.*` для QNX                      |
| TTY Devices (Android)   | ⚠️ Ограничен   | ttyAMA0 требует root, ttySC1/SC6 доступны                     |
| Engineering Mode        | ✅ Активирован | Broadcast выполнен успешно                                    |

---

## 🔍 ВЫВОДЫ

1. **ADB подключение работает** ✅
2. **QNX доступен через сеть** ✅ (активные соединения к 192.168.2.1)
3. **Порт 10005 слушает**, но не отвечает на подключения через port forwarding
4. **USB Serial Gadget не создаёт порт на Mac** - возможно, требуется специальная конфигурация или драйверы
5. **QNX Serial/UART включены**, но прямой доступ к TTY устройствам ограничен правами shell пользователя

---

**Следующие шаги:**

1. ✅ Проверить физическое подключение QNX порта - **выполнено**
2. ✅ Установить драйверы USB-to-Serial - **не обнаружены**
3. ✅ Переподключить USB кабель ADB - **выполнено, ADB работает**
4. ✅ Попробовать подключиться через сеть - **порт 10005 слушает, но не отвечает**
5. ⚠️ Исследовать альтернативные способы доступа к QNX:
   - Попробовать подключиться к портам 10004, 31030, 31040, 31050 через port forwarding
   - Исследовать QNX Shared Memory для чтения данных
   - Попробовать получить root доступ для работы с TTY устройствами
   - Исследовать USB Gadget конфигурацию для активации Serial интерфейса
