# Установка и запуск Remote Desktop

## 📋 ПОДГОТОВКА

### 1. Установить Pillow (для GUI версии):

```bash
# Вариант 1: С --break-system-packages
pip3 install --break-system-packages Pillow

# Вариант 2: Через brew (если доступно)
brew install pillow

# Вариант 3: Использовать упрощенную версию без GUI
# (не требует Pillow)
```

### 2. Подключить ГУ через USB:

```bash
adb devices
# Должно показать устройство
```

---

## 🚀 ЗАПУСК

### Вариант A: Упрощенная версия (без GUI, не требует Pillow)

**1. Запустить сервер:**
```bash
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX/mac_server
python3 simple_tunnel_server.py 22222
```

**2. Установить клиент на ГУ:**
```bash
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX
./install_remote_tunnel.sh
# Ввести IP Mac: 192.168.0.117 (локальный) или ваш публичный IP
```

### Вариант B: Полная версия (с GUI, требует Pillow)

**1. Установить Pillow:**
```bash
pip3 install --break-system-packages Pillow
```

**2. Запустить сервер:**
```bash
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX/mac_server
python3 remote_desktop_server.py 22222
```

**3. Установить клиент (как в варианте A)**

---

## ✅ ПРОВЕРКА

### После запуска сервера:

```bash
# Проверить, что слушает порт
lsof -i :22222
# Должен быть процесс python3
```

### После установки клиента:

```bash
# Проверить процесс на ГУ
adb shell 'ps | grep remote_tunnel'

# Проверить логи
adb shell 'tail -20 /data/local/tmp/tunnel_client.log'
```

**Должно быть:**
- "Подключение к серверу..."
- "Подключено к серверу"
- "Приветствие сервера: TUNNEL_READY"

---

## 🔌 ОТКЛЮЧЕНИЕ USB

**МОЖНО ОТКЛЮЧАТЬ USB КОГДА:**

1. ✅ Сервер запущен и показывает "Ожидание подключений..."
2. ✅ В логах клиента есть "Подключено к серверу"
3. ✅ ГУ подключен к WiFi/интернету
4. ✅ (Для GUI версии) В окне виден экран ГУ

**После отключения:**
- ГУ автоматически переподключится через WiFi
- Экран продолжит обновляться (если включен)
- Touch события будут работать

---

## 📊 МОНИТОРИНГ

### Логи сервера (Mac):
- Выводятся в консоль
- Или: `python3 simple_tunnel_server.py 22222 > server.log 2>&1`

### Логи клиента (ГУ):
```bash
# В реальном времени
adb shell 'tail -f /data/local/tmp/tunnel_client.log'

# Последние строки
adb shell 'tail -50 /data/local/tmp/tunnel_client.log'
```

---

## ⚠️ ЕСЛИ НЕ РАБОТАЕТ

1. **Проверить firewall:**
   ```bash
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/bin/python3
   ```

2. **Проверить IP Mac:**
   ```bash
   ifconfig | grep "inet " | grep -v 127.0.0.1
   # Или
   curl ifconfig.me
   ```

3. **Перезапустить клиент:**
   ```bash
   adb shell 'killall sh'
   adb shell '/data/local/tmp/remote_tunnel_client.sh &'
   ```

4. **Проверить интернет на ГУ:**
   ```bash
   adb shell 'ping -c 3 8.8.8.8'
   ```

---

## 🎯 БЫСТРЫЙ СТАРТ (2 команды)

```bash
# Терминал 1: Запустить сервер
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX/mac_server
python3 simple_tunnel_server.py 22222

# Терминал 2: Установить клиент
cd /Users/valentinezov/Projects/Tiggo/Knowledge_base/11_QNX
./install_remote_tunnel.sh
```

**Готово! Теперь можно отключать USB!**

