# Настройка Remote Desktop: ГУ → Mac с экраном и touch

**Дата:** 2025-11-27
**Цель:** Удаленный доступ к ГУ с отображением экрана и имитацией touch

---

## 🎯 ФУНКЦИОНАЛ

1. **Получение скриншота с ГУ (Android)**
   - Автоматическая передача каждые 500ms (2 FPS)
   - Опция включения/выключения для экономии трафика

2. **Получение скриншота с QNX (приборной панели)**
   - Через shared memory или другие методы
   - Опционально (требует дополнительной настройки)

3. **Имитация touch событий**
   - Клик мышью на Mac → touch на ГУ
   - Поддержка tap, swipe, drag

4. **GUI на Mac**
   - Отображение экрана ГУ
   - Кнопки управления
   - Переключение режимов

---

## 🖥️ НАСТРОЙКА MAC

### Шаг 1: Установка зависимостей

```bash
cd Knowledge_base/11_QNX/mac_server

# Установить Pillow для работы с изображениями
pip3 install -r requirements.txt
```

### Шаг 2: Запуск сервера

```bash
./start_remote_desktop.sh 22222
```

**Или вручную:**
```bash
python3 remote_desktop_server.py 22222
```

### Шаг 3: Использование GUI

После запуска сервера автоматически откроется окно:
- **Кнопка "Включить экран"** - переключение режима передачи экрана
- **Кнопка "Обновить"** - запрос нового скриншота
- **Клик на экране** - имитация touch на ГУ
- **Перетаскивание** - имитация swipe

---

## 📱 НАСТРОЙКА ГУ

### Шаг 1: Обновить ReverseTunnelClient

**Добавлены функции:**
- Автоматическая отправка скриншотов
- Обработка touch команд от сервера
- Переключение режима экрана

### Шаг 2: Установить APK

```bash
# Скомпилировать APK с обновленным кодом
# Установить на ГУ
adb install ReverseTunnelClient.apk
```

### Шаг 3: Проверка разрешений

**Требуемые разрешения:**
- `android.permission.WRITE_SECURE_SETTINGS` (для input tap)
- Или root доступ

**Если нет root:**
- Использовать Accessibility Service
- Или использовать `uiautomator` для touch

---

## 🔧 ОПТИМИЗАЦИЯ ТРАФИКА

### Текущие настройки:

- **Частота обновления:** 2 FPS (500ms)
- **Сжатие:** Base64 (можно улучшить до JPEG с качеством)
- **Разрешение:** Полное (можно масштабировать)

### Рекомендации:

1. **Сжатие JPEG:**
   ```java
   // В sendScreenshot()
   Bitmap bitmap = BitmapFactory.decodeByteArray(imageBytes, 0, imageBytes.length);
   ByteArrayOutputStream outputStream = new ByteArrayOutputStream();
   bitmap.compress(Bitmap.CompressFormat.JPEG, 70, outputStream);
   byte[] compressedBytes = outputStream.toByteArray();
   ```

2. **Масштабирование:**
   ```java
   // Уменьшить разрешение для экономии трафика
   int scale = 2; // Уменьшить в 2 раза
   Bitmap scaled = Bitmap.createScaledBitmap(bitmap,
       bitmap.getWidth() / scale,
       bitmap.getHeight() / scale,
       true);
   ```

3. **Delta updates:**
   - Отправлять только измененные области
   - Сложнее в реализации, но эффективнее

---

## 🎮 ИСПОЛЬЗОВАНИЕ

### Включение экрана:

1. Запустить сервер на Mac
2. Подождать подключения ГУ
3. Нажать "Включить экран" в GUI
4. Экран начнет обновляться автоматически

### Touch события:

1. Кликнуть на экране в GUI
2. Событие отправится на ГУ
3. Выполнится `input tap x y`

### Swipe:

1. Зажать кнопку мыши
2. Перетащить
3. Отпустить
4. Выполнится swipe на ГУ

---

## ⚠️ ОГРАНИЧЕНИЯ

1. **Требуется root или специальные разрешения:**
   - Для `input tap` нужен root
   - Или использовать Accessibility Service

2. **Трафик:**
   - Полный экран ~200-500 KB на кадр
   - При 2 FPS = ~400 KB/s - 1 MB/s
   - Рекомендуется использовать только при необходимости

3. **Задержка:**
   - Зависит от скорости интернета
   - Может быть заметна при медленном соединении

---

## 🔐 БЕЗОПАСНОСТЬ

1. **Шифрование:**
   - Рекомендуется использовать VPN
   - Или добавить TLS/SSL в протокол

2. **Аутентификация:**
   - Добавить проверку подлинности клиента
   - Использовать ключи или пароли

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ Добавить сжатие JPEG
2. ⏳ Добавить масштабирование
3. ⏳ Реализовать получение QNX скриншотов
4. ⏳ Добавить переключение между Android и QNX
5. ⏳ Оптимизировать трафик

---

**Статус:** Базовая функциональность готова. Требуется тестирование и оптимизация.

