# Получение скриншота с QNX (приборной панели)

**Дата:** 2025-11-27
**Цель:** Добавить возможность получения скриншота с QNX системы

---

## 🔍 ВОЗМОЖНЫЕ СПОСОБЫ

### Метод 1: Через QNX консоль

**Если есть доступ к QNX консоли:**
```bash
# Через ADB port forwarding
adb forward tcp:10005 tcp:10005

# Подключение к QNX
nc localhost 10005

# Команды для скриншота (если поддерживаются)
# Нужно проверить доступные команды в QNX
```

### Метод 2: Через Shared Memory

**QNX использует shared memory для связи с Android:**
```bash
# Проверить shared memory
adb shell ls -la /dev/shmem/
adb shell cat /sys/devices/platform/vdevs/1c050000.qnx,guest_shm/uevent
```

**Возможность:**
- QNX может передавать данные экрана через shared memory
- Нужно найти формат данных
- Декодировать и отобразить

### Метод 3: Через QNX приложения

**Проверить QNX приложения:**
```bash
# Найти приложения, которые могут делать скриншоты
adb shell find /system /vendor -name "*qnx*" -o -name "*screen*" -o -name "*capture*"
```

### Метод 4: Через Android приложения

**Некоторые Android приложения могут получать данные от QNX:**
- Проверить `com.desaysv.*` приложения
- Найти API для получения данных приборной панели

---

## 🛠️ РЕАЛИЗАЦИЯ

### Вариант 1: Расширить RemoteTunnelClient

**Добавить метод для получения QNX скриншота:**
```java
private void sendQnxScreenshot() {
    try {
        // Попытка 1: Через shared memory
        // Читаем данные из shared memory

        // Попытка 2: Через QNX команду
        // Выполняем команду через port forwarding

        // Попытка 3: Через Android приложение
        // Используем Intent для запроса скриншота

    } catch (Exception e) {
        Log.e(TAG, "Error sending QNX screenshot", e);
    }
}
```

### Вариант 2: Отдельный сервис для QNX

**Создать отдельный сервис:**
- `QnxScreenCaptureService.java`
- Специализированный для работы с QNX
- Использует shared memory или другие методы

---

## 📋 ПЛАН ДЕЙСТВИЙ

1. ⏳ Проверить доступные методы получения QNX данных
2. ⏳ Найти формат данных в shared memory
3. ⏳ Реализовать декодирование данных
4. ⏳ Интегрировать в Remote Desktop Server
5. ⏳ Добавить переключение между Android и QNX экранами

---

## 🎯 РЕКОМЕНДАЦИИ

**Для начала:**
- Сосредоточиться на Android экране (уже работает)
- QNX экран добавить позже, когда найдем способ получения данных

**Альтернатива:**
- Если QNX данные передаются через Android приложения
- Можно использовать Android Screen Capture API
- И получать данные через приложения

---

**Статус:** Требуется исследование методов получения QNX скриншотов.

