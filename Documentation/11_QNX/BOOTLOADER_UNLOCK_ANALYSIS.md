# Анализ разблокировки загрузчика и установки модифицированного образа - G6SH/T18FL3

**Дата:** 2025-11-27
**Цель:** Найти способы установки модифицированного образа системы без оригинальной подписи

---

## 🔒 ТЕКУЩИЙ СТАТУС ЗАГРУЗЧИКА

### Проверка статуса разблокировки

**Ключевые свойства:**
```bash
sys.user_unlocked=1                # ✅✅✅ ПОЛЬЗОВАТЕЛЬ РАЗБЛОКИРОВАН!
sys.oem_unlock_allowed=0           # ❌ OEM unlock не разрешен (но user уже разблокирован!)
ro.oem_unlock_supported=1          # ✅ Поддержка OEM unlock включена
ro.boot.veritymode=eio              # ✅ Verity в режиме EIO (errors ignored) - ОТЛИЧНО!
ro.secure=1                        # ❌ Secure режим включен
ro.debuggable=0                    # ❌ Debuggable выключен
ro.build.type=user                  # ⚠️  Production build
ro.build.tags=dev-keys              # ✅ Dev keys (хорошо!)
ro.build.ab_update=true             # ✅ A/B обновления поддерживаются
persist.sys.flash_write_protect=true # ⚠️  Защита от записи включена
ro.adb.secure=1                    # ⚠️  ADB secure включен
```

**Bootloader информация:**
```bash
ro.bootloader=unknown              # ⚠️  Неизвестная версия
sys.norflash.bootloader.version=80208390 - 19:17:11, Oct 26 2022  # ✅ Версия найдена
ro.hardware=g6sh                   # ✅ G6SH hardware
```

**Slot информация (A/B разделы):**
```bash
ro.boot.slot_suffix=_b              # ✅ Используется slot B
ro.boot.slot_suffix_bootloader=_b   # ✅ Bootloader slot B
ro.boot.slot_suffix_qnx=_b          # ✅ QNX slot B
```

---

## 🔍 ВОЗМОЖНЫЕ СПОСОБЫ УСТАНОВКИ

### 1. Разблокировка загрузчика (Bootloader Unlock)

#### ✅ КРИТИЧЕСКАЯ НАХОДКА:
```bash
sys.user_unlocked=1                # ✅✅✅ ПОЛЬЗОВАТЕЛЬ УЖЕ РАЗБЛОКИРОВАН!
```

**Это означает:**
- ✅ Загрузчик уже разблокирован на уровне пользователя!
- ✅ Можно устанавливать модифицированные образы!
- ⚠️ Но `sys.oem_unlock_allowed=0` - OEM unlock не разрешен (не критично, если user уже разблокирован)

#### Проверка поддержки:
- ✅ `ro.oem_unlock_supported=1` - Поддержка есть!
- ✅ `sys.user_unlocked=1` - **РАЗБЛОКИРОВАН!**

#### Способы разблокировки:

**A. Через Fastboot (если доступен):**
```bash
# Перезагрузка в fastboot режим
adb reboot bootloader

# Проверка статуса
fastboot oem device-info

# Разблокировка (если разрешено)
fastboot oem unlock
# или
fastboot flashing unlock
```

**B. Через инженерное меню:**
- Проверить наличие опции в `com.android.engmode`
- Возможно скрытая опция в настройках

**C. Через системные свойства:**
```bash
# Попытка установки (может не работать без root)
adb shell setprop sys.oem_unlock_allowed 1
```

#### Проблемы:
- ⚠️ Может требовать подтверждения на экране устройства
- ⚠️ Может стереть данные (factory reset)
- ⚠️ Может быть заблокировано производителем

---

### 2. Обход проверки подписи (Signature Bypass)

#### A. DM-Verity обход

**Текущий статус:**
```bash
ro.boot.veritymode=eio              # ✅✅✅ Errors Ignored - ОТЛИЧНО!
```

**EIO режим означает:**
- ✅ Ошибки верификации **ИГНОРИРУЮТСЯ**
- ✅ Можно читать модифицированные разделы
- ✅ Можно устанавливать неподписанные образы
- ⚠️ Но `persist.sys.flash_write_protect=true` - защита от записи включена

**Способы:**
1. **Модификация boot.img:**
   - Удалить/отключить dm-verity в cmdline
   - Пересобрать boot.img
   - Установить через recovery или fastboot

2. **Использование EIO режима:**
   - Если veritymode=eio, можно попробовать установить без проверки
   - Но нужен доступ к recovery/fastboot

#### B. Отключение проверки подписи в recovery

**Проверка recovery:**
```bash
# Найти recovery раздел
ls -la /dev/block/by-name/ | grep recovery

# Проверить доступность recovery
adb reboot recovery
```

**Модификация recovery:**
- Установить кастомный recovery (TWRP)
- Кастомный recovery может игнорировать подписи
- Но нужна разблокировка загрузчика

---

### 3. Использование режима разработчика (Developer Mode)

**Текущий статус:**
```bash
persist.sys.eng=1                   # ✅ Инженерный режим включен
persist.sys.dev=1                   # ✅ Режим разработчика включен
ro.build.tags=dev-keys              # ✅ Dev keys
```

**Возможности:**
- ✅ ADB включен
- ✅ Инженерное меню доступно
- ⚠️ Но может не давать доступ к установке образов

**Проверка:**
```bash
# Проверить настройки разработчика
settings get global development_settings_enabled
settings get global adb_enabled
```

---

### 4. Использование приложений обновления

**Найденные приложения:**
- ✅ `com.desaysv.update` - Приложение обновления
  - Activity: `MainActivity`
  - Service: `UpdateService`
  - Receiver: `USBDeviceStatusReceiver` (обрабатывает USB подключения)
  - Intent Action: `com.desaysv.update.UpdateApi`

- ✅ `com.desaysv.upgrade` - Приложение обновления (локальное)
  - Activity: `MainActivity`
  - Service: `UpdateService`
  - Intent Action: `com.desaysv.update.UpdateApi`

**Системные сервисы:**
- ✅ `update_engine` - Запущен и работает
- ✅ `update_verifier` - Проверка обновлений
- ✅ `otapreopt` - Оптимизация после OTA

**Директории обновлений:**
- `/data/update` - Локальные обновления
- `/data_fota` - FOTA обновления
- `/data_extra/update` - Дополнительные обновления
- `/data/vehicle/update/dsp` - Обновления DSP
- `/data/vehicle/update/mcu` - Обновления MCU

**Возможности:**
- Могут иметь скрытые функции для установки образов
- Могут игнорировать проверку подписи в определенных режимах
- Поддержка USB обновлений (через USBDeviceStatusReceiver)
- Требует анализа APK

**Активация:**
```bash
adb shell am start -n com.desaysv.update/.MainActivity
adb shell am start -n com.desaysv.upgrade/.MainActivity
```

---

### 5. Прямая запись в разделы (DD метод)

**Требования:**
- Root доступ (или обход)
- Знание разделов
- Бэкап оригинальных разделов

**Разделы:**
```bash
# Boot разделы
/dev/block/by-name/bootloader_a
/dev/block/by-name/bootloader_b
/dev/block/by-name/boot_a
/dev/block/by-name/boot_b

# System разделы
/dev/block/by-name/system_a
/dev/block/by-name/system_b
```

**Метод:**
```bash
# 1. Бэкап оригинального раздела
adb shell dd if=/dev/block/by-name/boot_b of=/sdcard/boot_b_backup.img

# 2. Запись модифицированного образа
adb push modified_boot.img /sdcard/
adb shell dd if=/sdcard/modified_boot.img of=/dev/block/by-name/boot_b

# 3. Перезагрузка
adb reboot
```

**Проблемы:**
- ❌ Требует root доступ
- ⚠️ Опасность "кирпича" устройства
- ⚠️ Может быть заблокировано SELinux

---

### 6. Использование A/B разделов

**Текущий статус:**
- ✅ Используется slot B (`_b`)
- ✅ Есть slot A и slot B

**Стратегия:**
1. Установить модифицированный образ в slot A
2. Переключиться на slot A
3. Если не работает - вернуться на slot B

**Команды:**
```bash
# Переключение slot (требует fastboot)
fastboot set_active a
fastboot set_active b
```

---

### 7. Модификация через инженерное меню

**Проверка функций:**
- `com.android.engmode` имеет разрешения:
  - `RECOVERY` - доступ к recovery
  - `REBOOT` - перезагрузка
  - `MASTER_CLEAR` - сброс

**Возможные скрытые функции:**
- Установка образов
- Разблокировка загрузчика
- Отключение проверки подписи

**Требует:**
- Декомпиляции `SVEngMode.apk`
- Анализа всех Activity и функций

---

## 🛠️ ПЛАН ДЕЙСТВИЙ

### Этап 1: Проверка доступности Fastboot

```bash
# 1. Перезагрузка в bootloader
adb reboot bootloader

# 2. Проверка подключения
fastboot devices

# 3. Проверка статуса
fastboot oem device-info
fastboot getvar all
```

### Этап 2: Анализ приложений обновления

```bash
# 1. Извлечение APK
adb pull /product/app/SVUpdateApp/SVUpdateApp.apk
adb pull /product/app/SVLocalUpdateApp/SVLocalUpdateApp.apk

# 2. Декомпиляция (JADX)
jadx -d decompiled SVUpdateApp.apk
jadx -d decompiled SVLocalUpdateApp.apk

# 3. Поиск функций установки
grep -r "install" decompiled/
grep -r "flash" decompiled/
grep -r "signature" decompiled/
```

### Этап 3: Анализ инженерного меню

```bash
# 1. Декомпиляция
jadx -d decompiled SVEngMode.apk

# 2. Поиск функций
grep -r "bootloader" decompiled/
grep -r "unlock" decompiled/
grep -r "flash" decompiled/
grep -r "recovery" decompiled/
```

### Этап 4: Проверка Recovery

```bash
# 1. Перезагрузка в recovery
adb reboot recovery

# 2. Проверка доступных команд
# (через recovery меню или adb в recovery режиме)

# 3. Попытка установки через recovery
# (если доступен sideload или install)
```

### Этап 5: Модификация boot.img

```bash
# 1. Извлечение boot.img
adb pull /dev/block/by-name/boot_b boot_b.img

# 2. Распаковка
unpack_bootimg.py --boot_img boot_b.img --out boot_unpacked/

# 3. Модификация cmdline
# Удалить: androidboot.veritymode=eio
# Добавить: androidboot.veritymode=disabled

# 4. Пересборка
mkbootimg --kernel boot_unpacked/kernel \
          --ramdisk boot_unpacked/ramdisk.cpio.gz \
          --cmdline "новый cmdline" \
          --output modified_boot.img

# 5. Установка (требует разблокировки или обхода)
```

---

## ⚠️ РИСКИ И ПРЕДУПРЕЖДЕНИЯ

### Высокий риск:
1. **"Кирпич" устройства** - неправильная установка может заблокировать устройство
2. **Потеря гарантии** - разблокировка загрузчика обычно аннулирует гарантию
3. **Потеря данных** - разблокировка может стереть все данные

### Средний риск:
1. **Несовместимость** - модифицированный образ может не работать
2. **Проблемы с QNX** - модификация может нарушить связь с QNX/MCU

### Низкий риск:
1. **Откат** - можно вернуться к оригинальному образу (если есть бэкап)

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПОДХОД

### ⭐ ВАРИАНТ 1: Через приложение обновления (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Загрузчик уже разблокирован (`sys.user_unlocked=1`)
- ✅ Verity в режиме EIO (игнорирует ошибки)
- ✅ Есть приложения обновления с поддержкой USB
- ✅ Меньше риска "кирпича"

**Шаги:**
1. ✅ Извлечь и декомпилировать `SVUpdateApp.apk` и `SVLocalUpdateApp.apk`
2. ✅ Найти способ обхода проверки подписи
3. ✅ Подготовить образ в формате, который принимает приложение
4. ✅ Установить через USB или локально

### Вариант 2: Через Fastboot (если доступен)

1. ✅ Проверить доступность fastboot
2. ✅ Проверить статус разблокировки
3. ✅ Разблокировать загрузчик (если разрешено)
4. ✅ Установить модифицированный образ

### Вариант 2: Через приложение обновления

1. ✅ Проанализировать `SVUpdateApp.apk`
2. ✅ Найти способ обхода проверки подписи
3. ✅ Использовать скрытые функции установки

### Вариант 3: Через инженерное меню

1. ✅ Декомпилировать `SVEngMode.apk`
2. ✅ Найти скрытые функции
3. ✅ Активировать режим установки

### Вариант 4: Модификация boot.img

1. ✅ Использовать EIO режим verity
2. ✅ Модифицировать boot.img для отключения проверки
3. ✅ Установить через recovery/fastboot

---

## 📋 ЧЕКЛИСТ ПЕРЕД УСТАНОВКОЙ

- [ ] Создан полный бэкап всех разделов
- [ ] Проверена доступность fastboot
- [ ] Проверен статус загрузчика
- [ ] Проанализированы приложения обновления
- [ ] Проанализировано инженерное меню
- [ ] Подготовлен план отката
- [ ] Понимание рисков

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ Проверить доступность fastboot режима
2. ⏳ Декомпилировать приложения обновления
3. ⏳ Декомпилировать инженерное меню
4. ⏳ Найти все скрытые функции
5. ⏳ Протестировать на безопасном устройстве (если возможно)

---

**Статус:** Требуется дальнейший анализ для определения доступных методов установки.

