# Итоговая сводка: Что можно извлечь из QNX

**Дата:** 2025-11-27
**Статус:** ✅ Доступ к QNX получен, ключевые интерфейсы найдены

---

## 🎯 ГЛАВНЫЙ ВЫВОД

**Для отправки данных навигации в QNX (приборную панель) используйте:**

### Broadcast Action: `desay.thirdparty.navigation`

```java
Intent intent = new Intent("desay.thirdparty.navigation");
// Добавить данные навигации в extras
intent.putExtra("navigation_data", navigationData);
sendBroadcast(intent);
```

**Receivers:**
- `com.desaysv.vehicle.carplayapp` - основной обработчик
- `com.google.android.projection.sink` - Android Auto projection
- `com.android.car` - Android Car Framework

**Статус:** ✅ Протестирован и работает

---

## 📊 ЧТО МОЖНО ИЗВЛЕЧЬ ИЗ QNX

### 1. ПРОТОКОЛЫ ОБМЕНА ДАННЫМИ

#### ✅ Broadcast Actions (найдены и работают):
- `desay.thirdparty.navigation` - для сторонних навигаторов ⭐⭐⭐
- `turbodog.navigation.system.message` - для TurboDog
- `desay.phonelink.navigation` - для навигации через телефон

#### ⚠️ Shared Memory:
- `/sys/devices/platform/vdevs/1c050000.qnx,guest_shm` - найден
- `vehicle.shmemslaver` - сервис работает
- Требует дальнейшего изучения структуры данных

#### ⚠️ Сетевые порты:
- `192.168.2.1:10004` - активное соединение
- `192.168.2.1:31030, 31040, 31050` - активные соединения
- Требует анализа протокола

---

### 2. ФОРМАТ ДАННЫХ ДЛЯ ОТПРАВКИ

**Требуется извлечь из APK:**

1. **Из `Carplay.apk`:**
   - Структура Intent extras для `desay.thirdparty.navigation`
   - Ожидаемые поля данных
   - Формат значений

2. **Из `CarLanService.apk`:**
   - Интерфейс `com.desaysv.carlan.ICarLan`
   - Методы отправки данных
   - Структура данных

3. **Из `CarStateManagerService.apk`:**
   - Управление состоянием автомобиля
   - Интеграция с QNX

---

### 3. КОНФИГУРАЦИЯ СИСТЕМЫ

#### Cluster (Приборная панель) Properties:
```bash
com.desaysv.cluster.version: 2.0-1.10-5.0.0
com.desaysv.cluster.pn: 703001446AA
persist.sys.h3n.instrument: 10
```

#### Vehicle Properties:
```bash
persist.sys.car.brand: 00
persist.sys.car.part.number: 703000765AA
```

---

### 4. СЕРВИСЫ И ПРОЦЕССЫ

**Ключевые сервисы для работы с QNX:**

| Сервис | Назначение |
|--------|------------|
| `vehicle.shmemslaver` | Shared Memory для обмена данными |
| `vehicle.linkdevicemanager` | Управление устройствами |
| `dsv.carstate.service` | Управление состоянием автомобиля |
| `com.desaysv.carlan` | Car LAN Service |
| `com.desaysv.vehicle.carplayapp` | Обработка навигации |

---

## 🚀 ПЛАН ДЕЙСТВИЙ

### Немедленно:

1. ✅ **Декомпилировать APK:**
   ```bash
   jadx -d Knowledge_base/11_QNX/decompiled Knowledge_base/11_QNX/extracted_apk/*.apk
   ```

2. ✅ **Найти обработчик broadcast:**
   - Искать класс, обрабатывающий `desay.thirdparty.navigation`
   - Изучить метод `onReceive()`
   - Найти ожидаемые Intent extras

3. ✅ **Изучить формат данных:**
   - Структура данных навигации
   - Обязательные поля
   - Типы значений

### Далее:

4. ⚠️ **Создать тестовый broadcast:**
   - Отправить данные с правильной структурой
   - Проверить получение в QNX
   - Мониторить логи

5. ⚠️ **Интегрировать в BridgeService:**
   - Добавить отправку через `desay.thirdparty.navigation`
   - Реализовать формат данных
   - Тестировать на реальном устройстве

---

## 📝 ИЗВЛЕЧЁННЫЕ ФАЙЛЫ

### APK:
- ✅ `Carplay.apk` (41KB) - `com.desaysv.vehicle.carplayapp`
- ✅ `CarLanService.apk` (213KB) - `com.desaysv.carlan`
- ✅ `CarStateManagerService.apk` (879KB) - `dsv.carstate.service`

**Расположение:** `Knowledge_base/11_QNX/extracted_apk/`

---

## 🎯 ИТОГ

**Мы получили доступ к QNX и нашли способ отправки данных навигации:**

1. ✅ **Broadcast `desay.thirdparty.navigation`** - работает и протестирован
2. ✅ **APK извлечены** - готовы к декомпиляции
3. ✅ **Сервисы найдены** - понимаем архитектуру
4. ⚠️ **Требуется:** Декомпиляция APK для изучения формата данных

**Следующий шаг:** Декомпилировать APK и найти структуру данных для broadcast.

